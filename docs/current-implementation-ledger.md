Warning: truncated output (original token count: 651896)
... 1559005 bytes omitted ...

# Prestige Limo Ops — Current Implementation Ledger

Latest verified application source checkpoint:
de37e129 Merge pull request #474 from Prestigelimosg/codex/customer-company-booker-root-repair

Latest pushed `origin/main` application checkpoint:
de37e129 Merge pull request #474 from Prestigelimosg/codex/customer-company-booker-root-repair

Latest separately observed `origin/staging` pointer:
6c6ad94c Merge PR #323: Scroll customer booking details into view

Latest directly observed Production deployment checkpoint:
`dpl_HQrZAN9cXkGQffv6yrXzKBtDbQYr` is `READY` on the Production target and its inspected Vercel metadata directly identifies exact merged commit `de37e1290611afaab364493e8817afc090974885`.

Purpose:
This file is the repo source of truth for Codex and future work. Inspect this file before adding new UI, API, helper, test, or docs.

## Monthly Billing Eight-Booking Owner-Confirmed Cancellation Repair (2026-09-02)

- The owner explicitly classified exactly eight historic completed bookings as Cancelled: public references `10861`, `10863`, `10869`, `10876`, `10880`, `10881`, `10889` and `10897`, rows `183`, `185`, `191`, `198`, `202`, `203`, `211` and `219`. No Customer, Company, Booker or Traveller identity is inferred or amended by this repair.
- Migration `20260902021000_monthly_billing_eight_cancelled_repair.sql` is one guarded SERIALIZABLE compare-and-set transaction. It changes only the exact eight booking rows' `status`, `admin_internal_status`, `customer_facing_status`, `cancellation_review_status` and one fixed `updated_at`. Exact current full-row and protected-row hashes, Customer/Company/contacts/access, invoices/monthly records, Driver links/status/DSP/photo/GPS evidence, messages/notifications, audit and Calendar-link dependencies are locked and compared; any mismatch aborts the complete transaction.
- This classification intentionally preserves the historical Driver Reports evidence on `10863`, `10869`, `10889` and `10897`: their booking headers become Cancelled while their existing OTW/OTS/POB/JC evidence remains untouched. It does not delete a booking, closeout, Driver event, link, photo or GPS row, and it creates no customer/driver notification or provider request.
- The existing Monthly grouping reads only `admin_internal_status=completed`, so the exact eight rows leave the Monthly action sector after the authoritative status change. The established Completed / History and customer saved-booking readers already map the explicit persisted Cancelled values to the visible `Cancelled` label; there is no blind dismissal or second cancellation lane.
- The emergency rollback plan `supabase/rollback-plans/20260902021000_monthly_billing_eight_cancelled_repair_rollback.sql` is not automatic. It can restore the exact prior status fields and original timestamps only when every exact repair field and protected booking hash still matches; any later amendment fails closed and requires a new owner-reviewed plan.
- Focused protection is `scripts/test-monthly-billing-eight-cancelled-repair-guard.mjs` plus the existing Monthly grouping/action-queue, Completed / History, customer status, Driver evidence, Calendar, notification, invoice and privacy guards. Invoice/payment, Monthly draft writers, Customer access, both Calendar lanes, Driver assignment/reports, messages, notifications, push/badges, payout/PayNow and providers remain unchanged.
- Production passed the complete forward rollback-only rehearsal before apply. The first rehearsal safely aborted before its UPDATE because the DSP summary dependency is a grouped read-only view; the migration was narrowed to lock its existing base event rows while retaining the summary in both dependency hashes, and the complete corrected rehearsal then passed with no persisted change. Fresh pre-read proved all eight original full hashes, protected aggregate `b76180ef9dbf9b5c9e26cacb5093c6c9`, 762 dependency rows and dependency hash `70cfb10dd464f5fe6a2cf5eb6f465d79` exact before the one committed transaction.
- Immediate post-read proves eight Cancelled rows, zero target rows remaining in the Monthly `admin_internal_status=completed` source and exact post full hashes: `10861` `3b2417c3f6891c64b9ad556b76261ee6`, `10863` `d1f4303c8abe20f2f73c7199263a51b0`, `10869` `516927eb7e7c16b2b7cfec398026a164`, `10876` `9ebf54d10e67f9fffc85443a64c37a91`, `10880` `e8bacfd89358b19aa580e17506f0226a`, `10881` `5eb02da8e0852d89d4591468b052042d`, `10889` `8afcda4a99ae4e0f95b6eb988be9c1de` and `10897` `77ed33c5bab9da71ac9e6b2759822005`. The protected aggregate, 762-row dependency count and dependency hash remain identical. The emergency CAS rollback was also executed with its final commit forced to `ROLLBACK`; it passed, and a second reread proved the exact eight post-repair hashes and Cancelled values remained.
- Signed-in light-mode Production Mac Chrome reloaded Dashboard and showed `0 jobs need Monthly Billing action for August 2026`; none of the eight references remained in the Monthly action sector. Completed / History was opened, August selected and all eight exact public references searched one by one; each returned one exact row with the visible `Cancelled` label. No action, deletion, notification, Calendar or provider control was clicked.

## Monthly Billing Action Queue And Exact-Booking Review (source checkpoint 2026-09-02)

- The existing Dashboard `Monthly Billing Draft` notification card keeps the established monthly grouping reader and adds no panel, route, writer, persistence, schema or dismissal state. It is an action queue: only current `Needs review` and exactly proven `Unpaid` rows are rendered. `Paid`, Billing-ready and already-invoiced/covered rows stay in the authoritative grouping result but are omitted from this action sector because no Monthly action remains.
- Each actionable row's exact booking reference/title is one compact 44px `Review` control that uses the existing guarded exact saved-booking GET and Dispatch loader. The status remains a separate nonclickable pill. Rows leave automatically after a later authoritative grouping read no longer classifies them as actionable; there is no browser-only hiding, blind dismissal or `Resolve` control.
- `Paid` or `Unpaid` is displayed only when the grouping reader finds exactly one unambiguous issued invoice record covering the exact Customer + Company + Booker + internal/public booking reference and its persisted status is exactly `Paid` or `Unpaid`. A missing record ID, zero match, multiple invoice records, unknown status or ambiguous identity returns no payment label; invoice coverage classification remains unchanged.
- Focused protection is `scripts/test-admin-monthly-billing-grouping-read-contract.mjs`, `scripts/test-codex-monthly-invoice-draft-auto-preparation-guard.mjs` and `scripts/test-admin-monthly-billing-dashboard-review-recheck-browser.mjs`. Fixtures prove exact Paid/Unpaid, unknown status, duplicate ambiguous invoices, same-record internal/public reference deduplication, one exact Dispatch GET under double click, the absence of non-actionable rows and `Resolve`, plain status pills, a 44px Review control, missing-reference fail-closed behavior, desktop/390px light-mode layout and zero mutation/protected-lane writes.
- Invoice layout/lifecycle, monthly draft preparation/writers, Customer/Company/Booker identity, Customer access, either Calendar, Driver, messaging, notifications, push/badges, payments, payout/PayNow, providers and Production data are unchanged. The stale dirty parked worktree `/private/tmp/prestige-admin-monthly-needs-review-20260901` remains untouched.

## Monthly Billing Completed Closeout And 10857 DSP Production Repair (2026-09-02)

- The owner authorized the established Billing-ready closeout result for exactly 21 completed August jobs: `10853`, `10858`, `10862`, `10864`, `10865`, `10870`, `10871`, `10872`, `10873`, `10877`, `10878`, `10883`, `10885`, `10888`, `10890`, `10896`, `10903`, `10911`, `10912`, `10914` and `10918`. Migration `20260902100000_monthly_billing_twenty_one_closeout_repair.sql` inserts only the exact `completed_booking_closeouts` rows produced by the existing `Ready Locally` / Billing-ready control contract. Every target booking full-row hash and the Driver link/status/DSP, invoice, monthly-draft, Customer-access and notification scopes are locked and compared before and after; any drift aborts the serializable transaction.
- The owner separately supplied exact missing timing evidence for DSP booking `10857` / row `179`: use its saved pickup `1 Aug 2026 12:00 SGT` (`2026-08-01T04:00:00Z`) as billing start and exactly two hours later, `14:00 SGT` (`2026-08-01T06:00:00Z`), as corrected billing end. Migration `20260902100100_monthly_billing_10857_dsp_closeout_repair.sql` appends one exact existing-lane Admin correction event with reason `Owner instructed saved pickup plus exactly two hours for Monthly Billing review.` and then one exact Billing-ready closeout in the same guarded serializable transaction.
- The 10857 correction does not overwrite its booking or Driver evidence. Immediate post-read proves booking hash `c48a974a7603bf52511187e99575d001`, saved pickup `04:00Z`, scheduled drop-off `12:16Z`, completed status, Customer `163`, Company `31`, Booker `24`, null Traveller and zero Driver status events remain exact. It creates only correction event `206d5866-e808-4155-abad-edf1d016daad` and closeout `70853358-525b-489b-93fc-e5236d9e257d`; invoice, monthly-plan and monthly-invoice counts remain zero.
- Both migrations passed a complete Production rollback-only rehearsal first. Independent rereads proved zero target closeouts and zero 10857 DSP events remained after rehearsal. Each approved transaction then committed once. All 21 standard targets now have one exact `ready_for_billing_prep` / completed / DSP-ready / extra-charges-ready / billing-prep-ready closeout with `source_surface=admin_api`, `actor_role=admin` and `actor_label=Owner Admin`; their exact booking hashes remain unchanged.
- The authoritative August completed set now consists of 22 Billing-ready rows, three exactly covered `Paid` rows (`10859`, `10860`, `10866`) and eight unresolved identity rows (`10861`, `10863`, `10869`, `10876`, `10880`, `10881`, `10889`, `10897`). There is no exact Unpaid row. After the action-queue application deployment, a fresh read therefore renders only those eight identity blockers; it does not delete or dismiss the 22 Ready or three Paid source records.
- Customer `192` is the owner-confirmed Codex QA account, not a real-customer identity source. It has only booking `10916` / row `239`, whose status is `admin_review_required`, not completed, with zero closeout, invoice, DSP or Driver status records; it is outside the current Monthly completed set and remains untouched.
- The migrations do not update or delete any row. Emergency rollback is limited to the exact migration-created event/closeout IDs and would require a separate explicit owner-approved guarded transaction; there is no blind UI rollback or deletion path. Bookings, statuses/timestamps, prices/rates, invoices/PDF/email/payment, monthly draft writers, Customer/Company/Booker/Traveller profiles, Customer access, either Calendar, Driver links/reports, messages, notifications, push/badges, payout/PayNow and providers remain unchanged.
- Focused protection is `scripts/test-monthly-billing-action-queue-production-repair-guard.mjs`, the existing grouping and DSP correction guards, and `scripts/test-admin-monthly-billing-dashboard-review-recheck-browser.mjs`.

## Existing-Booker Atomic Customer Profile Save Repair (source checkpoint 2026-09-02)

- Production diagnosis of Customer `183` proved two separate profile requests. The first `PATCH /api/admin-customer-accounts` succeeded and created exact Booker `37`; the immediate second PATCH failed atomically with PostgreSQL `column reference "company_id" is ambiguous`. The failed retry produced no audit row and left the Customer, Company and Booker `updated_at` values at the preceding successful transaction time. Customer `183` was not retried or changed during this repair.
- The defect was confined to the existing-Booker UPDATE branch of `apply_admin_customer_company_booker_profile`: the function returns a `company_id` output variable while its Booker predicate used an unqualified `company_id` column. The forward migration `20260902013000_fix_customer_company_booker_existing_booker_ambiguity.sql` retains the established function, authorization, optimistic exact-current checks, transaction, audit and rollback boundaries and qualifies only the target Booker's `id`, `company_id`, `customer_id` and returned row through one `target_booker` alias.
- Focused protection is `scripts/test-customer-company-booker-existing-booker-rpc-guard.mjs`, the existing root/API/profile/identity guards and the extended light-mode Customer profile browser acceptance. The browser fixture now proves save → authoritative reload → navigate away/reopen → second edit/save of the same existing Booker → authoritative reload → navigate away/reopen, with the same exact Customer, Company and Booker IDs on both writes.
- The focused RPC/root/API/profile/identity/Dispatch, public privacy, Admin/Customer/Driver push, locked invoice and personal Driver Calendar guards pass; TypeScript and the Production build pass. Visible local light-mode Mac Chrome passed the double existing-Booker save/reload/reopen sequence with two exact mocked PATCHes and no Production request. The unchanged Operations Calendar API contract guard retains its already-recorded current-main mismatch at `if (assignmentOnly) { const retainedAssignmentBooking`; this repair does not alter or weaken that guard or any Calendar source.
- No Customer record is converted by the migration. Bookings, invoices/billing/monthly, Customer access, either Calendar, messages, notifications, push/badges, Driver, rates, payments, payout, PayNow and providers remain outside the function and unchanged.
- PR `#476` merged the reviewed forward migration, guards, browser acceptance and source checkpoint to `main` as `3797bb877d9339adafbbb9cc805c09b7bcedd1c2`. The authenticated Production Supabase runner applied only `fix_customer_company_booker_existing_booker_ambiguity` as remote version `20260901171353`; authoritative function reread proves the aliased Booker UPDATE and qualified Company/Customer predicates are active, `service_role` retains execute and `anon`/`authenticated` do not. Customer `183`, Company `58` and Booker `37` retain exact `updated_at` `2026-09-01 16:59:29.176572+00`, and zero later profile-overwrite audit rows prove activation changed no Customer data.
- Vercel Production deployment `dpl_HPSergCcX6yNNzTv5DVfLok6DkTJ` is `READY` for exact merge `3797bb87`; its deployment-scoped Production error/fatal scan is clean. Signed-in light-mode Mac Chrome read-only acceptance navigated away from and reopened Customer `183`, reloaded authoritative title `Ms Amanda (amanda)`, and reopened the exact editor with Company `Ms Amanda`, Booker `amanda`, blank optional Booker email/contact and zero Travellers. It performed no save or mutation; the owner retains the explicit next retry.

## Atomic Customer + Company + Booker Profile Root Repair (source checkpoint 2026-09-02)

- This checkpoint supersedes the older split Customer-folder, Company-profile and nested-Booker save behavior. The one existing exact-Customer profile editor now saves the raw `customers.display_name`, every existing visible Company profile field, and the exact Booker name/email/contact plus Company/Customer binding through one server-only `PATCH /api/admin-customer-accounts` action and one guarded database transaction. The transaction locks the exact Customer, Company and Booker rows, compares the exact-current Customer name, full Company profile, full Booker profile and Booker binding, preserves all existing IDs, clears only the retired `customers.customer_type` classification, records a safe audit entry, and rolls back the whole save on stale or conflicting evidence.
- Company and Booker are mandatory for every amended or future Customer profile. Admin must explicitly select or create each identity; no Company, Booker or account title is inferred from the raw folder, booking, Passenger, Traveller, boss, email, phone or fuzzy name. Traveller is optional and remains separately editable. The optional Traveller editor treats the authoritative Booker as read-only and cannot overwrite it through its older nested path.
- Customer Billing Overview, the exact-Customer banner and the Dispatch Customer Account dropdown reuse `formatVerifiedCustomerAccountTitle`. A title is exactly `Company (Booker)` only when one linked Booker and its exact Company are verified; otherwise it is the compact `Customer account · Requires editing`. The raw Customer folder remains searchable and editable but never becomes the visible account title. Passenger, Traveller and boss records remain booking/search evidence only and never form the title.
- Future hotel/agency/personal Customer creation is retired. The visible creation choice is only `Create Company + Booker Account`; the booking parser rejects retired legacy creation payloads, and the legacy agency magic state fails closed before any Company or booking write. Existing legacy records are not batch-converted, deleted or recreated. Admin amends them one at a time through the same editor, preserving their Customer, Company, Booker, Traveller and booking IDs.
- The database function is `SECURITY INVOKER`, executable only by `service_role`; `public`, `anon` and `authenticated` execution is revoked. Existing table mutation grants for Customer/Company/Booker/Traveller are also revoked from browser roles, while the established same-origin verified Admin/Dispatcher session and purpose boundary remains the only exposed editor path. No arbitrary table, SQL or public/authenticated update access is added.
- Focused protection is `scripts/test-customer-company-booker-profile-root-guard.mjs`, `scripts/test-customer-company-profile-contract.mjs`, `scripts/test-customer-profile-verified-identities-guard.mjs`, `scripts/test-admin-customer-accounts-read-api-contract.mjs` and `scripts/test-admin-dispatch-crm-identity-selectors-guard.mjs`. Bookings and saved booking fields, invoice/billing/monthly, Customer access, either Calendar, messages, notifications, push/badges, Driver, rates, payments, payout, PayNow and providers are outside this transaction and remain unchanged.
- PR `#474` merged the exact reviewed source to `main` as `de37e1290611afaab364493e8817afc090974885`. The authenticated Production migration runner then applied only `customer_company_booker_profile_transaction` as remote version `20260901163606`; immediate readback proves 27 active Customers and 16 Bookers remained, zero Bookers lack Company/name, zero Customer IDs have duplicate Booker bindings, and zero `customer_company_booker_profile_overwrite` audit rows exist because no Customer profile was converted or saved during activation. The function exists with `service_role` execute only, browser roles retain no Customer-table update privilege, and the mandatory Booker Company/name constraints are present.
- Vercel Production deployment `dpl_HQrZAN9cXkGQffv6yrXzKBtDbQYr` reached `READY` for exact merge `de37e129`; the 15-minute post-deploy runtime-error scan was clean. Signed-in light-mode Mac Chrome read-only acceptance loaded all 27 Customer profiles and showed `Bridge Data Centres (Laurel Wong)` in Customer Overview, exact profile, reopened profile and the still-open editor. Dispatch showed the same exact title plus `Tiger Global (June)`, `The Law Society of Singapore (Danielle)` and other verified Company+Booker titles; incomplete records showed only `Customer account · Requires editing`. The local intercepted fixture separately proved one atomic edit/save plus navigate-away/reopen with no second write; it never reached Supabase or Production.

## Authoritative Company + Booker Customer Account Titles (source checkpoint 2026-09-01)

- The existing Customer Billing Overview and Dispatch `Customer Account` chooser now use one shared presentation formatter for corporate titles: exact verified `Company (Booker)`. The existing IDs, account grouping, Customer links, option keys, selection behavior, Save + CRM writers and invoice consumers remain unchanged.
- The existing guarded customer-account reader reuses its one authenticated route and adds only bounded safe Company and Booker directory projections. A Booker name is displayed only when its exact stored Booker ID revalidates against the same Customer and Company. Missing, partial, duplicate or inconsistent identity falls back to the stored Company or Customer directory label; booking customer labels, Passenger, Traveller and Boss text never become the corporate account title.
- The Dispatch chooser uses the same formatter with its already-loaded verified Rate Setup Company and Booker rows. Its bounded loaded-legacy fallback may show only the directory Company label until an exact same-Customer Booker record exists. Passenger/Traveller records remain search evidence and selection consumers only; they do not overwrite the visible account title.
- Focused protection is `scripts/test-admin-customer-accounts-read-api-contract.mjs`, `scripts/test-admin-dispatch-crm-identity-selectors-guard.mjs`, `scripts/test-admin-dispatch-customer-account-browser.mjs` and `scripts/test-customer-billing-overview-pagination-browser.mjs`. Synthetic evidence covers `Tiger Global (June)` overriding `Tiger Global [Stanley Ho]`, mismatched Booker fail-closed fallback, unchanged exact IDs/links/selection, desktop and 390px light-mode layout, and zero mutation requests. No Production record, booking, Company, Booker, Traveller, invoice, monthly, access, Calendar, message, notification, Driver, native, provider, schema or environment change is part of this source repair.
- Visible Dispatch verification completed the requested desktop and 390px Company/Booker title, selection and zero-write assertions before the owner-accepted unrelated narrow-harness observation that Chrome reported `window.innerWidth` 328 after a requested legacy 320px viewport; its containment, relative menu, no-overlap and zero-overflow assertions had passed, and that old exact-width contract was not changed. The unrelated `test-admin-booking-persistence-api-gate.mjs` temporary compilation also aborted before assertions because its stale copy list omits the existing Driver push dependency; the owner directed that harness and every push/notification lane remain untouched. Unchanged Admin browser/native, Customer, Driver and public-surface push/notification guards pass, proving no notification regression from this title-only repair. The complete app-smoke and nine-viewport mobile suites pass with zero test/console errors; the unrelated legacy Booking UI suite stops at its current mock-AI draft expectation before reaching this title lane and was recorded without changing that suite.

## Customer Directory Label And Profile Dirty-Scope Repair (source checkpoint 2026-09-01)

- The existing authenticated Customer-account reader now returns two deliberately separate safe labels: `customer_account` remains the verified presentation title `Company (Booker)`, while `customer_directory_label` is the exact raw `customers.display_name` used by the Customer folder editor. A formatted title, booking Passenger/Traveller/Boss text and a contaminated Company name can no longer populate or overwrite the editable Customer folder field.
- The existing Customer profile editor reads and verifies only the raw directory label for folder input, PATCH comparison and save read-back. Company and Customer-folder drafts have independent dirty scopes: a folder-only save uses only the established exact-Customer PATCH; an unchanged Company performs no POST. A Company-only save uses the established Company writer with only the exact changed fields plus its record identity, so a stale loaded full profile cannot be reposted over newer data. Create mode, explicit clears, Booker/Traveller separation, folder route refresh and the existing visible confirmation remain intact.
- Focused protection is `scripts/test-admin-customer-accounts-read-api-contract.mjs`, `scripts/test-customer-company-profile-contract.mjs`, `scripts/test-customer-company-profile-browser.mjs` and `scripts/test-customer-corporate-identity-browser.mjs`. Synthetic evidence covers a contaminated authoritative Company, independent raw folder persistence, formatted-title exclusion, folder-only zero-Company-write behavior, one-field Company update payloads and the unchanged verified Booker/Traveller editor handoff. Visible light-mode Mac Chrome passed the Customer profile and Billing Overview desktop/390px checks with zero overflow and no unintended mutation; Dispatch desktop/390px title/selection also passed before the already documented unrelated 320px Chrome viewport-harness drift (`328` reported for requested `320`).
- This repair adds no route, writer, panel, schema, migration or identity fallback. Invoice/monthly billing, booking persistence, Company+Booker identity, Calendar, messaging, Driver, push/badge/notification, native, provider, environment and Production data lanes are unchanged by the source repair.

## Admin Native Single-Start WebView Readiness Repair (source checkpoint 2026-09-01)

- The established `Prestige Limo Ops` iOS Admin companion now waits for all four native bootstrap inputs—installation ID, Face ID preference, saved notification token and current notification permission—before mounting or navigating the one Production WebView. Cold startup therefore mounts once with the final installation/notification bridge instead of mounting early and then remounting through the former unconditional preparation-time navigation-key change.
- The injected bridge emits one strict `admin_native_web_ready` message at `DOMContentLoaded`, or immediately when the document is already beyond `loading`. Native accepts only that exact one-field message, cancels the active 15-second readiness timer and marks the current WebView ready. Stale, extended, unknown and malformed bridge messages fail closed, and the existing `onLoadEnd` remains an idempotent readiness fallback.
- A truly non-ready/error/content-process load retains one automatic 15-second recovery followed by the existing manual `Reload Admin screen` state on a second failure. Normal backgrounding still pauses the timer and foregrounding resumes it without remounting. The same mounted WebView continues preserving in-memory OTP state, cookies, current URL, Face ID privacy-lock behavior and the established notification subscription/badge bridge.
- Focused executable protection is `scripts/test-admin-companion-webview-recovery-guard.mjs`, `scripts/test-admin-companion-background-otp-preservation-guard.mjs`, `scripts/test-admin-biometric-single-flight-guard.mjs` and `scripts/test-admin-companion-foundation-guard.mjs`. Evidence is source inspection and local focused guards only. Physical iPhone runtime remains untested; no native build, provider call, Production navigation, deployment or external action occurred.
- Invoice, Customer/account identity, booking/API/web Dashboard, either Calendar, messaging, schema, providers and push/badge/notification semantics remain unchanged.

## Admin Ask AI Read-Only Booking Review And Dispatch Navigation Extensions (source checkpoint 2026-09-01)

- Four bounded additions stay inside the one existing Dispatch `Ask AI` panel and authenticated `POST /api/admin-ai-assistant` route. No second Assistant panel, route, model tool, Customer folder, invoice workbench, Calendar lane, notification lane or writer is added. The deterministic prompt grammars are: `Show monthly billing blockers for <fixed reason> [for <previous month>]`; `Show billing readiness for booking <exact reference>` / `Why is booking <exact reference> blocked for billing?` / `Is booking <exact reference> already invoiced?`; `Show upcoming jobs for <exact Booker> [at|for|from <exact Company>]`; and `Show tomorrow's operations brief` / `Show operations brief for YYYY-MM-DD` / `Show flight <exact flight> jobs for YYYY-MM-DD`.
- Monthly blocker filtering uses only the fixed safe reason enum `billing_closeout`, `completion_evidence`, `dsp_billing_time`, `extra_charges`, `inconsistent_identity`, `missing_closeout`, or `missing_identity`. The established grouping reader remains authoritative. One narrow classification repair reports `Already invoiced` when a group has issued-invoice coverage and zero Ready/Blocked jobs even if the legacy aggregate label says Blocked; it does not change, create, save, lock, issue, select or send any draft or invoice.
- Exact-booking billing readiness is merged into the existing Booking Brief card. The helper first revalidates one active Customer and the exact `customer_id + company_id + booker_id` relationship, including the Booker's stored Company and Customer bindings, before reading issued-invoice evidence. Because `customer_invoice_records` has no Company column, Company isolation is enforced by that exact verified Booker relationship; invoice rows are then restricted and rechecked by exact Customer plus Booker and exact internal/public booking reference. A partial, duplicate or cross-Company identity fails closed before the invoice read. No invoice number, amount, payment state or invoice action is returned.
- Exact Company+Booker upcoming jobs reuse a new programmatic function in the established server-only saved-booking reader. It queries only the verified Customer+Company+Booker tuple with canonical pickup at/after the read time, orders by pickup, and fails closed above 250 rows. The Account Brief filters completed/billed/closed rows and returns ten safe rows per page. Names, labels, Passenger, Traveller, email and phone never replace the exact tuple.
- Tomorrow and exact-date Operations Briefs reuse the same Today's Work result card. Their saved-booking source is an exact Singapore-day range (`00:00` inclusive to next `00:00` exclusive), ordered by pickup and failed closed above 100 rows; they do not scan the all-bookings list. Driver Job Link and latest Driver-status evidence is read only by each returned exact booking reference, and identity revalidation is limited to those returned jobs. Exact flight matching normalizes spacing/case but remains exact and explicitly is not live airline status. The established `What needs my attention today?` monitorable snapshot remains unchanged.
- Booking labels in these structured results are user-clicked navigation only. They call the existing guarded exact saved-booking GET, load the same existing Dispatch editor state without recording a Customer-request dismissal in browser memory, explicitly select the existing phone `Details` step, then scroll and programmatically focus the existing focusable `Booking Details` section. The click never calls POST/PATCH/DELETE, saves a booking, selects an invoice job, creates/issues/sends an invoice, marks payment, changes billing timing, acknowledges/completes a job, or changes Customer, Driver, Calendar, messaging, notification, reminder or provider data. No invoice auto-focus or Customer-folder/invoice component change is included.
- Focused protection is `scripts/test-admin-ai-bounded-booking-read-guard.mjs`, `scripts/test-admin-ai-monthly-billing-review-guard.mjs`, `scripts/test-admin-ai-booking-brief-guard.mjs`, `scripts/test-admin-ai-account-brief-guard.mjs`, `scripts/test-admin-ai-todays-work-brief-guard.mjs`, and `scripts/test-admin-ai-assistant-guard.mjs`. All six guards, TypeScript, diff check and zero-error lint pass; lint retains the existing warning-only baseline. The Production build passes after using the package-lock-matched local dependency copy. Unchanged Pending Driver ACK, Admin completion, personal Driver Calendar, issued-invoice eligibility, multi-job invoice handoff, stored PDF/portal, invoice lifecycle and Admin notification guards also pass.
- Visible light-mode local Mac Chrome acceptance used intercepted synthetic Assistant and exact-booking data only. Desktop rendered monthly blocker, exact-booking readiness, exact Company+Booker upcoming-job and exact flight+date cards; the navigation label made one exact saved-booking GET, loaded the established editor and focused `Booking Details`. At 390×844, the upcoming-job card had no horizontal overflow and its same navigation button selected the existing phone `Details` step, loaded the exact synthetic booking and focused the same section with `tabindex=-1`. The focused browser regression reports four intercepted Assistant requests, one exact booking GET, zero unexpected mutation requests and zero actionable console errors. No Production/provider read or write, model call, external send, deployment, push, PR or merge occurred.

## Admin Ask AI Floating Launcher (source checkpoint 2026-09-01)

- The one existing Admin Ask AI entry now has one presentation-only floating `Ask AI` launcher on the established Admin Ops shell. Desktop and phone both use that exact visible label at the lower right; the phone button remains a compact pill with the browser safe-area inset included. It adds no icon, overlay, modal, drawer, chat board, AI mode selector, route, request helper or second result lane.
- Clicking the launcher only selects the existing `Dispatch` tab, selects the phone `Message` step, switches the existing Dispatcher Intake mode to `Ask AI`, then scrolls to and focuses the same established textarea. A currently typed Dispatcher Intake message and existing Ask AI history/result remain in the same mounted state. If an AI request is already running, the launcher is disabled and cannot interrupt it.
- The launcher never checks the AI safety checkbox, sends a prompt, calls `/api/admin-ai-assistant`, clears text or history, creates a Job Card, loads a booking, navigates to a Customer account, or performs any write or external send. The established explicit safety acceptance and `Send to AI` action remain required, and `Create Job Card` remains disabled while Ask AI mode is selected.
- Focused protection remains `scripts/test-admin-ai-assistant-guard.mjs` and adds `scripts/test-admin-ai-floating-launcher-browser.mjs`. The synthetic light-mode browser acceptance opens the same board from another Admin tab at desktop and phone widths, proves one launcher, one board, one selector, preserved draft text, focused input, mobile safe-area placement, no horizontal overflow, zero Assistant requests and zero mutation requests before `Send to AI`. The complete mocked Booking UI browser passed in `471.8s` with zero test, console, Supabase-request or mutation errors; mobile usability passed all nine phone/tablet/desktop widths in `75.7s`; App smoke passed in `276.9s`; TypeScript, zero-error lint and the Next.js Production build passed. Lint retains only existing repository warnings. A clean-main-identical stale source-marker failure remains in the existing Operations Calendar API contract guard and was not changed in this presentation-only task.
- The exact Company + Booker read skills, structured result cards, single authenticated Assistant POST, parser, Customer/invoice layout and controls, invoice PDF/email/payment, personal and Operations Calendar, Driver Reports, Pending ACK Queue, messaging, native apps, schema, environment, providers and Production data remain unchanged. This source change carries no Production deployment approval or deployment action.

## Test Harness Dependency And Ledger Checkpoint Maintenance (source checkpoint 2026-09-01)

- This bounded maintenance changes only four existing test scripts and this ledger. It does not change any application, library, API, schema, migration, environment, provider, data, UI, invoice, Customer, booking, notification, Driver or Calendar runtime lane.
- The two monthly sequencing guards no longer classify the append-only ledger's documented public Supabase project reference as a secret. Their real-secret checks remain active for secret environment assignments, Supabase URLs and JWT-shaped values, and each guard contains test-of-test samples proving those detections before scanning the protected source and documentation.
- The Admin app-notification API contract now supplies one fail-closed temporary `driver-device-push-notification` dependency required by the established booking adapter. The booking-persistence kill-switch harness now supplies the current route dependency graph through isolated temporary modules; every notification, push, email, Job Card, invitation, OTP and portal provider stub throws if the kill-switch test unexpectedly enters that lane. Its verified Customer test identity is an exact `customer_id + company_id + booker_id` tuple used only to reach and prove the existing disabled-write assertions.
- Clean-main fail-first evidence was exact: both monthly guards stopped only on the historical project-reference regex, the notification contract stopped before assertions on the omitted Driver push module, and the kill-switch harness stopped before assertions on its stale temporary dependency graph. After the test-only corrections, all four focused guards pass and the original workflow, privacy, zero-provider and zero-database-mutation assertions remain unchanged.
- Companion Admin notification, Automation, monthly grouping/draft, monthly auto-preparation and booking-adapter guards pass. Lint completes with the existing warning-only baseline, the Production build passes, and localhost browser gates pass for Rates, the full Booking UI in 468.5 seconds, Production-disabled Driver Job, nine mobile/tablet/desktop viewports, Customer profile, exact corporate identity and the full app smoke in 279.7 seconds; the completed browser gates report zero errors and zero console errors. The sequential `test:safe` unit/AI/lint/build portion also passes, but its initial Rates browser step found no localhost server because that script does not start one; the same Rates gate passed after the established local server was started.
- Two unmodified companion harnesses, `scripts/test-core-booking-persistence-safe-path-guard.mjs` and `scripts/test-admin-booking-persistence-api-gate.mjs`, independently stop before their assertions on the same omitted temporary Driver-push dependency. They are attributed as additional clean-main test-harness debt and are not widened into this owner-approved four-test repair.
- The separate clean-main Driver Job browser timeout at `POB blocked status feedback` is not included in this repair. It remains a diagnosis-only follow-up with no Driver, status, mock-store or Calendar source changes unless a distinct runtime defect is proven and separately approved.

## Admin Ask AI Exact Invoice Payment Preparation (source checkpoint 2026-09-01)

- Phase 6 stays inside the one existing Dispatch `Ask AI` panel and authenticated `POST /api/admin-ai-assistant` route. It recognizes only `Prepare invoice <exact invoice number> to mark paid` and `Help me mark invoice <exact invoice number> paid`. Bulk, Booker-name, Company-wide, malformed, additional-action and prompt-injection wording stops before every invoice, identity and model read.
- The server resolves one exact stored invoice number and revalidates one complete `customer_id + company_id + booker_id` account through the exact stored invoice Booker ID, Booker-to-Customer/Company relationship and Company record. Same-Company Bookers remain separate. A duplicate invoice number, missing or inconsistent identity, missing record, quotation, credit note or draft returns zero invoice detail and no preparation; Customer/company display text, Booker name, Passenger and Traveller never establish the lookup.
- One exact issued Unpaid invoice returns a structured read-only review card outside model and browser history. It shows only invoice number, exact Company · Booker and IDs, Paid/Unpaid, total and calculated full balance, issue/due dates and stored booking references. It lists the choices that remain explicitly human-owned in existing Section 2: `Bank transfer` / `Card` / `Cash` and whether to send the payment thank-you. An already Paid invoice remains non-executable and reports a zero balance.
- Ask AI never calls the existing invoice PATCH/POST writers, changes Paid/Unpaid, chooses a payment method, confirms receipt, sends a thank-you, issues/regenerates/downloads/emails a PDF, charges a provider or writes any invoice/payment data. The route still reports `write_action:false` and `external_send:false`, and the structured result never enters OpenAI or conversation history.
- The only handoff is a user-clicked `Open Customer Account` to the established exact Customer folder. Phase 6 deliberately does not add an invoice-number query parameter or modify/focus the owner-locked invoice panel because no established safe invoice-focus input exists. Admin manually selects the exact invoice in `2 · Total invoices`, reviews the payment method and thank-you choice, then uses the one existing `Mark paid` confirmation.
- No invoice page, workbench, control, action route, writer, schema, migration, payment provider, PDF/email lane, Calendar, Driver, monthly billing, messaging or Email AI behavior changes. The approved invoice section order, selected-invoice action bar and explicit external-send choice remain intact.
- Focused protection is `scripts/test-admin-ai-invoice-payment-preparation-guard.mjs` plus the unchanged Assistant, exact-invoice search, reminder/payment, invoice lifecycle and layout guards. Fixtures cover exact Unpaid/Paid, missing and duplicate invoice numbers, wrong Booker and incomplete identity, same-Company/two-Booker isolation, quotation/credit/draft rejection, booking-reference privacy, bulk/name/injection rejection, zero mutation/model/history leakage and light-mode synthetic browser acceptance.

## Admin Ask AI Monthly Billing Review PA (source checkpoint 2026-09-01)

- Phase 5 stays inside the one existing Dispatch `Ask AI` panel and authenticated `POST /api/admin-ai-assistant` route. It recognizes only `Show monthly billing review`, `Which monthly billing drafts need attention?`, and an optional exact previous Singapore billing month such as `August 2026` or `2026-08`. The attention wording returns only `Ready`, `Blocked`, and `pending_admin_review` accounts. Another month, action-shaped wording, malformed input, or prompt injection stops before every monthly source and model read.
- The read-only helper reuses the established monthly grouping, saved draft-plan, saved invoice-draft, issue-record lock and Rate Setup identity readers. It never calls the first-of-month orchestrator or cron route and never creates, refreshes, updates, locks, issues, numbers, renders, sends, pays or schedules a draft or invoice. It adds no monthly route/page/panel, scheduler, notification, schema, migration, provider or second review lane.
- Every verified row is grouped only by exact `customer_id + company_id + booker_id + billing_month`, then revalidates that exact Booker against the same Customer and Company before displaying Company · Booker or a Customer-folder link. Same-Company Bookers remain separate; cross-Booker issue records cannot lock another draft. Missing, partial, inactive or inconsistent identity remains a separate `Blocked` manual-review row with names and Customer link withheld. Customer-account display text, Passenger and Traveller are never Booker fallbacks.
- The card preserves the established meanings: grouping classifications show `Ready`, `Blocked` and `Already invoiced`; a saved invoice draft may show exact `pending_admin_review`; and only the existing non-archived/non-voided issue-record `locked_for_issue` evidence shows `Locked`. It displays exact safe trip/reference counts, booking references, bounded blocked reasons, saved plan/draft status and read time without amounts, rates, invoice numbers, internal notes, contacts, payout/PayNow or provider state.
- Results are server-computed in ten-row pages and remain structured outside the model and browser conversation history. `Load more` appends only unique exact row keys. `Open Customer Account` and `Open monthly billing review` are manual links/handoffs to the established Customer folder and Dashboard review; there is no automatic navigation or action.
- Focused protection is `scripts/test-admin-ai-monthly-billing-review-guard.mjs` plus the unchanged Assistant, Company+Booker grouping, draft-plan, invoice-draft and automatic-preparation guards. Synthetic fixtures cover zero results, exact identity, same-Company/two-Booker separation, cross-Booker lock isolation, partial identity, Ready/Blocked/Already invoiced/`pending_admin_review`/Locked states, exact references/counts, previous-month parsing, pagination, injection, privacy and zero mutation/model fallback.
- Pre-change monthly grouping, draft-plan, invoice-draft and automatic-preparation guards passed. The two older monthly sequencing guards still reproduce their already-recorded current-main false positive against the ledger's historical Production project reference; Phase 5 changes none of their protected monthly workflow source, selectors, routes or persistence and does not hide that unrelated baseline debt.

## Admin Ask AI Exact Company + Booker Account Brief (source checkpoint 2026-09-01)

- Phase 4 remains inside the one existing Dispatch `Ask AI` panel and authenticated `POST /api/admin-ai-assistant` route. Deterministic read-only intents recognize `Show Su Ling account`, `Show Su Ling's unpaid bookings`, `Show all customers with unpaid bookings`, and the Company-qualified form `Show Su Ling account for Tiger Global` before the model fallback. Action-shaped, malformed and prompt-injection input is blocked before any identity, booking, invoice or model read.
- Every exact result first resolves one verified `customer_id + company_id + booker_id` tuple from the established Company/Booker projection. Duplicate Booker names return zero detail and list the exact Company choices; Company qualification must still resolve one complete tuple. Null/partial/legacy identity returns zero detail with manual Customer-folder guidance. A Passenger or Traveller/boss name is never treated as a Booker, and Company-wide or name-only fallback is forbidden. Same-Company Bookers remain separate accounts.
- `Jobs not billed yet` and `Unpaid invoices` are deliberately separate visible sections. The first reuses the established Section 3 eligibility rule: one exact-tuple saved booking remains eligible only when its safe status is not billed/closed and no exact same-account issued invoice carries its exact internal or public booking reference. The second reads only exact-account `document_type=invoice`, `document_state=issued`, `status=Unpaid` records and displays their stored amounts as unpaid balance. Cross-Booker, draft, quotation, credit-note and null-Booker invoice rows never cover a booking or enter the unpaid list; unknown amounts or statuses are not guessed.
- The exact account card shows only verified Company · Booker identity, safe IDs, upcoming/completed counts, issued-invoice count/total, Jobs-not-billed count, unpaid-invoice count/balance and visible excluded-legacy anomaly notices. The all-Customer summary groups only exact tuples, returns ten rows per server-computed page and uses `Load more` to append unique tuple keys. Every `Open Customer Account` handoff requires a user click; there is no automatic navigation.
- Structured account results remain outside OpenAI input and browser conversation history. The helper reuses the established verified rate-setup and saved-booking readers plus a read-only issued-invoice projection. It adds no Customer or invoice route/page, writer, mark-paid, issue, PDF, email, reminder, payment, payout, PayNow, provider/send, booking mutation, Calendar action, schema or migration, and it does not alter the locked Customer-folder or invoice layout/workflow.
- Focused no-write protection is `scripts/test-admin-ai-account-brief-guard.mjs` plus the unchanged exact-account invoice-search, Booker scope, issued-invoice eligibility, identity and Assistant guards. Synthetic fixtures cover unique and duplicate Bookers, Company disambiguation, same-Company/two-Booker isolation, exact/cross-Booker and legacy-null invoice coverage, Traveller-not-Booker, partial identity, all-account pagination, prompt injection, no mutation, privacy exclusions and zero account-data reads before an exact identity resolves.
- Visible light-mode local Mac Chrome acceptance used only intercepted synthetic data. It rendered the exact account card, separate `Jobs not billed yet` and `Unpaid invoices` sections, verified IDs/counts/amounts/anomaly wording and manual Customer-account link; then it cleared the card, rendered ten all-account tuples, loaded the remaining two on page 2 and cleared again. All three Assistant requests carried empty conversation history, only the existing Assistant POST occurred, zero other mutation requests and zero actionable console errors occurred. Ten expected local background reads failed closed with 403 because the isolated browser had no Admin session. The established canonical-localhost full Booking UI regression also passed all Dispatch, Dashboard, Driver, status, route-assist, Completed/History, Customer privacy and public Driver checks in 475 seconds with zero errors, zero console errors and zero blocked Supabase mutations. No Production customer/invoice read, model call, financial/provider action or external send occurred.

## Admin Ask AI Today's Work Brief (source checkpoint 2026-09-01)

- Phase 3 remains inside the one existing Dispatch `Ask AI` panel and authenticated `POST /api/admin-ai-assistant` route. One deterministic read intent recognizes only `What needs my attention today?` or `Show today's work brief` before the model fallback. Action-shaped, malformed and prompt-injection requests return the read-only instruction with zero Dashboard-source or model calls.
- The brief reuses only the established server readers that already feed the Dashboard: monitorable saved bookings, queued Admin app notifications, newest active Driver Job Links, exact Driver status events, active Customer/Company/Booker CRM identity and blocked monthly invoice drafts. It creates no Dashboard, route, notification, polling store, action executor, Calendar/provider read or external sender. Every source is read through the existing verified Admin/Dispatcher boundary.
- Results are grouped and counted as new/amendment/cancellation Customer booking reviews, urgent unassigned operational jobs inside the established one-hour-before to 24-hour-after monitor window, newest-active-link Pending Driver ACK alerts, active Driver `Job Completed` reports still requiring the established explicit `Admin confirm completed`, and blocked `pending_admin_review`/blocked monthly drafts. The server paginates the filtered structured rows in ten-row pages before returning them; `Load more` appends only unique row keys outside conversation history.
- Every Customer-scoped row enforces the permanent `1 Company + 1 Booker = 1 account/profile` boundary. Complete `customer_id + company_id + booker_id` must re-read one active Customer, the exact Company and the exact Booker bound to both Customer and Company. Same-Company Bookers remain separate. Missing, partial, inactive, cross-Booker and legacy identities stay visible only as manual-review rows with IDs and labels withheld; Passenger, Traveller/boss, Customer display name, email, phone and parser text are never Booker identity fallbacks.
- The structured card contains only exact safe references/IDs after verification, Company · Booker labels, pickup/recorded time, review category, billing month and safe operational explanation. It excludes Customer price, invoice/payment data and amounts, Driver payout/PayNow, Driver/customer contact, Passenger/Traveller labels, internal notes, parser/debug data, link tokens/raw context, provider state and status notes. Returned rows are never added to model input or browser conversation history.
- Handoffs require a user click and reuse the established controls: exact urgent unassigned bookings load through the guarded saved-booking read and existing Dispatch load path; Pending ACK opens the one existing Driver ACK Queue; Customer reviews, Driver completion evidence and blocked monthly drafts open the existing Dashboard. Nothing auto-navigates or executes Accept/Reject/Cancel, assignment, ACK/link, completion, billing, invoice, payment, Calendar, message or provider actions.
- Focused no-write protection is `scripts/test-admin-ai-todays-work-brief-guard.mjs` plus the established Assistant, invoice-search, exact-booking, urgent Dashboard, Pending Driver ACK, Admin completion and monthly auto-preparation guards. Synthetic fixtures cover new/amendment/cancellation, same-Company/two-Booker isolation, cross-Booker and partial legacy blocking, Traveller-not-Booker privacy, urgent timing, newest-link precedence, Driver JC without automatic completion, blocked monthly drafts, pagination, prompt injection, no mutation and no model/history leakage.
- The pre-existing `scripts/test-admin-monthly-billing-draft-invoice-sequencing-guard.mjs` baseline still rejects the ledger's already-recorded Production Supabase project reference through its broad secret-pattern check. The monthly auto-preparation guard passes, and Phase 3 changes no monthly application source, schema, scheduler, draft, invoice or provider behavior; this unrelated test-harness debt remains parked.

## Production Company + Booker Customer Consolidation (operational checkpoint 2026-09-01)

- Owner-approved dry-run `CB-MERGE-20260901-B48CBB19`, mapping SHA-256 `b48cbb19eb0bdddb2654538d61e19248824c9fbec5e795d21b10b2f7c84cb78b`, executed once as one hash-guarded atomic Production transaction. Every approved Customer, Company, Booker, Traveller and booking row was locked and compared by full-row SHA-256, all direct and protected dependencies were re-counted, booking `178` remained absent and the total booking count remained `56`; any difference would have rolled back the complete transaction.
- Tiger Global now has one exact account boundary: Customer `197` + Company `56` + Booker `28` June. Bookings `192`, `193`, `232` and `233` retain their exact internal/public references and now use Traveller `44` Stanley; bookings `235` and `236` stayed byte-for-byte unchanged on Traveller `42` Deep. The previously blank canonical Company operations email received the redundant Company's existing value, without exposing or changing any other Company field. Customers `175` and `196` were retained as inactive history profiles; redundant Company `40`, Booker `27` and Traveller `41` were deleted only after exact zero-reference checks.
- Nomura Customer `171`, Company `38`, Booker `16` Mavis and Traveller `29` Jwalant stayed byte-for-byte unchanged. Orphan Customer `189` was retained and marked inactive. Customer `162` stayed byte-for-byte unchanged and booking `178` stayed excluded. No invoice, payment, payout, PayNow, access account, monthly draft, contact, Calendar, provider, message or external-send row was changed.
- Append-only audit row `784` records the exact dry-run ID/hash, safe before-image, deleted-row rollback material and exact after-map. Exact reference-keyed Driver Job Link, Driver status, notification-outbox, audit and GPS-history counts were re-read unchanged after commit, so operational history remains attached to the same booking references.

## Admin Ask AI Exact Booking Operational Brief (source checkpoint 2026-09-01)

- Phase 2 remains inside the one existing Dispatch `Ask AI` panel and authenticated `POST /api/admin-ai-assistant` route. One deterministic server intent recognizes only an exact command such as `Show booking 10912`, `Find booking 10912`, or `What is happening with booking ADM-...`. It resolves exactly one persisted public or internal booking reference before the model fallback. Names, Customer display labels, Company text, Booker text, Passenger, Traveller/boss, email, phone and parser text are never reference or identity fallbacks; malformed, fuzzy, action-shaped and prompt-injection requests return a read-only instruction with zero database/model calls.
- Every successful card enforces the permanent `1 Company + 1 Booker = 1 account/profile` boundary. The saved booking must contain complete `customer_id + company_id + booker_id`; the server must re-read one active Customer, the exact Company and the exact Booker bound to both Customer and Company. A saved Traveller is displayable only when its exact ID also belongs to that same Company + Booker. Missing, partial, inactive, cross-Booker, same-Company/wrong-Booker, Traveller-as-Booker or otherwise inconsistent identity fails closed and returns no operational card or Customer-account handoff.
- The structured card is returned outside the model and browser conversation history. It contains only public/internal reference for the exact manual handoff, verified Company · Booker · Traveller labels and IDs, service, persisted pickup/route/status, safe assigned Driver name/plate, newest exact Driver Job Link active/revoked/expired state and its own pending/acknowledged evidence, plus latest persisted exact OTW/OTS/POB/JC timestamps. It does not select or return Customer price, invoice/payment, payout/PayNow, Driver contact, Passenger contact, internal notes, parser/debug fields, tokens, raw link context, credentials or provider state.
- `Load in Dispatch` is an explicit user click that reuses the existing guarded exact-booking GET and established `loadSelectedBooking` path. `Open Customer Account` is an explicit link emitted only after the exact Customer + Company + Booker verification. The result never auto-navigates, writes a booking, creates/revokes a Driver Job Link, changes ACK/status, calls either Calendar, sends a message, invokes a provider or performs a financial/external action.
- Visible light-mode Mac Chrome verification used one intercepted synthetic response on the local app only. The request carried `history:[]`, page 1 and `Show booking 10912`; the structured card visibly rendered the exact Customer/Company/Booker/Traveller IDs, Company · Booker · Traveller labels, service/pickup/route/status, safe Driver/plate, newest-link pending ACK and OTW/OTS evidence, plus the two manual handoffs. The URL stayed on Dispatch, no handoff was clicked, and `Clear Message` removed the complete card. No Production booking/customer read, model call, booking/status/link write, Calendar/provider call, message or external send was used.
- Focused no-write protection is `scripts/test-admin-ai-booking-brief-guard.mjs` plus the aligned existing Assistant and invoice-search guards. Mocked coverage includes exact public/internal success, zero/duplicate reference, partial identity, same Company with another Booker, Traveller-not-Booker, newest-link precedence, active/revoked/expired links, older ACK not clearing a newer pending link, exact latest status evidence, prompt injection, write wording, no mutation, field privacy, no model/history leakage and manual-only UI handoffs.
- Release attribution also reran `scripts/test-driver-job-page-browser.mjs` against a separate detached clean `origin/main` worktree at `cedea73a` under the identical canonical localhost server/harness. Clean main and this Phase 2 branch both fail identically at the pre-existing `POB blocked status feedback` wait, while the focused Driver Job Link API, status persistence, Pending ACK Queue, Admin completion and personal Calendar guards all pass. Phase 2 does not touch the Driver Job page, status route, mock store or either Calendar lane; this browser debt remains separately parked and was not repaired or hidden inside the Ask AI change.
- The phased operational-PA roadmap remains one allowlisted skill and one reviewed PR at a time: Phase 1 exact Company + Booker issued-invoice search; Phase 2 exact booking brief; Phase 3 exact Today's-work brief from established Dashboard alerts/counts and exact links; Phase 4 exact Company + Booker account brief; Phase 5 monthly `pending_admin_review` billing-draft brief; Phase 6 prepared exact-record action cards that only hand off to established confirmed controls; Phase 7 internal scheduled operational briefs/anomaly reminders only through already-established Admin notification/cron lanes and only when exact timing/recipient configuration is proven rather than guessed. The approved unpaid-work skills must keep `Jobs not billed yet` (exact eligible bookings not covered by an exact same-account issued invoice) separate from `Unpaid invoices` (issued invoices with unpaid balance), group all-Customer summaries by exact `customer_id + company_id + booker_id`, block null/partial identity and never combine same-Company Bookers. No PA phase gains model SQL/tools/credentials, broad writes, unsafe bulk-by-name actions, invoice layout changes, Calendar changes or financial/external execution.

## Admin Ask AI Exact Company + Booker Invoice Search (source checkpoint 2026-08-31)

- The product direction for Admin Ask AI is a practical operational PA with a Zia-like experience: natural-language requests should find exact live records, summarize and flag work, prepare bounded next actions and reduce Admin effort. Prestige keeps its own established lanes and controls; there is no Zoho integration or brand-copy dependency. Each capability must be a separately allowlisted server skill with exact identity, server privacy filtering, auditable results and explicit exact-record confirmation before any future financial or external write.
- Phase 1 stays inside the existing Dispatch `Ask AI` panel and existing authenticated `POST /api/admin-ai-assistant` route. A deterministic server intent recognizes a bounded command such as `Show me all Su Ling invoices` before the model fallback. It resolves only an exact Booker, verified Company relationship and durable Customer relationship. Duplicate exact Booker names return zero invoice rows and ask for Company; `Show me all Su Ling invoices for Tiger Global` can then resolve one exact Company + Booker + Customer tuple. Traveller-only names, missing names, and null, partial or inconsistent legacy identity return zero invoice rows with visible manual Customer-folder guidance. No name, Passenger, Traveller, email, phone, invoice label or model output is used as a fuzzy identity fallback.
- Only issued invoice records matching exact `customer_id + booker_id`, `document_type=invoice` and `document_state=issued` are read. The exact filters run server-side before the 10-row page range. Cross-Booker, draft, quotation, credit-note and legacy null-Booker rows are excluded. A count-only check reports that legacy rows were excluded without exposing them. Results show Company · Booker, Singapore read timestamp, issued count, invoice number, issue/due dates, Paid/Unpaid, amount, calculated balance, exact stored booking references, explicit `Load more`, and a user-clicked `Open Customer Account`; there is no automatic navigation.
- Live result rows are returned as a separate structured server payload and rendered beneath the existing Ask AI conversation. They are never added to browser conversation history or sent to OpenAI. Ordinary non-invoice questions retain the existing no-tools, `store:false` model lane. Invoice-shaped prompt injection and payment, mark-paid, issue, email, edit, delete or other bulk/write wording is rejected before any database or model call and directs Admin to the established confirmed invoice controls.
- This phase adds no route, page, schema, migration, model-generated SQL/ID, invoice writer, issue/PDF/email/mark-paid/payment/payout/PayNow/provider action, external send or auto-navigation. The locked invoice workflow and every Customer, Driver, Calendar, messaging, Email AI and monthly-billing consumer remain unchanged. Later PA phases may add read-only operational summaries and anomaly/reminder skills, then prepared exact-record actions, and only later bounded automation; each remains separately approved and guarded rather than broad model access.
- Visible light-mode Mac Chrome verification used only an intercepted synthetic response on the local app. The first request carried `history:[]`, page 1 and the exact natural-language command; the structured panel showed Company · Booker, Singapore timestamp, 10 invoice rows, Paid/Unpaid balances, booking references, manual legacy guidance, explicit Customer-account link and `Load more`. The second request again carried `history:[]` with page 2, appended exactly two rows, removed `Load more`, retained zero conversation-history rows and logged zero browser warnings or errors. `Clear Message` removed the synthetic result. No Production customer or invoice record, OpenAI model, Supabase read/write, invoice action or external provider was used.
- Focused no-write protection is `scripts/test-admin-ai-invoice-search-guard.mjs` plus the aligned established `scripts/test-admin-ai-assistant-guard.mjs`. Mocked cases cover zero, unique exact identity, duplicate Booker, Company disambiguation, Traveller-only name, null/partial identity, cross-Booker and legacy-null exclusion, issued-only filters, pagination, prompt injection, no mutation, no model/history result leakage and explicit Customer-account navigation.
- The broader public-response privacy aggregate initially could not run because its existing Driver bidding temporary harness did not provide the device-push module imported by the already-merged saved-booking adapter. `scripts/test-driver-portal-bidding-api-contract.mjs` now supplies only a fail-closed temporary stub that throws if bidding ever enters that unrelated device-push lane. This is test dependency alignment only; Driver bidding, reassignment, device push and application runtime source are unchanged.

## Monthly Billing Company + Booker Isolation Repair (source checkpoint 2026-08-31)

- A no-write mocked regression reproduced the established monthly preparation path combining two Bookers under one Company when their customer/display evidence collided. The grouping reader did not select Company or Booker IDs, issued-invoice coverage used only Customer ID plus booking reference, and null or partial legacy identity entered `ready`. One Booker therefore could be treated as covered by the other Booker's issued invoice, while the draft and lock paths still keyed records by display account plus billing month.
- Monthly grouping now requires exact verified `customer_id + company_id + booker_id` before a completed booking can become ready. It exposes those safe IDs on the established group/job result, separates each exact tuple for the billing month, and keys issued coverage by the same Customer + Company + Booker scope plus exact internal/public booking reference. Missing, null, partial, malformed or inconsistent identity remains visible as `Blocked` with `Verified Company and Booker identity is missing or incomplete.`; no identity is inferred from Company, Booker, Customer, Passenger or display text.
- The existing trip-candidate, draft-plan, invoice-draft, duplicate-trip, lock, upsert, Admin UI handoff and first-of-month orchestrator paths now carry and compare the same exact identity tuple. Candidate reads require all three IDs, another Booker returns zero candidates, draft/plan writes reject incomplete identity, exact-tuple locks prevent overwrite after issue, and scheduler idempotency matches exact tuple plus billing month. Existing schedule `0 0 1 * *`, 08:00 SGT first-day timing, previous-month policy, `pending_admin_review`, one consolidated internal Admin notification, Automation gate, limits and no-auto-issue/send/payment/provider boundaries remain unchanged.
- Production migration `20260831142905_monthly_billing_company_booker_scope.sql` adds nullable restrictive Company/Booker foreign keys to the two existing monthly planning/draft tables, replaces only their display-account/month unique indexes with exact Customer + Company + Booker + month unique indexes, and adds exact-identity read indexes. Exact-project read-back from `kvvsguhklmfgkebhxatm` proves both pairs are nullable `int8`, all four exact composite indexes exist, both tables remain empty, and migration history records remote version `20260831142905`. Existing legacy rows remain unchanged with nullable identity; there is deliberately no guessed backfill, policy, grant or data statement. RLS and service-role-only server access remain unchanged, and legacy rows cannot be selected as exact current drafts. Security advisors reported no new error or warning; the existing server-only RLS-without-policy information remains. Performance advisors reported four non-blocking unindexed-foreign-key information items because the exact composite indexes do not lead on each individual Company/Booker foreign key; no second migration or speculative index was added in this bounded functional repair.
- Focused no-write protection is in the established monthly grouping, trip-candidate, draft-plan, invoice-draft and automatic-preparation guards. It covers one Company/two same-name Bookers, cross-Booker issued coverage, null/partial identity, exact candidate/draft/lock filters, exact upsert conflicts, legacy scheduler zero-write behavior, preserved Admin notification and the migration's no-backfill/no-policy/no-grant boundary. No manual cron trigger, invoice issue/PDF/email, mark-paid, payment, payout, PayNow, Calendar, Driver, messaging, provider or customer contact is part of this repair.

## Atomic Saved-Booking Driver Reassignment Protection (source checkpoint 2026-08-31)

- The owner reproduced a regression in the established loaded-booking `Save Driver Assignment` action: its assignment-only `/api/admin-bookings` PATCH updated the booking's Driver fields through the generic multi-table writer, but did not execute the older replacement-driver protection that existed only after a later replacement-link acknowledgement. A previous Driver could therefore retain a still-active exact-booking private link and submit OTW after Admin had already saved a different verified Driver. This was a path divergence, not a request for a second assignment or Driver Job Link lane.
- The existing assignment-only PATCH now declares only the bounded server `driver_assignment` update mode. Initial null-to-Driver assignment remains on the established booking writer. An unchanged same-Driver request is rejected without a write. Only an existing verified Driver A to different active verified Driver B change uses the protected service-role-only `apply_admin_driver_reassignment` transaction: it locks and version-checks the exact saved booking, re-reads Driver B's authoritative safe assignment fields, updates only those four booking Driver fields, expires only Driver A's still-active, unrevoked, unexpired links for that exact booking with `revoked_at` unchanged, queues exactly one bound Driver-A notification, and writes one safe audit record. Any validation, concurrency, booking, Driver, link, notification or audit failure rolls the whole transaction back.
- The exact old-Driver notification text is `Job reassigned, do not proceed.`. Delivery reuses the one established Driver device-push sender and re-verifies the exact transaction-created queued outbox row plus the exact expired-link/old-Driver binding before loading only Driver A's active subscriptions. Provider delivery remains best-effort after the atomic database result; it cannot roll back the saved reassignment, and testing uses only mocked providers. No notification is sent to Driver B by this action. The existing separate `Create Link` action remains the only replacement-link issuance and normal new-job alert path.
- After a successful reassignment the established Driver Job Link read refreshes immediately, so the existing Pending Driver ACK Queue count, badge and pulse derive from the new active-link state without another queue, poller or store. Customer cancellation and manual `Revoke Link` remain explicit and unchanged. Link issuance alone still does not expire an old link; the later different-verified-Driver acknowledgement protection remains defense-in-depth. Initial assignment, same-driver selection, another booking, the newer Driver's links, unassigned links, and already expired/revoked links remain untouched.
- Protected behavior is in `scripts/test-admin-driver-reassignment-transaction-guard.mjs`, the executable reassignment case in `scripts/test-admin-booking-supabase-adapter-contract.mjs`, and the mocked exact-recipient/exact-copy/fail-closed cases in `scripts/test-driver-job-device-push-alert-guard.mjs`, alongside the locked Pending Driver ACK Queue, Driver Job Link API, status/old-token, Admin completion and Driver Calendar guards. Calendar, Driver Reports/history, OTW/OTS/POB/Job Completed persistence, GPS, messaging, customer identity, Rates, invoice/billing/payment, payout, PayNow, privacy, native source and customer cancellation are not modified. The owner-approved protected Production function was installed as migration `20260831124441_admin_driver_reassignment_transaction`; read-back proves security invoker, empty search path, no `anon` or `authenticated` execute access, service-role-only execution, corrected copy and still-active-link filters. The DDL changed no Production booking, link, Driver, notification, audit or provider record, and no external contact occurred.

## Customer Profile Save Scope And Optional Contact Clear Repair (source checkpoint 2026-08-31)

- The owner reproduced the exact Customer Profile failure in the installed Apple Admin app: after editing a Booker / PA or Traveller inside the open `Customer profile & invoice prefix` section, pressing the parent `Save profile` action closed the complete editor without sending the nested identity writer. Refresh then correctly reloaded the unchanged stored identity, making the attempted amendment appear to have been saved and reverted. The Admin native app is the established WebView of the same Production page; it has no separate profile persistence lane.
- The two existing writers remain separate and are now named by their exact scope: the parent action is `Save company details`, while the nested exact-identity action is `Save Booker / Traveller`. The nested editor reports only whether its current Booker / Traveller draft has changed. While that draft is dirty, the parent company action stops before confirmation or any request, keeps every draft value mounted, shows a direct instruction, scrolls to and focuses the existing nested save action. After the nested write reloads and verifies the exact Company + Booker + Traveller relationship, the parent company action becomes available normally.
- Existing company updates may now deliberately clear only the optional company contact fields `accounts_email`, `billing_address`, `billing_email`, `main_phone`, `mobile_phone`, `operations_email`, `primary_contact_name`, and `website`. An edit serializes an intentionally blank value as explicit `null`; the established contract accepts null only for that bounded allowlist on `company_update`, and the existing exact-ID `companies` update carries those nulls through its returned-row readback. `company_name`, `domain`, company ID, action type and every other field remain non-clearable identity or forbidden input. Create mode continues omitting blank optional values.
- A cleared optional website is no longer visually repopulated from the preserved company domain on reload. The domain remains the existing company identity value and is not cleared by the Website field. Customer folder name continues to use only the existing exact `customers.display_name` PATCH; Company name, Booker, Traveller, Passenger, saved bookings, invoices, rates, access, Calendar, Driver, messaging, payment, payout, PayNow, provider, schema, migration, native source and Production data remain unchanged.
- Fail-first and passing source/runtime protection is in `scripts/test-customer-company-profile-contract.mjs`, `scripts/test-customer-profile-verified-identities-guard.mjs`, `scripts/test-admin-company-traveler-crm-identity-contact-write-contract.mjs`, and `scripts/test-company-traveler-crm-runtime-write-action-api-contract.mjs`. Both visible intercepted Mac browser guards passed: `scripts/test-customer-company-profile-browser.mjs` preserved the folder-only company handoff, while `scripts/test-customer-corporate-identity-browser.mjs` proved the new dirty-draft block, focus, zero-write boundary, nested save/reload, Traveller add, selected-job identity steps, and unlinked-profile fail-closed boundary. This checkpoint is source-only: no Customer, Company, Booker, Traveller, Tiger Global, June, Deep, Stanley or other Production record was read through a write path or changed, and no deployment is claimed.

## Email AI Exact Company Display Punctuation Repair (source checkpoint 2026-08-31)

- The owner-approved one-time, no-retry replay of complete private email `15798` reached OpenAI once as provider request `req_e360997c8a62439296a26b6ad7758bd2`. Twenty-two of twenty-three bounded validations passed. The only failure was exact Company display: the provider returned `UBS SuMi TRUST Wealth Management Co., Ltd` while the explicit source supplied the same semantically validated Company with its final period. Because every validation did not pass, the compare-before-write gate made no Production update and no retry. Intake `fe60d579-c607-4346-acbc-75fb23a561a2` remained `failed` / `uncertain`, with zero stored model tokens and zero parsed bookings.
- The established explicit-source validator intentionally normalizes punctuation when proving semantic Company equality and remains a pure accept-or-reject guard. The narrow repair adds one post-validation display-preservation step: only after that validator succeeds, only for exactly one non-ambiguous booking, and only when the structured Company and explicit source Company are already normalization-equal, the queued analysis retains the exact explicit source Company spelling and punctuation. Exact-equal values retain the existing object unchanged.
- Missing, truncated, substituted, ambiguous and multi-booking Company evidence continues to fail closed. The new step cannot create a Company, infer an account, convert Passenger/Traveller into account identity, alter another structured field, or bypass the full-email AI result and source validator. The complete fail-first regression keeps the validator output unchanged, then proves only the already-validated Company display receives the source's final period.
- Focused protection is in `scripts/test-admin-email-ai-intake-runtime.mjs` and `scripts/test-admin-email-ai-intake-guard.mjs`. No prompt, model, mailbox gate, provider retry, OpenAI call, route, API, table, schema, migration, account selector, Company + Booker identity rule, Save + CRM, Rates, Customer access, Driver, native app, push/badge, Calendar, invoice, billing, payment, payout, PayNow or messaging lane changes. Production intake `15798` remains unchanged; any later replay remains a separate exact action-time approval after deployment.

## Email AI Explicit Passenger Mobile Role Repair (source checkpoint 2026-08-31)

- After the deployed Company-label repair, the owner approved exactly one no-retry OpenAI validation of failed intake `15798`. Provider request `req_85fbd89ded644e15a6c2ff3139fbb361` reached the existing full-email structured-analysis lane, but the established source-fact validator rejected the result. The compare-before-write gate stopped before persistence: the exact intake remained `failed` / `uncertain` with zero stored tokens and zero parsed bookings, and no Job Card, booking, Customer Account, CRM, Calendar, invoice, billing, message or other Production write occurred. No second provider call was made.
- Narrow inspection proved a second independent role defect in the same established source-evidence helper. The source explicitly supplied the Passenger's separate possessive contact as `Mr. Nakamura's mobile`, while Client details supplied a different requester/Booker email and phone. The helper recognized only generic labels such as `Passenger mobile`, ignored the possessive Passenger label and treated the Client-details phone as `passengerContact`. A correct AI result carrying the explicitly named Passenger's mobile could therefore fail the source validator against the wrong role.
- The existing helper now reads the labelled Passenger identity and accepts a possessive `contact`, `mobile` or `phone` only when every normalized identity token on that label belongs to the exact labelled Passenger. An exact Passenger-specific phone takes precedence for `passengerContact`; the different Client-details phone remains untouched in the AI result for Booker review. A possessive phone for another named person is ignored, multiple Passenger-specific candidates still fail closed, and the established Client-details fallback remains unchanged when no exact Passenger-specific contact exists.
- The anonymized exact-shape fail-first case in `scripts/test-admin-email-ai-intake-runtime.mjs` initially failed because the correct Passenger mobile was rejected. It now passes while proving the different Client-details phone remains Booker contact evidence, substitution of that phone for the Passenger fails, and another person's possessive mobile cannot override the Passenger. The complete focused Email AI suite, including the validation-replay evidence runner, passes without a provider or Production call.
- This repair changes no prompt, model, mailbox gate, classifier, UI, route, API, table, schema, migration, Customer Account rule, Company + Booker identity, Passenger/Traveller booking-only rule, Save + CRM, Rates, Customer access, Driver, native app, push/badge, Calendar, invoice, billing, payment, payout, PayNow, messaging or provider-send lane. Production intake `15798` remains unchanged; any later replay remains a separate exact action-time approval after deployment.

## Email AI Company + Booker Account Recognition Repair (source checkpoint 2026-08-31)

- Read-only Production evidence reproduced the exact new booking email `Prestige Transport 15798` as one failed private Email AI intake. Its explicit source line placed the next field label on the same line as the company: `Company name UBS SuMi TRUST Wealth Management Co., Ltd. Company Address`. The existing evidence extractor captured the trailing `Company Address` label as part of the customer company, while the AI result correctly returned the company name without that label; the established validator therefore failed closed with `AI booking result is missing or conflicts with explicit source evidence; manual review required.` The intake remained outside Dashboard review and made no booking, CRM, Calendar, invoice, billing or provider-send write.
- The existing source-evidence extractor now removes only an exact trailing `Company Address` field label at the line boundary. A standalone `Company Address` line cannot become the company, missing/truncated/substituted company evidence still fails closed, and legitimate names such as `Company Address Services Pte Ltd` remain intact. No prompt, model, mailbox gate, classifier, OpenAI request count, canonical field, route, table, schema, migration or second Email AI lane changes.
- The established Email AI `Create Job Card` customer check now follows the current identity rule: one Customer Account is one exact Company + Booker pair, and Passenger/Traveller remains booking-only. A read-only possible-account notice requires an exact Booker email or exact Company plus exact Booker name, the Booker's durable `customer_id`, and one active Customer record carrying that exact Customer and Company. Company-only, Passenger-name, Traveller, fuzzy, domain, display-label, phone and parser-derived identity are not account evidence. No match explicitly does not prove a new customer; ambiguous, unavailable and unlinked results fail visibly to manual selection.
- The recommendation remains GET-only and suggestion-only. It never selects IDs, merges or creates an account, binds a Booker, creates a Traveller, saves a booking, calls Save + CRM, or writes Customer, access, Rates, invoice, billing, Calendar, Driver, notification, provider or Production data. Admin must still choose the exact existing Company + Booker Customer Account or the existing explicit new-account path before Save + CRM.
- Fail-first and passing coverage is in `scripts/test-admin-email-ai-intake-runtime.mjs`, `scripts/test-email-ai-customer-identity-section-four-guard.mjs` and `scripts/test-booking-ui-browser.mjs`. The complete mocked light-mode Booking UI browser passed in `428.6s` with zero test errors, zero console errors and zero blocked or escaped Supabase mutations. The focused Email AI suite, TypeScript, lint, Next.js Production build, Save + CRM, Dispatch identity, Operations and personal Driver Calendar, invoice lifecycle/layout handoff, Driver ACK/completion and actual privacy guards all passed. Existing lint warnings remain unrelated and were not repaired.
- Production intake `15798` has not been replayed, reclassified, edited, closed or deleted. Any one-time Production replay/OpenAI call, booking QA, data cleanup or provider action remains a separate exact action-time approval after the reviewed source is live. Invoice workflow/layout and both Calendar systems are unchanged.

## Orphan Legacy Customer Profile To Dispatch Company + Booker Repair (source checkpoint 2026-08-31)

- Read-only Production evidence reproduced the exact mismatch for Company `46` / Customer `180`, displayed as `Alson Chua UOB`: both records are active, but there is no Booker, Traveller or Customer access row. Completed bookings `10877` and `10878` retain the exact Company `46` + Customer `180` relationship with null Booker and Traveller IDs. The future Dispatch chooser therefore had no Company + Booker tuple to display even though the Customer profile existed.
- The established Dispatch `Customer Account` chooser now includes an active orphan legacy profile only when its Company has no current Company + Booker option. The existing row is labelled `Existing customer profile · Booker required`; selecting it carries only the verified Company and Customer IDs and clears any Booker name, contact or email left from an earlier draft. It never derives a Booker or Traveller from the profile name, old passenger, contact, email, phone or booking display text.
- A future save of that selected profile no longer enters the retired company-only compatibility writer. Admin must enter and approve the real Booker through the existing Save + CRM flow. The server accepts the selected Customer as a `Use existing account` candidate only after re-reading at least one exact saved booking with the same verified Company ID + Customer ID, re-reading the active Customer, and finding no conflicting exact Company + Booker evidence. The existing collision review remains mandatory before the established Booker-to-Customer binder can set `bookers.customer_id`.
- Loaded legacy saved bookings retain their company-only read/update compatibility. Existing Company + Booker choices, Customer Profile exact-Booker scope, Passenger/Traveller booking-only behavior and all current Save + CRM collision/conflict guards remain unchanged. Customer `180`, Company `46`, bookings `10877`/`10878`, protected Customer `174`, QA Customer `192`, and every Production row remain unmodified at this source checkpoint.
- Focused source, mocked Supabase adapter and light-mode browser coverage is in `scripts/test-admin-dispatch-crm-identity-selectors-guard.mjs`, `scripts/test-admin-booking-supabase-adapter-contract.mjs`, `scripts/test-admin-dispatch-customer-account-browser.mjs`, and `scripts/test-booking-ui-browser.mjs`. The focused browser selected the existing profile with exact Company `46` + Customer `180`, blank Booker and Traveller IDs, zero booking POSTs and zero browser errors. The complete Booking UI browser regression passed with zero errors and no blocked Supabase mutation requests.
- No Customer, Booker, booking, invoice, billing, Calendar, Driver, Rates, access, notification, provider, schema, migration, environment, Vercel, native or Production write is part of this source checkpoint. Invoice layout/workflow, Calendar, Driver ACK, Email AI and the parked Email AI failure remain untouched.

## Admin Mobile Customer Account Chooser Containment Repair (2026-08-31)

- The owner-provided installed Admin iPhone screenshot reproduced the established Dispatch `Details` step with the open `Customer Account` menu covering the Company, Booker and contact fields below it. Source inspection proved the menu panel used `absolute left-0 top-full` at every breakpoint, so the phone menu was removed from normal document flow even though its width and internal scroll boundary remained contained.
- The established chooser remains in the same Booking Details location with the same native `<details>` control, search, options, Company + Booker identity tuples, handlers and selected values. Only its existing menu panel positioning changes: it is `relative` and therefore in-flow below the summary on phone widths, while `md:absolute md:left-0 md:top-full` preserves the accepted compact overlay from the existing medium breakpoint upward. Opening the phone menu now pushes the following booking fields down instead of covering them.
- Fail-first focused Chrome evidence used the real mobile Dispatch navigation and `Details` quick step at 390-by-844 device metrics. A synthesized touch opened the exact chooser, then the unchanged source failed with computed menu position `absolute`. After the repair, the same run proved computed `relative` positioning, zero overlap with the following Company field, internal `overflow-y: auto`, zero horizontal page overflow, touch selection of exact Company `55` / Booker `5501` / Customer `550`, null Traveller identity and zero booking POSTs. A second 320-by-568 containment probe also passed. The existing 1440px regression continues proving computed `absolute` desktop positioning, the approved one-column width, search and repeat-account behavior.
- Focused protection is `scripts/test-admin-dispatch-customer-account-browser.mjs` and `scripts/test-admin-dispatch-crm-identity-selectors-guard.mjs`. No selector, panel, route, API, writer, data field, account rule, Customer profile behavior, Save + CRM review/binding/persistence, Passenger/Traveller rule, Customer access, Rates, invoice/billing, Calendar, Driver, messaging, push/badge, native source, GPS, provider, environment, Supabase schema/data, payment, payout or PayNow lane changes.

## Exact Customer Profile Company + Booker Scope Repair (2026-08-31)

- Fail-first light-mode browser evidence placed two different Customer Accounts under the same verified Company: Booker `17` / Customer `165` and Booker `18` / Customer `166`. The existing Customer Profile identity editor scoped only by Company, so Customer `165` incorrectly exposed Booker `18` and its Traveller. The same editor could also create another Booker without the durable `bookers.customer_id` relationship that Dispatch requires, leaving a future Save + CRM operation to review that profile again.
- The established Customer Profile editor now receives its exact Customer ID and reuses the existing safe `/api/admin-rate-setup` Booker projection. It requires exactly one Booker whose `company_id` matches the loaded Company and whose durable `customer_id` matches the exact Customer profile, then displays and maintains only Travellers carrying that exact Booker ID. Same-Company Bookers and Travellers belonging to another Customer Account are excluded.
- Customer Profile no longer creates or binds a Booker. The existing Dispatch Save + CRM review and guarded Booker-to-Customer binder remain the sole account approval/creation lane. `Add Traveller` reloads and reuses only the already-bound exact Booker; Booker name, email and contact edits continue through the existing typed Booker PATCH, while Traveller creation/linking continues through the established guarded Traveller writers. Passenger/Traveller remains booking-only.
- A profile with no exact bound Booker, multiple/malformed evidence, a changed Booker relationship or a cross-customer mismatch fails closed with a visible explanation and no Booker or Traveller writer. No Company name, Customer label, email, phone, Passenger text, prior form state or parser output is used to infer an account. Protected null-link and legacy cases therefore remain untouched and must continue through the existing explicit Dispatch review when permitted.
- Focused source and mocked-browser protection is `scripts/test-customer-profile-verified-identities-guard.mjs` and `scripts/test-customer-corporate-identity-browser.mjs`. The browser proves one exact bound Booker edit, one booking-only Traveller add, zero Booker POSTs, exclusion of another Customer under the same Company, and a zero-write null-link failure. The unchanged Dispatch Customer Account browser still proves exact `bookers.customer_id` selection and zero booking posts.
- No Supabase schema, migration or Production data change is included. Dispatch runtime, Save + CRM binding/collision review, Customer access, Rates and legacy precedence, QA Customer `192`, protected Customer `174`, Booker `20`, bookings, invoices, billing, Calendar, Driver, messaging, push/badge, native apps, GPS, provider, environment, payment, payout and PayNow lanes remain unchanged.

## Save + CRM Booking-Read Purpose Repair (2026-08-30)

- Signed-in Production QA on deployed source `2569e1a0acb01f95270555d60e9c566b472ff2a2` selected the established QA Company `Prestige Internal Native Push QA`, Booker `Owner PA Native Push Test`, and Passenger `OWNER DRIVER REMINDER QA`. The exact Company + Booker Customer Account was already selected, and Save + CRM correctly did not ask to create or merge another account. The save then failed closed before its booking POST with `Booking save failed: Admin booking persistence is available only from the internal admin dashboard.` Vercel Production runtime evidence recorded the corresponding `GET /api/admin-bookings` as HTTP `403` while the deployment remained `READY`.
- The exact cause was the existing Save + CRM billing-identity preflight read sending the legacy-data purpose header to the established `/api/admin-bookings` route, whose verified boundary requires `admin-booking-persistence`. The in-place repair changes only that one preflight GET header. It adds no route, writer, selector, panel, account rule, persistence path or fallback, and it leaves the established booking POST and Company + Booker resolution unchanged.
- Focused protection in `scripts/test-save-crm-billing-identity-review-guard.mjs` now scopes the assertion to `fetchRecentAdminBookingPersistenceRecordsForBillingIdentity` and requires the exact booking-persistence purpose while rejecting the legacy-data purpose there. Supabase data/schema, Vercel configuration, Rates, invoice/billing, Calendar, Driver ACK/reminders, messaging, native apps, Customer access, privacy, payment, payout and PayNow lanes remain untouched.
- The failed pre-repair QA attempt created no booking: the saved-booking count remained `55`, and the unsaved draft was cleared. PR `#449` then merged the bounded source, guard and ledger repair to `main` as `98d1c1677f23e758e8eeb84c289e49862e3423a6`. The existing Git integration produced only Production deployment `dpl_DecTepPy2AB3yftPNQLnfQCSnHKM`; it reached `READY` on `app.prestigelimo.sg` from that exact verified commit without a manual or duplicate deployment.
- Fresh owner-approved signed-in Mac Chrome acceptance on build `98d1c167` selected the same existing QA Customer Account and created exactly booking `10921` / `ADM-20260830150754` for `31 Aug 2026, 1200hrs`. No create, merge, link or account-choice prompt appeared. Runtime evidence recorded the repaired preflight `GET /api/admin-bookings` as HTTP `200`, followed by the one booking `POST /api/admin-bookings` as HTTP `200`; the visible saved count moved once from `55` to `56`.
- Readback through the repaired route proves booking `10921` carries verified Company `53`, Booker `26` and Customer `192`, while `traveler_id` remains null and `OWNER DRIVER REMINDER QA` remains only the booking Passenger. It has no assigned Driver. The established Save + CRM side effects completed unchanged: the Operations Calendar sync returned HTTP `200` with no guest email, and the internal Admin urgent-booking native-push ticket was accepted and receipted. No Customer or Driver message, Driver Job Link, ACK queue row, invoice, billing, payment, payout or PayNow action occurred. The QA booking and its established Calendar artifact remain intentional test data pending a separately approved exact cleanup.

## Company + Booker Existing-Customer Link Production Acceptance (2026-08-30)

- The owner approved linking only eleven proven Company + Booker identities to their already-existing Customer profiles through the established nullable `bookers.customer_id` relationship: `14/39→173`, `16/38→171`, `17/33→165`, `18/36→169`, `21/47→184`, `22/48→185`, `23/50→188`, `24/31→163`, `27/40→196`, `28/56→197`, and `29/30→167`. This is a relationship link, not a Customer merge. Passenger/Traveller remains booking-only.
- The official migration `20260830141847_company_booker_existing_account_backfill.sql` asserts the exact current global counts, eleven null Booker links, active target Customers, exact booking-ID evidence and Customer ownership, exact aligned invoice IDs, empty candidate Booker/Company rates, no candidate legacy-Traveller rate override, no active access-account collision, no competing Booker link, and the existing validated restrictive FK, ready unique partial index, nullable column, RLS default-deny boundary and zero Booker policies. Any drift aborts the complete serializable transaction before a relationship changes.
- The only persistent statement updates `public.bookers.customer_id` for those exact eleven Booker/Company rows and requires an affected-row count of eleven. It does not insert, delete or update Customers, Companies, bookings, invoices, rates, access accounts, memberships, principals, sessions, devices, subscriptions, notifications, Calendar, Driver, GPS, billing, payment, payout or PayNow records; it adds no runtime, schema, policy, grant, provider, environment, Vercel or native-app change.
- Booker `15` / Customer `174` remains excluded for protected legacy access/history compatibility. Booker `20` / Customer `177` remains excluded to preserve its nonempty legacy Traveller rate precedence. Already-linked Booker `19` / Customer `170` retains its accepted Booker rates, and QA Booker `26` / Customer `192` plus the complete QA access graph remain unchanged. The sixteen legacy/no-pair Customers `160, 161, 162, 164, 166, 168, 175, 178, 180, 183, 186, 187, 189, 190, 193, 194` remain non-inference targets and are explicitly protected.
- PR `#447` merged the reviewed migration, guard and source checkpoint to `main` as `267c24ff1beda7c634b03a1f1d1eff96ced170bf`. The authenticated Production Supabase migration runner then applied only `company_booker_existing_account_backfill` as remote version `20260830142840`. Immediate readback proves all eleven exact links, thirteen linked Bookers total, only protected Bookers `15` and `20` still null, zero links to the sixteen non-inference Customers, all seventeen exact bookings and three aligned invoices unchanged, zero active access-account touches, and zero candidate Booker, Company or legacy-Traveller rate changes. QA Customer `192`, protected Customer `174`, accepted Booker `19` rates and Booker `20` legacy rate precedence remain intact.
- The validated restrictive FK, ready unique partial index, nullable column and Booker RLS default-deny boundary remain unchanged. Post-migration advisors reported only existing project-level informational findings plus the unrelated Auth leaked-password-protection warning; this data-only migration added no schema, policy, grant or index finding, and no unrelated advisor remediation was attempted. Source protection is `scripts/test-company-booker-existing-account-backfill-guard.mjs` plus the existing Company + Booker identity, Rates, Customer access/session, privacy, notification/badge/fanout, Calendar, DSP and invoice guards; the focused post-migration set passed again with no runtime write or provider action.

## Company + Booker Customer Rates Production Acceptance (2026-08-30)

- The owner approved opening only `PRESTIGE_CUSTOMER_RATES_WRITE_ENABLED=true` for the existing Vercel project's Production environment. The exact key was previously absent. No second Vercel project, domain, repository, source deployment or environment lane was created. Vercel's existing Production `Redeploy` action rebuilt the selected current `main` source without a code change as deployment `dpl_67exipzGHq4st6ZuRjkHiAt456n9`; it reached `READY`, retained `app.prestigelimo.sg`, and reports verified Git SHA `c50056b35739dc90e76610c850a36dec4fddeba4`.
- The first native Admin load after redeployment temporarily remained on `Loading secure Admin sign-in…`. Read-only runtime evidence showed the existing native push-subscription request succeeded with HTTP `200`, `/admin-sign-in` returned the expected signed-session HTTP `307`, the deployment remained `READY`, and no error/fatal runtime log was present. The installed app then loaded normally without sign-out, reinstall, credential reset, Face ID reset, native build or application repair; this transient observation is not claimed as a reproduced source defect.
- The owner then selected the existing exact Customer Account `ATOM International [Rachal Quinlan] · rachal` in the installed native Admin Rates surface and pressed the established `Save Override` once with only MNG AVF `85` and DEP AVF `75`. Production recorded one `POST /api/admin-customer-rates-runtime-write-action` at 21:25:51 SGT with HTTP `200`; the native UI reported `Override saved`.
- Read-only Supabase verification proved exact Booker `19` remains bound to Company `37` and Customer `170` and now stores only `{"DEP":{"AVF":75},"MNG":{"AVF":85}}`. Company `37` retains empty Company rates, `transzend_excel_privacy=false`, and `card_option_default_enabled=false`. It is the only non-empty Booker customer-rate row, all Company customer-rate rows remain empty, and protected Booker `20` remains unchanged with empty rates. No second save, direct SQL write, migration, booking, Customer Account, invoice, billing, Calendar, Driver, notification, provider, payment, payout, PayNow, native build or release action occurred.
- This accepts the existing Company + Booker customer-rate writer in Production; it does not broaden identity or precedence. Exact Booker rates continue overriding Company rates, Company rates continue overriding defaults, Passenger/Traveller remains booking-only, and legacy Traveller precedence remains available only when no approved Company + Booker account exists. The Production gate remains open for the established guarded Rates save/remove workflow. Closing or removing the gate and redeploying remains the bounded kill switch and was not exercised after this accepted save.
- Focused post-acceptance protection remains `scripts/test-customer-rates-runtime-write-action-api-contract.mjs`, `scripts/test-customer-rates-runtime-app-wiring.mjs`, `scripts/test-customer-rates-runtime-activation-readiness-guard.mjs`, `scripts/test-pricing-customer-rates-approval-packet.mjs`, and `scripts/test-customer-rates-runtime-create-path-guard.mjs`. All five passed on the exact current-main tree before this docs-only checkpoint.

## Company + Booker Browser QA Reselect Race Repair (2026-08-30)

- Read-only Production inspection on the owner's signed-in Mac Chrome confirmed QA booking `10917` remained on the established verified Company + Booker account and exposed no Passenger/Traveller account prompt. No field, booking, account, Calendar, invoice, notification or provider action was changed.
- The isolated zero-write Customer Account browser acceptance then failed after selecting the same verified Booker for a changed booking Passenger. Its existing wait accepted the already-present Company/Booker dataset before the asynchronous exact-Booker verification completed; the late selection completion then closed the newly opened create-choice dialog and produced a false timeout.
- The browser guard now requires the existing Customer Account chooser to be closed and its search input to be cleared, in addition to the exact Company/Booker/Customer IDs and changed Passenger. Those two UI states are set only by the established completed account-selection path, so the following single Company + Booker new-account assertion cannot race the pending verification. No delay, retry, weakened assertion or application source change is introduced.
- This is test-and-ledger-only acceptance stabilization. Save + CRM runtime, Company/Booker/Passenger behavior, Customer access, Rates, invoices, Calendar, Driver ACK, push/badges, native apps, Supabase, Vercel, Production data, payments, payout, PayNow, messaging and all provider lanes remain unchanged. Protection remains `scripts/test-admin-dispatch-customer-account-browser.mjs` plus the existing Company + Booker identity guards.

## Automatic First Driver ACK Reminder (2026-08-30)

- The owner confirmed the Driver physically received the initial new-job native alert for test booking `10916`, then approved one automatic follow-up reminder about 15 minutes after the exact Driver Job Link was issued only if that newest link remains active and unacknowledged. There is no automatic reminder at 30 or 45 minutes. Any later second or third recovery reminder remains an explicit Admin action in the established Pending Driver ACK Queue, subject to the existing 15-minute cooldown and three-reminder maximum.
- One isolated authenticated Vercel Cron GET calls the established server-only reminder helper every minute. The runner selects only active, unrevoked, unexpired links issued at least 15 minutes earlier, rejects acknowledged or malformed records, and then reuses the existing exact-link reminder path, which re-verifies newest-link identity, the nonterminal matching booking/driver, encrypted native handoff, one active Driver app account and exactly one active native subscription immediately before the provider attempt. The schedule can therefore run roughly 15 to 16 minutes after issuance; it does not depend on an open Admin browser.
- Exactly one automatic attempt is allowed for each exact Driver Job Link. Any existing reminder audit row for that link suppresses further automatic attempts, while the established unique reminder event key protects a simultaneous manual/automatic race. The automatic audit is explicitly marked `automatic_first_reminder` with the system actor. Provider rejection remains a recorded failed attempt rather than an automatic retry; after the existing cooldown Admin can use `Remind again` deliberately.
- The existing queue now shows `Auto reminder scheduled` before 15 minutes, `Auto reminder processing` while the first server attempt is due, `Auto reminder sent` only from a provider-accepted audit, and later `Remind again` after cooldown. Its existing 10-second exact-link read refresh supplies safe reminder count, last attempt time and provider-accepted state; no browser timer, second queue, second sender, polling store, route, schema, migration or native build is added.
- Before activation, read-only Production inventory found test booking `10916` / `ADM-20260830085118`, exact link `5c67ac27-4621-4132-a1cc-17865b861b34`, assigned Driver `17`, active and unacknowledged with zero reminders, as the only globally eligible link. Production merge `3e7a702a` deployed READY as `dpl_CmyGSg8DdixTRQAfwNcqyrZnLym6` on `app.prestigelimo.sg`. The 17:36:30 SGT cron returned `200`; at 17:36:33 SGT it reserved exact event `pending-driver-ack-reminder:5c67ac27-4621-4132-a1cc-17865b861b34:1` as system trigger `automatic_first_reminder`, and the native provider accepted exactly one request with `send_succeeded`. The 17:37:30 SGT cron also returned `200` while the exact audit count remained one with the same timestamp, proving no automatic repeat. Signed-in Mac Chrome on build `3e7a702a` showed the one exact `10916 · New` row with disabled `Auto reminder sent`; physical phone receipt of this follow-up remains owner confirmation rather than a server-side claim.
- Driver Job Link creation/copy/revoke, acknowledgement persistence, replacement-driver protection, Driver Reports, explicit Admin completion, Operations and personal Driver Calendar, Live Dispatch/GPS, booking and Company + Booker identity, Customer access/messaging, native app source, push/badge sender internals, Rates, invoice/billing/payment, payout and PayNow remain unchanged. Focused protection is in `scripts/test-driver-ack-auto-reminder-guard.mjs` and `scripts/test-driver-ack-auto-reminder-runtime.mjs`, registered beside the existing ACK reminder, queue, Driver Job Link API, completion, Calendar, native-push, privacy and staged-change guards.

## Company + Booker Active Access-Account Candidate Repair (2026-08-30)

- Signed-in Production fail-first evidence on exact source `7f89b8ba2653c4eda75d368ef09912997788c859` selected Company `53` plus Booker `26` for QA Customer `192`. The exact active access account already carried all three verified IDs, while `bookers.customer_id` remained null and the pair had no prior booking. The established first-booking review therefore rendered `Use existing account` disabled and `Create new account` enabled. The draft remained `Not saved yet`, the saved count remained `52`, and no Customer, Booker, booking, access, notification, Calendar, invoice or provider write occurred.
- The established Admin booking adapter now reads `customer_access_accounts` by the exact verified `company_id + booker_id` pair only when the durable Booker Customer link is null. Exactly one active access row with one valid positive Customer reference is unioned with the existing exact prior-booking evidence, and the existing active-Customer validation supplies the same existing review candidate. Names, email, phone, Passenger/Traveller text, Company labels and parser output remain non-evidence.
- Matching access-account and exact prior-booking evidence deduplicates to one candidate. Disagreement, multiple exact rows, malformed identity, an inactive access row, an inactive/missing Customer or any other ambiguity fails closed before a Customer insert, Booker bind or booking write. When an active access pair already identifies a Customer, the existing `create_new` resolution is rejected before writes so it cannot split that identity.
- The established `Use existing account` review and Booker-to-Customer bind writer remain the only confirmation path. No selector, panel, route, API, table, schema, migration, backfill or second writer is added. `app/page.tsx` is unchanged; the existing chooser continues presenting both explicit decisions, and neither decision writes before Admin confirmation.
- Focused fail-first and passing protection is in `scripts/test-admin-booking-supabase-adapter-contract.mjs`, with the unchanged CRM selector, Save + CRM and visible light-mode Customer Account browser guards retained. The browser guard performed zero booking posts. QA Customer `192`, protected Customer `174`, all legacy/no-pair Customers and Booker `20` legacy rate precedence remain unchanged; any actual QA booking or Booker link is a separate action-time-approved Production write.
- Rates, invoice layout/workflow, DSP billing, Calendar, push/badge/fanout, Customer access/session authorization, messaging, Driver, GPS, privacy, native apps, provider, payment, payout and PayNow lanes are unchanged.

## Admin Mobile Details Draft Clear (2026-08-30)

- The installed Admin app uses the existing web-served Dispatch quick-booking layout; no native-container or Expo change is required for this control.
- The mobile `Details` step now exposes one compact `Clear` action in the existing Booking Details heading. It reuses the complete established `New booking` reset lane, clears the current form, parser text/review artifacts and loaded edit identity, explicitly arms the existing new-booking path, and remains on the empty Details step.
- The existing Message-step `Clear Message` contract is unchanged: it clears parser/conversation text only and deliberately preserves the current booking form and loaded edit identity. The existing `New booking` action still performs the same complete reset and returns to Message.
- The new control is local state only. It does not save/delete a booking, call an API, touch Supabase, Calendar, invoice/billing/payment, messaging, push/badge, GPS, provider or native code, and it is hidden on desktop where the established `New booking` action is already visible.
- Focused protection remains in `scripts/test-mobile-dispatch-quick-booking-guard.mjs` with browser acceptance in the established booking/mobile suites.
- The complete booking browser gate reproduced its completed-only Dashboard fixture checking the visible `Refresh Dashboard` control while the established notification read still correctly disabled it. The fixture now waits only for that existing control to become enabled before clicking; Dashboard runtime, loading state and refresh behavior are unchanged.

## Pending Driver ACK Native Reminder — Local Only (2026-08-30)

- Owner-approved local work adds one `Remind driver` action only to each positive `Pending for Driver ACK Queue` row. It does not add `Resend`, recover/reissue/revoke a private link, or claim that a notification was delivered. The zero-pending compact row and exact-link local-only `Close` behavior remain unchanged.
- The existing verified Admin `PATCH /api/admin-driver-job-links` lane now distinguishes the original revoke payload from the exact `remind_ack` action. The server re-verifies the exact newest active, unexpired, unrevoked, unacknowledged link; matching nonterminal booking and driver; encrypted native handoff; active one-phone Driver account; and exactly one active `driver_native_ios` subscription before any provider request.
- The first reminder is blocked until 15 minutes after exact link issue, later reminders are limited to one per 15 minutes, and each exact link is capped at three. A unique exact-link/time-bucket event key is reserved as an archived audit-only `customer_driver_app_notification_outbox` row before the provider call, so it cannot appear later as an in-app message. Provider copy is fixed to `Job acknowledgement needed. Tap to review.` and carries only the established opaque job key; browser Web Push, raw token, route, passenger, customer, contact, Calendar, GPS, invoice, billing, payment and payout data are excluded.
- The current Driver native app handoff is reused without native source/config changes or EAS. Tapping the alert remains subject to the existing signed Driver session, installation/device binding, exact driver, newest-link, booking assignment and nonterminal checks before the server decrypts the private handoff. The reminder reuses the established native sender's badge reservation and delivery-health internals; existing alert callers retain their prior defaults.
- Fail-first evidence was captured before runtime implementation because `lib/admin-driver-ack-reminder.ts` did not exist. Focused source/runtime coverage is in `scripts/test-pending-driver-ack-reminder-guard.mjs` and `scripts/test-admin-driver-ack-reminder-runtime.mjs`, with the existing native push, ACK queue, Driver Job Link API, completion and Calendar guards retained. The exact-worktree booking browser gate also reconfirmed the already-merged Customer Copy cleanup: the retired SMS pseudo-button and readiness label remain absent while Email, Copy, Edit, Invite, App Link, preview and live-location helper stay present. No live reminder was sent. This checkpoint is local only: no push, PR, merge, deployment, Production data write or native build is authorized or claimed.

## Pending Driver ACK Queue Compact Zero State (2026-08-30)

- When the existing Dispatch `Pending for Driver ACK Queue` has zero rows, it now renders one slim green row containing only the established title and `0 pending` badge. The explanatory `Newest active Driver Job Link...` sentence and duplicate `No driver acknowledgements pending.` empty-state sentence are hidden at zero. Any positive count retains the complete existing pulsing amber queue, explanatory sentence, every uncapped exact-link row, revision label and local-only `Close` control unchanged.
- Complete read-only inspection proved a queue-row `Resend` action cannot safely recover or copy an issued private link: the safe Admin GET never returns the one-time token, while the established Driver Job Link POST creates a new link and inseparably performs the existing best-effort Driver native push attempt and temporary live-location authorization. No `Resend` control, new writer, route, token recovery, provider claim, revocation, expiry or acknowledgement behavior is added in this checkpoint. Any future shortcut must reuse the visible Driver Job Link setup lane under a separately chosen owner-approved meaning.
- Fail-first source coverage rejected the missing compact branch before the presentation-only repair. The focused browser guard then reproduced the same initial queue timeout on untouched `origin/main`: the shared Admin root now opens Dashboard, but the older Dispatch-only guard never selected Dispatch. The guard now opens the established Dispatch tab after each hard navigation before asserting exact-link dismissal isolation and persistence; this changes no runtime or tab default.
- Driver Job Link creation/copy/revoke, acknowledgement persistence, Driver Reports, explicit Admin completion, Operations and personal Driver Calendar, push/badge/native files, GPS writers/polling, booking, messaging, Customer access, invoice, billing, payment, payout, PayNow, provider, schema and Production data remain unchanged.

## Verified Company + Booker Save + CRM Passenger Boundary (2026-08-30)

- A direct light-mode fail-first browser test selected one exact verified Company + Booker Customer Account with no Traveller identity, supplied the existing ordinary trip fields and clicked the actual `Save + CRM`. The unchanged runtime blocked before any booking write with `Passenger/traveler name is required before Save + CRM can choose the billing account`, proving Passenger still incorrectly gated the account/profile lane.
- The established billing-identity review now skips its Passenger/Traveller requirement only when both exact verified `company_id` and `booker_id` are already selected. The corporate resolver likewise requires only the verified Booker for account identity. Passenger remains booking-specific release-readiness evidence, clears unverified `traveler_id`, and does not create, identify, select or price the Customer Account.
- First-time or ambiguous Company + Booker profile review, typed-name/contact suggestion boundaries, ordinary booking/trip validation and all existing Save + CRM persistence/reload/reset behavior remain unchanged. The focused browser test stops deliberately at a mocked Company-profile read after proving the account gate passed, performs zero booking writes, and requires no Passenger/Boss billing-identity review.
- Full-gate diagnosis separately reproduced that the Driver Job browser assertions completed but the Node process could remain alive after the raw Chrome `SIGTERM` wait timed out, because the captured Chrome stderr pipe stayed open. The existing test harness now uses the shared bounded terminate/escalate helper and explicitly destroys only that captured pipe after Chrome cleanup; Driver page/runtime/native, push, badge, GPS, Calendar, status, message and provider behavior are byte-unchanged.
- The final PA-scope protection batch reproduced one test-only assertion failure identically on unchanged `origin/main`: its exact access-account select string predated the established `principal_cutover_at` and `legacy_link_revoked_at` fields. The guard now requires those two existing cutover fields in their exact current safe read order; customer access, session, privacy and runtime source are unchanged.
- The same unchanged-main comparison proved the Company Profile sync guard still named the retired generic/agency new-account choices even though the established browser suite now enforces the single `Create Company + Booker Account` path. Only that guard's UI and browser-evidence fragments are aligned to the current Company+Booker choice, field retention and pre-write fail-closed coverage; Company Profile, booking, CRM and Calendar runtime are unchanged.
- The complete preactivation suite also reproduced its compact action-feedback guard's obsolete visible `SMS checked` requirement identically on unchanged `origin/main`, after PR `#437` removed that blocked Customer Copy pseudo-button. That guard now explicitly rejects the retired SMS action selector instead; the disabled SMS route, ordinary Copy fallback and all notification runtime remain unchanged.
- The ledger source-of-truth gate then reproduced the top clean/pushed runtime pointer lagging one already-merged application commit on unchanged `origin/main`. At that checkpoint both runtime pointers were aligned to `d4afcd43 Simplify Customer Copy actions`; the current header now advances only with later separately verified runtime, push and deployment evidence.
- Signed-in Production no-write acceptance on exact full source `52846fa3f2461be7b26275ce3ef051ba44335a21` selected verified Company `53` plus Booker `26` with Passenger blank. The browser allowed only the exact account/read preflights and intercepted the sole `POST /api/admin-bookings` before network persistence, returning a local `409`; the draft remained `Not saved yet`, no billing-identity review or Passenger/Boss prompt appeared, and no booking, customer, account, Calendar, invoice, alert, push or provider write occurred.
- Rates, invoice layout/workflow, DSP/hourly calculations, Calendar, push/badge/fanout, Customer/Driver/native source, messaging, GPS, provider, schema and data lanes are unchanged. Protection remains in the existing Admin Dispatch CRM identity guard and Customer Account browser suite.

## Customer Copy Visible Noise And SMS Pseudo-Button Cleanup (2026-08-30)

- The established Customer Copy section no longer renders the redundant `Customer booking details.` subtitle, the three-state readiness label line, the manual WhatsApp/SMS helper sentence, the `Email recipient:` status line, or the blocked/no-op SMS pseudo-button. This is visible-copy/control cleanup only.
- The existing readiness result and Email gating still determine whether Email and Send In-App are enabled; the verified Booker email is still resolved and submitted through the existing Email handler. The admin-gated readiness API retains its three-state result for internal gating and diagnostics. The parked SMS GET-only route, handler and no-live protections remain unchanged and cannot send.
- `Copy`, `Edit`, `Email`, `Send In-App`, `Copy Booking Invite`, `Copy + App Link`, message preview/disclosure, the live-location helper and all Copy/app-link success or error feedback remain in their existing locations with their existing handlers. Ordinary Copy remains the manual external fallback.
- Focused source guards and Booking UI/app-smoke browser coverage require the removed visible strings and SMS selectors to stay absent while preserving the established Email, Send In-App, Copy, app-link, access, notification and privacy boundaries. No push/badge/native, automatic Driver ACK, Calendar, invoice, billing, payment, payout, DSP, GPS, provider, schema or data behavior is changed.
- The complete mocked Driver browser gate reproduced on untouched `origin/main` with two different immediate empty bridge-state assertions even though Driver route, native and test sources were byte-identical. The existing browser harness now waits only for the already-required asynchronous service-worker remembered-link and embedded native-notification bridge results before asserting them; no Driver runtime, native bridge, push registration or notification behavior changed.
- A later complete preactivation run reproduced one obsolete compact-feedback assertion identically on untouched `origin/main`: it still required the retired visible `SMS checked` button result after the approved SMS pseudo-button removal. That one test-only fragment is removed while the separate SMS disabled-route, no-live and Customer Copy absence guards continue protecting the blocked backend and absent UI; no runtime, handler, provider or notification behavior changed.
- The same restart proved the ledger header still named the earlier app-link runtime and PR `#432` deployment after PR `#437` had already merged and deployed the Customer Copy cleanup. The header now records exact latest runtime `d4afcd43` and exact verified deployed merge `19358121`; Git history and Production state are unchanged by this documentation alignment.

## Saved Customer App Link Independent Of Trip Readiness (2026-08-29)

- The saved-booking `Copy + App Link` action is eligible from one persisted booking reference plus its exact verified `customer_id + company_id + booker_id`. It no longer requires route, flight, vehicle, passenger-count, driver or Dispatch trip-readiness fields. The existing server route still re-reads the exact Booker relationship and requires that Booker's saved verified email before it can create an access account or one-use invitation.
- Fail-first focused and mocked-browser evidence reproduced the old block by clearing Pickup locally on an already-saved Company + Booker booking: the action became disabled even though no Calendar request was made. The focused readiness guard now rejects trip, customer-copy and driver-readiness dependencies; browser coverage requires the saved action to remain enabled with the Calendar-call count unchanged.
- Save + CRM validation and the separate Operations Calendar eligibility, sync, event identity and provider behavior are unchanged. The access handler, one-use 30-minute invitation, exact Booker recipient, local clipboard-only result and no-provider-send boundary are also unchanged. No Calendar, push/badge, native, invoice, DSP/pricing, payment, payout, GPS, provider, schema or data lane is added or modified.

## Customer Driver Details Readiness Label (2026-08-29)

- Owner-visible installed Admin evidence on exact Production build `c90a8d02` proved the Customer Copy readiness title was static: a blank unsaved draft displayed `Customer driver details ready` beside `Driver not assigned`, while loaded booking `10911` with complete safe driver name, contact, plate and vehicle correctly displayed the ready title and matching safe details.
- The established Customer Copy row now derives one truthful title from its existing driver-readiness result: no assigned driver name is `No driver assigned`; an assigned driver with incomplete contact, plate or vehicle details is `Waiting for complete driver details`; and the existing complete driver-details state remains `Customer driver details ready`. The existing admin-gated GET review item returns the same three-state title from its already-computed missing requirements.
- This readiness repair does not change `Edit`, `Copy`, `Copy Booking Invite`, Email, SMS, Send In-App, the manual WhatsApp Copy fallback, automatic Driver ACK customer delivery or their handlers. The separately approved saved-booking access repair below replaces the test-era `Manage Access` control; the removed visible WhatsApp pseudo-button stays absent.
- Focused fail-first evidence stopped on the missing `No driver assigned` route contract. The existing review-item API guard now executes no-driver, incomplete-driver and complete-driver cases; the existing Booking UI and app-smoke browser expectations protect incomplete and blank-draft labels. No native source, EAS build, booking/data write, provider send, Calendar, invoice, payment, payout, PayNow, push/badge or Customer/Driver workflow is part of this repair.

## Booker-First Copy + App Link And Company+Booker Customer Access (2026-08-29)

- Saved-booking Customer Copy now has one human `Copy + App Link` action. It requires the exact persisted `customer_id + company_id + booker_id`; the server re-reads that exact Booker and requires its saved Customer binding, name and email before any access-account write. The browser cannot submit a recipient email, role, Traveller, Passenger or membership scope.
- A successful first action creates one 30-minute, one-use activation link for the exact verified Booker email and copies it locally. It sends no Email, SMS, WhatsApp, push or provider message. An already-active exact Booker returns a truthful active status and does not create another invitation, reset the PIN or touch devices, sessions or subscriptions.
- New operational access is one nullable-Traveller root membership keyed by exact Company + Booker. Passenger/Traveller remains booking-specific. Root saved-booking, message-history and live-location reads require the booking's persisted Company and Booker to match exactly; another Company or Booker is blocked. Existing legacy PA/Boss Traveller memberships and multi-principal shared conversation/alert/badge fanout remain compatibility data and are not rewritten or removed.
- Principal activation now enforces the existing legacy-link cutover columns: once exact principal activation records `legacy_link_revoked_at`, the old permanent portal token is rejected even if its historical revision still matches. Legacy account 174 remains readable until it actually cuts over; QA account 192, its principals, memberships, invitations, devices, sessions and subscriptions remain intact pending separate destructive approval.
- The additive Supabase migration only makes `customer_access_memberships.traveler_id` nullable for `managing_pa` roots, adds one exact root uniqueness index and an active Company+Booker lookup index, and preserves all RLS/grants and existing rows. It does not delete or rewrite principals, memberships, devices, sessions, subscriptions or messages.
- Push sender, recipient fanout, badge counts, subscription registration, Customer/Driver native source, Calendar, invoice, billing, payment, payout, PayNow, GPS writers/polling and provider sends are protected and unchanged. Focused schema, access-link, principal, saved-booking, message/privacy, live-location, cutover and browser guards must pass before schema or application publication.
- The full preactivation restart stopped first on the permanent-link rebooking guard's obsolete browser-trusted Company/Booker and PA/Boss membership fragments. That same established guard now retains every legacy link-helper, account-revision, receipt and session compatibility check while requiring the current server-verified Booker email, exact Company + Booker root membership and fixed Booker-first role; it also rejects any browser-supplied recipient, role or membership scope. No receipt, booking-memory or legacy rebooking runtime changed for this guard-only alignment.
- The restarted source-of-truth guard then proved the ledger's pushed-runtime line still pointed to the earlier schema commit after PR `#434` had merged the verified Company + Booker application runtime. The top pushed checkpoint is aligned only to the exact latest runtime commit reachable from current `origin/main`, `03f64987 Complete Company Booker customer accounts`; no deployment claim, Git history or application source is changed by this docs-only alignment.

## Future Booking Company + Booker Identity Boundary (2026-08-29)

- Future Admin bookings use one Customer Account identity: exact verified Company ID + exact verified Booker ID. The normal create chooser exposes only `Create Company + Booker Account`; the former blank-Company Personal and agency-only new-account choices are not offered. A future booking missing Company or Booker fails before the booking, CRM or Calendar writer.
- Existing Company + Booker accounts remain one explicit selector. First-time or ambiguous ownership still uses the established `Use existing account` / `Create new account` review; a securely verified repeat preserves the approved account without another passenger prompt. Passenger remains booking-specific, clears any unverified Traveller ID and never chooses billing ownership.
- Email-AI, typed name, email, phone and company matches are suggestion-only. Neither the direct Customer-profile recommendation nor the older repeated-passenger status may preselect or bind `customer_id`, `company_id`, `booker_id` or `traveler_id`; Admin must choose the exact Customer Account.
- Legacy saved bookings with older Company-only or agency-shaped identity remain readable when that exact saved booking is loaded. An active orphan legacy Customer profile whose Company has no current Company + Booker option may also appear in the same future chooser only as an explicit conversion candidate labelled `Booker required`; it is not a complete billing identity, does not infer a Booker or Traveller, and cannot bind until Admin supplies the real Booker and approves the existing guarded `Use existing account` review. No legacy Customer, booking or invoice is rewritten, merged or deleted by merely listing or selecting the profile.
- Fail-first browser evidence previously allowed a blank-Company Mr Lee booking to create a Personal customer and Calendar continuation, allowed new agency-only accounts, and exposed a passenger-match action that bound CRM IDs. The focused guard and booking browser now require the Company + Booker-only choices, suggestion-only matching and zero booking/CRM/Calendar writes for missing identity.
- The additive identity migration was reviewed and merged separately through PR `#433` at main commit `a86324d9b634b088040a01bd762a983467a85773` before any application rollout. Its exact merged SQL SHA-256 is `dec2d172c19a511bc1ffecd20956e62303c5d61450ecf46d48587c595266d53a`; the authenticated Production Supabase migration runner applied only `company_booker_customer_account_identity` as generated remote version `20260829115454`. No historical migration record was edited or repaired.
- Production readback proves nullable `bookers.customer_id`, default `NULL`, one valid/ready unique partial index for non-null values, and a validated `customers(id)` foreign key with `ON UPDATE RESTRICT` and `ON DELETE RESTRICT`. All 15 existing Booker rows remain, with zero newly bound customer IDs. RLS remains enabled with no row policies, so this established server-only table remains default-deny to row access; existing grants were not broadened.
- Rates, invoice layout/workflow, DSP/hourly billing, Calendar source/routes/provider behavior, push/badge fanout, automatic Driver ACK customer details, Driver/Customer native apps and legacy history remain unchanged.

## Company + Booker Customer Rate Identity (2026-08-29)

- Customer rate identity is the exact verified Company ID + Booker ID pair. Same Company and same Booker resolve to one Customer Account rate profile; another Booker under the same Company is a separate choice and may carry different customer rates. Passenger, Boss, display name, email and phone never select or authorize a customer rate.
- Booker rates override Company rates, which override the global defaults. Traveller rates are legacy compatibility only when an approved Company + Booker account is absent. Existing legacy Company and Traveller rate rows remain readable and are not rewritten or deleted; normal Customer Account save/remove writes only the exact verified Booker row through the established gated customer-rates route.
- The existing Rates surface now uses one `Customer Account` chooser displaying `Company · Booker`. The normal Customer Account list and editor no longer present Traveller or Boss as customer-rate identity. Existing Company override compatibility and Company-owned driver payout/card/privacy behavior remain in the established section and writer; no duplicate panel, route, table or save lane is added.
- Saved booking `customer_id`, `company_id` and `booker_id` flow into the existing customer-folder, DSP correction and issued-invoice calculation consumers. Exact Booker ownership requires all three verified IDs to match the loaded rate record; a partial or mismatched pair cannot select a Booker rate. Passenger remains booking-specific and does not decide billing ownership or rate identity.
- The separate additive migration created through the Supabase CLI adds only non-null JSONB `bookers.customer_rates` with the existing empty-map default. Read-only Production audit before implementation found 24 Companies, 15 Bookers, 16 Travellers, 52 bookings, zero Company customer-rate overrides, two legacy Traveller overrides, 14 unambiguous Company + Booker tuples and no same-Company multi-Booker Production row at that checkpoint. No legacy row was changed. Its exact merged SQL SHA-256 is `cc75f66f885783716511331be9e1ea7064daa8568552576c0f9f54208a660360`; the authenticated Production runner applied only `booker_customer_rates` as generated remote version `20260829115615` after the identity migration.
- Fail-first focused evidence stopped on the missing current ledger contract, the missing Booker runtime action and the old traveller-first price result before implementation. Focused protection covers the exact additive `bookers.customer_rates` migration shape, exact Booker write filtering by Booker + Company + Customer IDs, Company fallback, legacy Traveller fallback only without an approved account, one human Customer Account chooser, no normal Traveller write, and unchanged protected consumers.
- The registered historical Rate override, customer-rates, payout and Company/Traveller CRM guards are aligned only at their runtime source boundaries: they now protect the established Company compatibility fallback plus exact Booker + Company + Customer scoped Customer Account save/remove, reject CRM identity creation or Traveller-targeted normal rate/payout writes from Rates, and keep Company payout removal separate. Their older planning sections and legacy helper-preservation assertions remain unchanged historical evidence.
- Production readback proves `bookers.customer_rates` is non-null JSONB with default `'{}'::jsonb`; all 15 existing rows are non-null and zero were populated by the migration. RLS/grants remain unchanged. Supabase security and performance advisors report no migration-specific error or warning; the existing server-only `bookers` RLS-without-policy information item, unused-index information item and unrelated leaked-password-protection warning remain recorded rather than altered in this bound.
- Driver payout, invoice layout/workflow, DSP billing rules, Calendar, push/badges and native apps remain unchanged. The migrations changed schema only: no Production booking/account/rate row, invoice, Calendar event, notification/provider state, native build or release was created or modified. Application publication remains blocked until the complete restarted guard, build, browser, staged-diff and parity gates pass.
- The complete gate restart also aligned three stale cross-lane invoice-planning guard assertions to the already-current Customer Copy lock sentence that forbids duplicate Email, WhatsApp or SMS sectors. No invoice source, workflow, layout, route, renderer, calculation or provider behavior changed.
- The full safe pricing suite failed first because its legacy Traveller fixture still expected the retired rate-source label `boss`. The fixture now supplies the established explicit `legacy_traveler` source and requires that exact label; Company + Booker rate results remain `account`, and no pricing calculation, amount, precedence or runtime source changes in this test-only alignment.

### Shared Admin Dashboard Root Default (source checkpoint 2026-08-28)

- The established shared Admin web root now initializes the existing `Dashboard` tab instead of `Dispatch`. Bare `/`, browser/PWA root, the existing `Admin Home` link, and the already-installed Admin native wrapper's unchanged root, cold/reload and root-notification entries therefore open the same Dashboard after the updated web page loads. This is one web-state default only; it adds no route, query handler, persisted last-tab state, local storage, Home control or duplicate lane.
- Manual tab selection remains unchanged. The existing `Dispatch` tab still opens the same complete intake, Save + CRM, customer/CRM identity, booking edit, Driver Job Link and operational workflow. The exact `/?tab=dispatch&booking_reference=...` handoff still overrides the root default, performs one guarded exact booking GET and loads that job into Dispatch; the completed-cancel handoff remains unchanged.
- Admin browser/PWA and native notification delivery, payloads, privacy-safe root target, service worker, badge counts, native parser, Face ID, mounted-WebView/background retention, authentication and recovery behavior remain unchanged. No Admin/Customer/Driver native source, Expo/EAS/TestFlight/App Store action, provider/device send, deployment, Production action, booking writer, Calendar, billing, invoice, payment, payout, PayNow, messaging, GPS, schema, migration or environment lane changes in this checkpoint.
- Focused protection keeps the current shared-message three-read refresh contract, verifies the root Dashboard before any browser click, retains manual Dispatch selection, preserves the exact Dispatch booking-query handoff, and leaves Admin browser/native root-notification guards unchanged. The installed Admin wrapper requires no new binary for this hosted web default; it consumes the change after a future separately approved web deployment and full page load/reload.

### Save + CRM Billing Identity Review One-Shot Continuation (source checkpoint 2026-08-28)

- The established Billing Identity Review still blocks the first `Save + CRM` attempt and preserves every existing required-field, verified-identity, collision and stale-review check. After Admin presses the existing `Confirm Account` or `Create New Customer` action for that exact current review, the app now consumes one booking-and-review-keyed pending intent and continues that same `Save + CRM` once; Admin no longer presses `Save + CRM` a second time.
- The pending intent stores only inert booking/review keys, never an executable callback. A synchronous continuation lock prevents a same-tick double confirmation from starting a second save. Booking or review mismatch fails closed without persistence, loading or starting a new booking clears the pending review/confirmation, and every completion or failure releases the continuation lock so the existing ordinary retry remains available.
- The auto-continuation begins inside the pre-confirmation React render closure, so one resolved billing-account override is now captured for that exact save invocation and reused for both its initial duplicate guard and its post-success `lastSuccessfulBookingSaveRef` key. This prevents the scheduled confirmation rerender from changing the otherwise identical save identity. Focused coverage locks both key writes, and the browser fixture proves an unchanged post-success Save action leaves the booking POST and existing Calendar continuation counts at exactly one.
- The continued action re-enters the existing `saveBooking` validation, customer/CRM collision handling, one `/api/admin-bookings` POST and unchanged post-save Calendar continuation. It adds no second writer, route, button, panel or save lane. The complete mock-only booking browser fixture proves zero booking POSTs before confirmation, two immediate confirmation clicks produce exactly one booking POST, and the existing mock Calendar continuation occurs exactly once without a second Save click.
- Before the application repair, the unskipped current-main browser baseline required two fixture-only realism alignments: the loaded saved-booking mock now carries the established canonical confirmed internal/customer statuses as well as its legacy status, and its existing `Load this booking` proof explicitly mocks and asserts the current exact guarded `/api/admin-bookings?booking_reference=...` GET. Those test-only changes do not skip a fixture, weaken the general request audit or alter application behavior.
- Protected Calendar routes, helpers, calls, labels, status, identity, content, reminders, OAuth and provider behavior are unchanged. `Update + Cal`, `Accept + Cal`, automatic Driver ACK to Operations Calendar, personal Driver Calendar, `app/customers/**`, invoice/billing/pricing/payment/payout/PayNow, Driver/Customer/native, messaging/GPS, schema/migrations, environment and provider lanes remain untouched. Focused protection is `scripts/test-save-crm-billing-identity-review-guard.mjs` plus the existing full `scripts/test-booking-ui-browser.mjs` fixture.

### Customer And Driver Accepted-Baseline Handoff (2026-08-28)

- The complete current continuation record is `docs/handoffs/2026-08-28-customer-driver-handoff.md`. Read it with `AGENTS.md`, this ledger, `git status --short --branch`, and `git log --oneline -10` before any Customer or Driver work. It distinguishes source/guard evidence, owner-confirmed physical acceptance, untested items, exact QA cleanup, protected workflows, App Store readiness, and next safe actions. Earlier dated statements below remain historical evidence; this handoff is the current continuation checkpoint and does not rewrite their at-the-time status.
- Authoritative main at this checkpoint is `69ebcf1a`, containing PR `#428` Driver private-job copy cleanup (`1f0fc633` → merge `b47ea02c`) and PR `#429` Driver/Customer Apple-native governance lock (`d3c6035e` → merge `69ebcf1a`). The accepted Apple baselines remain Customer `Prestige SG` `0.1.0 (12)`, bundle `sg.prestigelimo.customer`, Apple build `b42ea290-492b-472f-bcd1-72a154d9523d`, and Driver `Prestige SG Driver` / installed `Prestige Driver` `1.0.0 (17)`, bundle `sg.prestigelimo.drivercompanion`, Apple build `ca106e08-4f36-44d6-afe9-012d7a726680`.
- Owner-confirmed physical acceptance includes Driver Build 17's 180-second Face ID boundary, opaque app-switcher cover, stationary locked/background GPS freshness beyond six minutes, Customer live map, personal Calendar, POB, delayed OTS-photo arrival, Admin-to-Driver message/badge, Driver Alert Admin and JC Admin/Customer alerts/badges. Final booking `10908` acceptance also proved Driver-to-Customer and Admin-to-Customer messages on both PA and Boss, plus the Boss notification deep link opening exact booking `10908`. Customer 60-second grace, privacy cover, and badge-reset behavior retain source/guard evidence but are not overstated here as separate owner-confirmed physical acceptance.
- Exact QA bookings `10906` / internal `229` / `QA-CUSTOMER-BADGE-20260827-001` and `10908` and their proven booking-specific database, Storage, notification, message, GPS/status/link and Calendar artifacts were removed with zero exact residue. Reusable QA identities, access, principals, memberships, devices, sessions, subscriptions, verified Driver identity/access/enrollment/device, and reusable Calendar connection were preserved. Do not restore or recreate either booking.
- No new Customer or Driver binary is justified from this checkpoint. No public App Store metadata or build was selected or submitted, and no App Store Connect write, external testing, App Review submission, phased release, or public release occurred. Both public releases are **HOLD — Admin completion and final cross-app regression required**. The parked App Store metadata/screenshot packet created no local artifact; prepare it fresh only after that regression and under a separate exact approval.

### Driver And Customer Apple Native App Owner Governance Lock (2026-08-27)

- The owner locks only the two accepted Apple-native project baselines: Driver Build 17 in `driver-companion/` and Customer Build 12 in `customer-companion/`. Native source/configuration, package/lockfile, permissions, entitlements, bridges and release identity remain owner-controlled. There is no permanent designated engineer: every Codex agent remains read-only by default for both native projects.
- Before any native edit, every Codex agent must ask for and receive fresh explicit Admin/Owner approval for that exact bounded task. No agent may self-appoint, infer or reuse a prior approval, transfer authority automatically, perform an automatic handoff, or act as an unapproved replacement. After approval, only the engineer the Admin/Owner permits may perform that bound, following approved-runtime defect reproduction, focused guard and ledger evidence.
- Any later native repair requires exact approved-runtime reproduction, one explicit owner-approved bound, a focused regression guard and ledger evidence, complete no-cost native/privacy/release/downstream checks and exact diff review. Paid EAS, TestFlight/App Store mutation, Production deployment and data/provider writes remain separate action-time approvals. Existing remote auth, navigation, alert/badge, messaging, GPS/tracking, Calendar and privacy consumers must be preserved whenever an authorized native repair is made.
- Owner physical acceptance on Driver Build 17 confirms one Admin-to-Driver in-app message appeared and the Driver app-icon badge appeared. This does not claim notification tap/open targeting, badge reset, generic/new-job or one-hour pickup-reminder APNs acceptance, or Driver-to-Customer messaging.
- Owner physical acceptance also confirms the Driver Build 17 `Job Completed` action passed, Admin received its JC in-app alert and app-icon badge, and Customer received its JC in-app alert and app-icon badge. This does not claim notification tap/open targeting or badge reset. Booking `10906` still requires owner physical confirmation that its JC evidence remains in Admin Driver Reports/Active Assigned Jobs and did not move automatically to Completed/History before the existing explicit `Admin confirm completed` action; no booking mutation is part of this lock.
- This is a repository instruction and guard, not an OS or GitHub ACL. Codex itself must enforce the read-only default and fresh exact approval rule. It does not broadly freeze remote web/API, Admin, invoice or billing work and it changes no application/runtime/package/schema/environment/release/data/provider behavior. Protection is `scripts/test-driver-customer-native-owner-governance-lock.mjs`, registered in preactivation verification.

### Driver Private-Job Copy Noise Cleanup And Help Disclosure Move (2026-08-27)

- The owner-approved presentation cleanup removes only the four owner-identified static help-copy groups from the existing private Driver Job surface: the one `Message Customer` explanation; the `Report Issue` introduction and footer; the verified embedded/native blue privacy note; and the two `OTS Photo to Admin` explanations. Ordinary mobile web retains `Mobile web driver card. Keep this link private and use it only for this assigned job.` The installed-app header, build marker and safe job card remain unchanged.
- Every functional label and state remains: `Message Customer`, its text placeholder, `Send to customer`/busy/closed/feedback states; `Report Issue`, issue field, placeholder, nine safe options, `Alert Admin`/busy/validation/success/error states; and `OTS Photo to Admin`, Ready/Sent state, Photo/Shoot/selected-file control, hidden camera input, `Send Photo to Admin`/busy/validation/success/error states. No replacement help sentence, control, panel, handler, route or writer is added.
- The same bounded presentation pass moves the single existing `How this page works` disclosure intact to the bottom of the private Driver Job page. Its title, collapsed native disclosure behavior, helper, native and mobile-web lists, privacy boundary, data selectors and content remain exactly once; only its DOM/CSS order changes from the earlier pre-message position to after `Report Issue`.
- `scripts/test-driver-private-job-copy-cleanup-guard.mjs` failed first on the exact still-present Driver `Message Customer` explanation. It now locks all seven removed static strings, all essential controls and feedback, the native-only note removal with ordinary mobile-web warning preserved, one unchanged bottom disclosure, this ledger checkpoint and preactivation registration. Existing typed-message, OTS-photo and embedded-safe-summary guards plus the two Driver browser suites are aligned to protect the same established controls and bottom layout rather than retired copy.
- The complete mocked app-smoke suite stopped before its Driver checks on an older Admin assertion that expected successful `Update + Cal` to clear the applied booking identity. A clean detached `origin/main` baseline reproduced the identical `LOADED-OPS-001` result. The authoritative no-reload handoff intentionally retains that exact returned booking when no customer-folder return is pending and focuses the existing Driver Job Link Create Link control. The test-only assertion now requires the retained exact mock identity, visible existing handoff and focused existing Create Link button; no Admin or Driver runtime, handler, route, persistence or workflow changes.
- After that alignment, the same broad suite reached `/my-bookings` and rejected its established same-origin `/api/customer-principal-access` mount read because the Customer portal allowlist predated PA/Boss principal access. Current source always performs that private no-store read to distinguish one active signed principal/device/membership session from legacy Customer access; missing or invalid access fails safely to `403`. The test now permits that exact endpoint only in the `/my-bookings` Customer portal runtime allowlist, not globally or on public booking surfaces, while every other API, storage, payment, notification, provider and WebSocket ban remains intact. No Customer runtime, session, route or authorization behavior changed.
- The next broad `/my-bookings` assertion still required four retired fixed one-tap Customer-to-Driver buttons. Commit `3613624c` intentionally replaced those controls with the current PA/Boss typed shared conversation, and the existing focused Customer source/browser guards already require its composer, bounded `client_message_id + message_text` send and exact-booking privacy boundary. The broad mock now requires zero retired quick-reply buttons plus the visible established `Type a message to the driver` composer and `Send to driver` control. No Customer message, authorization, persistence, history or runtime source changed.
- No message, issue alert, OTS photo, status, Calendar, GPS, notification, booking, customer, invoice, billing, payment, payout or PayNow behavior changed. Admin/Customer surfaces, tokens, authorization, privacy projections, API/persistence/storage, schema, environment, provider sends, native shell, EAS/TestFlight and Production data remain unchanged.

### Customer Message Driver Copy Removal And Build 12 Consolidation (2026-08-27)

- The owner explicitly requested that the explanatory sentences in the existing Customer `Message Driver` card be removed and that explanatory area remain blank. Read-only complete-path inspection located one exact static paragraph—`Boss and managing PA share this booking conversation. The driver sees the verified Boss name.`—between the existing heading and composer. The existing text area, `Send to driver` action, typed `client_message_id + message_text` request, verified Customer principal and exact-booking authorization, verified Boss display identity, shared PA/Boss conversation, notification outbox, Driver/Admin message history, pre-POB closure and privacy filters are separate established behavior and remain unchanged.
- The narrow UI repair removes only that paragraph. It does not add a replacement sentence, helper, card, route, composer, sender, provider or write path. `scripts/test-customer-driver-quick-reply-ui-guard.mjs` failed first while the paragraph remained, then requires the `Message Driver` heading to continue directly to the established composer, requires the retired sentence to remain absent, and retains every established source, persistence, PA/Boss, notification, history and privacy contract.
- Owner physical acceptance on exact reusable QA booking `10906` proved the Customer-to-Driver typed conversation persisted and appeared in the established Admin message history, while both Driver-to-Customer and Admin-to-Customer returned the same `403`—`Customer app notifications require secure customer account auth before saved notifications can be read.` Read-only Production data evidence resolved exact booking company/booker/traveller scope, one active Boss membership, one active managing-PA membership, two active principals, two active devices and two active native subscriptions. The two rejected attempts created zero Customer notification-outbox rows, so this was a pre-insert authorization failure rather than a delivery, read or provider failure; the failed messages are not retried or recreated.
- Complete source tracing proved both Customer-target producer paths still authorized by the retired controlled customer-ID allowlist after exact active principal/device subscription access had replaced that legacy Customer account lane. The narrow repair reuses the existing native Customer audience resolver already used by exact-booking Customer push: the Admin `admin_customer_job_messages` writer and token-scoped Driver-to-Customer quick reply may proceed only for the same exact booking with active Boss or same-company/booker managing-PA membership, active principal, active device and active `native_expo` subscription. Booking/link scope, the Driver pre-POB gate, safe templates/free-text bounds, insert-before-best-effort-push persistence, Customer-to-Driver, Admin visibility, customer-only target visibility and every privacy exclusion remain unchanged. A fail-first booking-`10906`-shaped contract reproduced the old `403`; the repaired contract proves both exact producers save one row without a provider send, while an otherwise active principal audience with no active native subscription remains `403` with zero inserts.
- The owner required the one existing expanded My Bookings detail lane to read in this exact order: the existing `Message Driver` composer and `Send to driver`; the existing Customer/Driver `Trip Updates` cards; then the existing `Driver Tracking` controls and map. The repair moves only the already-wired rendered blocks inside that same detail section. It adds no duplicate panel, message lane, state, timer, request or map. Refresh/close/track controls, message cards and statuses, the eight-second tracking refresh, mounted-panel return, tracking eligibility, map URL/data, accessibility and mobile containment remain wired to their existing handlers and adapters.
- Customer-visible canonical service codes now use the owner-approved labels `Arrival`, `Departure`, `City Transfer` and `Hourly` only in the Customer saved-booking adapter. Stored `MNG`/`DEP`/`TRF`/`DSP`, APIs, parser, Admin, Driver, Calendar, invoice/billing descriptions, rates and persistence remain unchanged. The adapter failed first with visible `MNG`; its focused contract now proves all four exact labels and forbids the four codes in mapped Customer display output.
- The owner separately approved removing only the redundant ready-state sentence `Latest driver trip updates are shown below.` while retaining the existing `Refresh` label. The trip-update adapter failed first on that exact sentence, then returns an empty ready-state summary so the UI renders the existing update cards directly without a blank bordered notice. Blocked and empty explanations, update cards, labels, ordering, polling and controls remain unchanged. Mocked browser coverage at 390, 430 and 440 px proves `Message Driver → Trip Updates → Driver Tracking`, the exact service label, no redundant ready sentence, no horizontal overflow and zero browser errors without any non-GET request.
- The same read-only audit and passing focused executable guards prove the current Customer source already keeps one mounted WebView under an immediate opaque, non-interactive and accessibility-hidden privacy cover; reveals it without Face ID only for a valid monotonic return below `60,000ms`; requires one Face ID attempt at or above `60,000ms`, cold/invalid/backwards timing or cancellation; retains one open tracking panel and eight-second refetch loop; and enforces the server-side `OTW/OTS + fresh GPS + within 30 minutes of pickup` rule with its existing Customer explanation. These established lanes are not rewritten for Build 12 and remain physical iPhone acceptance items.
- App Store Connect requires the next immutable Customer binary number, so only `customer-companion` `ios.buildNumber` advances from processed Build 11 to proposed Build 12. App name `Prestige SG`, public version `0.1.0`, iPhone-only light mode, bundle `sg.prestigelimo.customer`, EAS project `ce71ff91-7f71-4297-bcef-edf420f94316`, App Store Connect app `6802691447`, Apple Team, frozen signing credentials and the existing internal `Owner Testing` group remain unchanged. One paid build may start only after every no-cost source, native, privacy, release, Metro/Hermes, staged-change and identity check passes; any mismatch is a stop condition with no blind retry.
- The exact reviewed 17-file Customer-only source bound passed the complete no-cost matrix and merged through green PR `#423` as main commit `4171a603010e9bf53ff6961225dd765d67370a8a`. Git-linked Vercel Production deployment `dpl_AufYdPAfK1BEzsyT21FBGCc92bLC` is `READY` and `PROMOTED` at `app.prestigelimo.sg` with that exact source commit. No manual Production deployment, Production data write, booking mutation, provider send or QA cleanup occurred.
- Exactly one paid Customer Build 12 was created: EAS build `b2b97b87-8a7b-420c-9f93-22efcc699eee`, fingerprint `1a29432e9f3bdee43e17d7ec57fa6fd0ba77b88a`, from exact merge `4171a603010e9bf53ff6961225dd765d67370a8a`. The downloaded signed IPA SHA-256 is `3fc78b198a71c8b53c9d91928d527a8e5220bb071225c79f9c26653a336fd267`; direct inspection proves `Prestige SG`, `sg.prestigelimo.customer`, version `0.1.0`, build `12`, iPhone-only light mode, production APNs, Team `U9Y2574Y7S` and provisioning profile `de9f56aa-c6b4-4d6f-b9ec-7e01c549d79d`. Exactly one submission, `e5da7d88-048c-45be-9673-df9d3d32681b`, delivered it to exact Customer App Store Connect app `6802691447`; Apple build `b42ea290-492b-472f-bcd1-72a154d9523d` is `VALID` and `IN_BETA_TESTING`. Exact existing internal `Owner Testing` group `c66d522b-c799-4134-be22-225a7192bf4a` reads back `1 Tester · 12 Builds` with `0.1.0 (12)` as `Testing`; external state remains `READY_FOR_BETA_SUBMISSION`, with no external testing, App Review submission or public release. No second build, submission, group or tester mutation occurred.
- A separate read-only Stripe audit confirmed that Customer invoices remain authenticated view/download-only and that no Stripe dependency, Checkout/payment route, webhook or Stripe Production environment-variable name exists. The Customer native navigation allowlist also does not authorize Stripe-hosted pages. Stripe therefore remains parked as a separately approved test-mode provider lane and is not bundled into Build 12; no Stripe code, configuration, environment value, provider object, payment status or native navigation changes here.
- Driver Build 17 background/stationary GPS is a separate native binary and is not changed or rebuilt in this Customer consolidation. Reusable QA booking `10906` remains protected for physical acceptance and later exact cleanup. Admin, Driver workflow, alerts/badges, booking, Calendar, invoice, billing, payment, payout, PayNow, schema, environment, provider sends and Production data remain unchanged.

### Customer Native Rendered-Map False Warning Repair (2026-08-27)

- Physical Customer Build 11 acceptance on reusable QA booking `10906` proved the repaired Google map now renders inside Prestige SG with a fresh `Live` marker, `Updated 27 Aug 2026 at 17:03 SGT` and `Accuracy 14m`. The existing yellow blocked-navigation notice still appeared above that successfully rendered map, so the core map repair passed but the complete native acceptance did not.
- A signing-disabled disposable Release simulator using the exact shipped callback reproduced the physical result and captured the full navigation sequence: the exact Prestige page, non-top-frame `/maps?...&output=embed` request and bounded `/maps/embed?origin=mfe&pb=...` redirect were allowed, then Google emitted two exact non-top-frame `about:blank` iframe bootstrap requests. The shipped helper rejected both; the existing callback set the generic warning on each rejection and no later successful navigation cleared it. The map remained fully rendered. This proves the notice was a false positive from a harmless iframe bootstrap, not an external/top-level navigation, GPS, tracking, Customer session, booking, Driver or Production page failure.
- The existing Customer map-navigation helper now additionally accepts only the exact string `about:blank` when WebView explicitly marks it non-top-frame. The same URL as top-frame or without frame evidence remains blocked. `about:srcdoc`, `data:`, `javascript:`, `file:`, external origins and every existing forbidden Customer/map/top-level URL remain blocked. The existing warning wording and behavior for a real blocked navigation are unchanged.
- `scripts/test-customer-companion-inapp-map-navigation-guard.mjs` failed first on that exact non-top-frame bootstrap, while its top-frame and undefined-frame assertions remained closed. The same focused guard retains the complete Google origin/path/parameter/coordinate/zoom/hash and wildcard rejection matrix and now explicitly rejects the other local-document schemes.
- The same signing-disabled disposable Release simulator was rebuilt with the exact repaired helper and real callback. It visibly rendered the map and reported `No blocked navigation`; its event list recorded both Google URLs followed by both exact non-top-frame `about:blank` requests as allowed. Screenshot evidence is `/private/tmp/prestige-customer-map-blank-bootstrap-pass.png`. This is no-cost Simulator proof, not physical iPhone or TestFlight acceptance.
- Complete no-cost verification passes all 137 registered Customer/public guards from a fresh start; changed-file ESLint; Customer and root TypeScript; Expo Doctor 21/21; resolved iPhone-only light-mode public configuration; and the Next.js 16.2.11 Production build. A fresh iOS Metro/Hermes export produced bytecode SHA-256 `75218a1daa757b55903fab537bc4a21abb7d707622b1084a6cba9a0c63927010` and retains the exact `about:blank`, Prestige/Google origins, `/maps/embed`, `origin=mfe` and existing real-navigation warning markers.
- The broad safe suite again passes parser, pricing, Driver-link, API and Admin-AI steps before the unchanged pre-existing `app/admin-sign-in/admin-sign-in-form.tsx:58` effect-state lint error. The full preactivation runner again passes phone OTP and browser-fixture boundaries before the unchanged Admin Email AI `PATCH` versus global no-live guard. Neither unrelated Admin source or guard is modified or waived.
- No Customer tracking control, page wording/workflow, map URL, 30-minute gate, session, PA/Boss access, alerts/badge, Face ID/privacy behavior, booking, Driver/Admin, message, Calendar, invoice, billing, payment, payout, PayNow, schema, environment, provider or QA data is changed. No EAS Build 12, TestFlight mutation, external send, Production data write or QA cleanup is authorized by this source repair.

### Customer Native Google Embed Redirect Repair And Build 11 Preflight (2026-08-27)

- Customer Build 10 physical acceptance failed on reusable QA booking `10906`. The installed Prestige SG app showed the exact Driver Tracking panel as `Live`, a current `Updated 27 Aug 2026 at 15:42 SGT` timestamp and `Accuracy 7m`, proving Customer authorization, the 30-minute eligibility gate, Driver OTW/sharing, latest-position storage and the Customer live-location read were working. The map frame itself remained blank and the native wrapper displayed its existing blocked-navigation security notice; the iPhone Maps app did not open.
- A read-only GET of the exact generated iframe URL proved Google returns HTTP `301` from `/maps?q=1.3271117,103.855347&z=16&output=embed` to the same-origin `/maps/embed?origin=mfe&pb=...` form, followed by HTTP `200`. Build 10 allowed only the first exact non-top-frame `/maps` request, so its native navigation policy rejected Google's observed same-frame redirect and caused the blank panel. This is a native WebView policy defect, not a GPS, Customer session, booking, Driver, Production API or Google map-data failure.
- The existing single Customer navigation helper now additionally accepts only that observed non-top-frame `https://www.google.com/maps/embed` shape: exactly one `origin=mfe`, exactly one bounded `pb` coordinate payload with zoom `16`, no extra query key, hash, credentials, invalid coordinate or alternate origin. Both the initial and redirected Google map URLs remain blocked as top-level Customer pages. Wildcard HTTPS remains forbidden and the existing Prestige Customer pages, Universal Links, native contact actions and security notice are unchanged.
- `scripts/test-customer-companion-inapp-map-navigation-guard.mjs` failed first on the exact observed redirect and now executes it alongside top-frame, scheme, origin, path, parameter, duplicate/extra parameter, coordinate-range, zoom, hash and wildcard rejection coverage. Customer TypeScript passes after the repair.
- The complete Customer/public preflight exposed one stale test-only expectation in `scripts/test-retired-ritz-mock-customer-guard.mjs`: it still required three Ritz/Marriott mock-domain rows that commit `b2bba2aa` had intentionally removed with the obsolete Dispatch mock-customer suggestion lane. The guard now protects that committed retirement by requiring those three rows to remain absent. No application/runtime code was changed for this test correction.
- The restarted Customer/public preflight then exposed one stale source fragment in `scripts/test-customer-copy-multi-channel-existing-workflow-lock.mjs`: it still required the retired trip-details helper call that commit `3613624c` intentionally replaced with the verified PA/Boss principal-access invitation and active-access update lane. The guard now locks the current invitation wording, one-time email/PIN activation, 30-minute expiry, active-PA update handling, manual-copy/no-provider-send boundary and the absence of the retired helper. No Admin or Customer runtime source or wording was changed for this test correction.
- The next release-identity gate correctly stopped because `scripts/test-native-apple-badge-testflight-release-guard.mjs` still pinned the already-processed Customer Build 10. Its Customer build lock now advances only to proposed Build 11 while retaining exact Admin Build 7, Driver Build 17, all three names, bundle identifiers, EAS project IDs, App Store Connect app IDs, light-mode configuration, notification plugin and historical badge-release evidence.
- The public-navigation gate then exposed its pre-principal-access `/my-bookings` allowlist. Commit `3613624c` intentionally added one exact `/customer-access/sign-in` Link for a blocked native session and one same-origin `window.location.assign("/customer-access/sign-in")` after the established Customer logout request completes. The guard now allows exactly those two bounded recovery paths plus the existing `/book` Link and continues denying every other imperative, external, Admin and session navigation. No public runtime source or wording was changed for this test correction.
- The release-hardening test-of-test then expected the Driver public-information allowlist in a different source order from the authoritative, already-passing `scripts/test-public-driver-job-action-surface-guard.mjs`. Its assertion now matches the established exact order `/driver-portal`, `/google-calendar`, `/privacy`, `/terms` while retaining the four-Link count and OTS photo boundary. No Driver source, route, action or behavior was changed for this test correction.
- The Customer-folder Dispatch handoff guard then retained its pre-`4acbb4c7` synchronous loader fragment. The established exact booking GET already returns the full Admin persistence record, so the current handoff intentionally calls the asynchronous loader with `adminBookingRecordOverride: targetBooking` and `focusCustomerCopy: true`; this preserves the saved Customer Special Request while retaining the existing Customer Copy focus. The guard now requires that exact override handoff. No runtime, invoice, customer-folder or Customer Copy source changed for this test correction.
- The public Customer portal session-surface guard then retained the pre-`3613624c` fixed saved-bookings query. The established adapter now builds exact `limit=25` and `page=1` parameters, adds `traveler_id` only for a safe integer verified traveller scope, and fetches the serialized bounded query with the same `no-store`, same-origin credential and customer-purpose protections. The guard now requires that exact shape. No runtime, session, authorization or saved-booking adapter source changed for this test correction.
- The permanent-link rebooking/receipt guard then still expected the Admin access route to issue another legacy link revision. Commit `3613624c` intentionally changed only that normal Admin access handoff to the current verified-membership PA/Boss principal invitation, returning bounded access status, principal ID and one-time invitation URL under `customer-principal-invitation-v1`. The guard now protects those current route fragments while retaining every legacy link-helper/account revision check needed for existing receipt and session compatibility. No runtime, receipt, access, session or invitation source changed for this test correction.
- The pricing/customer-rates boundary guard then still sliced through a legacy Traveller insert helper intentionally removed by commit `996c3f3f`. The current pricing lane retains the company legacy-create fallback, but a Traveller override requires one existing verified Traveller and updates that exact record through the established separate customer-rate and driver-payout boundaries. The guard now bounds the company helper correctly, requires the obsolete Traveller-insert helper to remain absent, and retains all company/Traveller update, remove and pricing/payout separation checks. No pricing or runtime source changed for this test correction.
- The following customer-rates app-wiring guard encoded obsolete indentation around that same established Traveller conditional. Its assertion is now whitespace-tolerant while still requiring `travelerCustomerRatesRuntime` to depend on `hasCustomerRateOverrides` and call `saveCustomerRatesRuntime` only in the true branch; the existing ordering and legacy-overwrite checks remain unchanged. No pricing or runtime source changed for this test correction.
- A disposable no-cost iOS harness copied the exact repaired navigation helper byte-for-byte, built an explicit Release `iphonesimulator` app with code signing disabled, installed it on simulator `E3C40530-6312-4CC8-9DD3-EC7EC503978A`, and visibly rendered the exact Google map at the physical-failure coordinates with `No blocked navigation`. This proves the observed redirect can render under the repaired native policy in Simulator; it is not physical iPhone or TestFlight acceptance.
- Final no-cost preflight passes all 137 registered Customer/public guards from a complete restart; changed-file ESLint; Customer and root TypeScript; Expo Doctor 21/21; the resolved public Expo configuration; the Next.js 16.2.11 Production build; and the disposable staged-app-change ledger/guard gate. A fresh iOS Metro/Hermes export produced bytecode SHA-256 `04c952e5165c7221f4b835c68b8147f0578eab5b655d2a3394c8ab876121be4e` and retains the exact Prestige origin, Google origin, `/maps/embed` and bounded navigation-helper markers.
- The broad safe suite again passes parser, pricing, Driver-link, API and Admin-AI steps before the unchanged pre-existing `app/admin-sign-in/admin-sign-in-form.tsx:58` effect-state lint error. The full preactivation runner again passes phone OTP and browser-fixture boundaries before the unchanged Admin Email AI `PATCH` versus global no-live guard. Neither unrelated Admin source or guard is modified or waived in this Customer release.
- Only Customer `ios.buildNumber` advances from processed Build 10 to immutable Build 11. App name, public version, iPhone-only light-mode target, bundle, EAS project, App Store Connect app, Apple Team, frozen signing credentials and existing internal `Owner Testing` group remain unchanged. No paid Build 11 may start until every prebuild check passes. Any source, native, privacy, release, Metro/Hermes, identity, credential, entitlement, target or staged-app-change mismatch is a stop condition with no blind build or retry.
- Customer wording, tracking controls/data/API, session and PA/Boss scope, alerts/badge, 60-second Face ID grace and privacy cover, booking, Driver/Admin, Calendar, invoice, billing, payment, payout, PayNow, schema, environment, provider send and reusable QA booking `10906` remain unchanged. Physical acceptance remains required after Build 11; QA cleanup remains protected until the in-app map and app-switch return both pass.
- PR `#420` passed both Vercel checks and merged the exact reviewed source to `main` as `b822f75c08e9cbbb8e94a4e01bd164a5dd51a8b1`. Automatic Vercel Production deployment `dpl_21cqk3oXMZYfe5f7ww6ZizBUSLnF` reached `READY`, targets Production, owns `app.prestigelimo.sg`, and reports that exact verified Git SHA. The public root follows its established sign-in redirect and returns HTTP `200`; no manual redeploy, promotion, environment change or Production data write was used.
- Exactly one paid frozen-credentials Store build, EAS build `87da252c-b107-485f-95bf-f79f928f1849`, finished from that exact merge as Customer `0.1.0 (11)` with fingerprint `28b2f1ac0b9a2b5ad76c593676001bd347d0d45a`. The exact downloaded IPA SHA-256 is `706b3debbc2211334c523cfdd012e52ec52b128583337022da2c288888227c76`; archive, installed name, bundle, version/build, arm64 iPhone-only family, iOS 16.4 minimum, light mode, Production APNs, `get-task-allow=false`, exact Customer associated domain, Apple Team `U9Y2574Y7S`, and frozen active provisioning profile `de9f56aa-c6b4-4d6f-b9ec-7e01c549d79d` all passed direct inspection. The profile and signed entitlements are byte-identical to Apple-accepted Build 10, OpenSSL verified the embedded profile CMS, and the compiled Hermes bundle retains the exact Prestige/Google origins, `/maps/embed`, `origin=mfe` payload markers and Customer navigation security notice while Build 10 lacks `/maps/embed`. The local Mac trust lookup returned the same `CSSMERR_TP_NOT_TRUSTED` warning as accepted Build 10, so no broader local trust-chain claim is made.
- Exactly one EAS submission, `b91be646-1c63-4c10-9059-35fdf9798f6c`, completed without retry against existing Customer App Store Connect app `6802691447`. Apple resolved exact Build 11 ID `05123adb-226d-49ae-ba70-7d3d40065f08` as `VALID`, unexpired and ready for internal testing. EAS did not attach the requested group, so signed-in App Store Connect added only exact Build 11 to existing internal `Owner Testing` group `c66d522b-c799-4134-be22-225a7192bf4a`; visible readback proves the unchanged group has one tester and eleven builds and exact `0.1.0 (11)` is `Testing`. Independent provider readback reports Build 11 `IN_BETA_TESTING` while external state remains `READY_FOR_BETA_SUBMISSION`.
- No second EAS build or submission, credential replacement, group/tester creation or mutation, automatic future-build access, external testing, App Review, public release, notification, booking/status, GPS, message, Customer wording, Driver/Admin app, Calendar, invoice, billing, payment, payout, PayNow, schema, environment or QA cleanup write occurred. TestFlight processing and Simulator evidence do not prove the physical map repair: the owner must install exact Customer Build 11 and confirm the in-app map plus leave/return privacy-cover flow before reusable QA booking `10906` is eligible for separate cleanup.

### Customer Native In-App Driver Map And Build 10 Repair (2026-08-27)

- Physical Customer Build 9 acceptance on exact QA booking `10906` proved the existing `Track driver` action obtained a fresh authorized marker but immediately opened the iPhone Maps app instead of rendering the existing map panel inside Prestige SG. The same embedded map remained correct in an ordinary browser, isolating the failure to the installed Customer WebView rather than the Customer live-location API, Customer ownership, Driver sharing, the 30-minute gate, booking data or Google map URL generation.
- Exact source and `react-native-webview` behavior proved the cause: the established panel loads one `https://www.google.com/maps?...&output=embed` iframe while the native wrapper's `originWhitelist` contained only `https://app.prestigelimo.sg`. The WebView library hands a non-whitelisted navigation to iOS, so the external Maps app opened before the existing Customer navigation policy could retain the iframe.
- The bounded native repair adds only the exact `https://www.google.com` iframe origin and allows only one canonical non-top-frame `/maps` request with exact `q`, `z=16` and `output=embed` parameters plus bounded latitude/longitude. The same URL as a top-level request remains blocked; top-level Google navigation remains blocked, wildcard HTTPS remains forbidden, and all existing Prestige Customer pages, Universal Links, contact actions and security notices retain their established policy. The driver map must remain inside the Customer app.
- `scripts/test-customer-companion-inapp-map-navigation-guard.mjs` failed first on the absent subframe policy and now executes the exact accepted iframe plus top-frame, origin, path, parameter, duplicate/extra parameter, coordinate-range and wildcard rejection matrix. It is registered in preactivation beside the existing Customer foundation and Universal Link guards.
- The pre-Build 10 gate then stopped before any paid build because Expo Doctor found Customer Build 9's isolated Expo `57.0.15` / React Native `0.86.2` patch set one supported SDK 57 patch behind. Only the Customer companion package and lockfile align to Expo `~57.0.17`, React Native `0.86.3` and Expo's exact compatible module patches; SDK 57, application identifiers and every workflow remain unchanged. Expo Doctor now passes all 21 checks. The Production dependency audit has zero high or critical findings; ten inherited moderate Expo/Xcode build-tool advisories remain because npm's proposed forced fix would incorrectly downgrade Expo to 46, so no destructive audit fix is applied.
- App Store Connect requires the next immutable Customer binary number, so only Customer `ios.buildNumber` advances from processed Build 9 to `10`. The app remains `Prestige SG`, public version `0.1.0`, iPhone-only light mode, bundle `sg.prestigelimo.customer`, EAS project `@prestige-limo-ops/prestige-customer` / `ce71ff91-7f71-4297-bcef-edf420f94316`, App Store Connect app `6802691447`, Apple Team `U9Y2574Y7S`, frozen signing credentials and existing internal `Owner Testing` group `c66d522b-c799-4134-be22-225a7192bf4a`.
- No Customer wording, tracking button, map data, API, session, PA/Boss scope, notification, badge, Face ID timing, booking, Driver, Admin, Calendar, invoice, billing, payment, payout, PayNow, schema, environment, provider send or QA cleanup changes. A paid Build 10 may start only after every source, native, privacy, release, Metro/Hermes and staged-app-change check passes; any identity, credential, entitlement, target, signed-binary or group mismatch is a stop condition with no retry. Physical acceptance must then prove `Track driver` keeps the map inside Prestige SG, background GPS remains fresh, Customer leave/return retains the open panel under the privacy cover, and top-level external navigation remains blocked before QA cleanup.
- Pre-build verification passes the new fail-first guard, Customer foundation, Universal Link, biometric, principal native-alert, live-location adapter/runtime/app-side, PA-scope and public privacy guards; focused Customer lint; Customer and root TypeScript; Expo Doctor 21/21; resolved public Expo configuration; the root Next Production build; and a fresh iOS Metro/Hermes export whose compiled bundle SHA-256 is `6aecfccfd3a22027fd03eec407c34d62598a0c7219644c2429504ecc3019123d` and retains both exact allowed origins plus the bounded map-policy marker. The repository safe suite passes every parser, pricing, Driver-link, API and Admin-AI step before stopping on the unchanged pre-existing `app/admin-sign-in/admin-sign-in-form.tsx:58` effect-state lint error; that unrelated Admin file is not modified or repaired in this Customer task.
- PR `#418` passed its Vercel checks and merged the exact source as `7ccea30323db141aa73684f3bb9bc461ee6d9239`. Vercel Production deployment `dpl_6vS77JxHRZYfqr33x1uPPeVPSEVL` is `READY` / `PROMOTED`, serves `app.prestigelimo.sg`, and reports that exact verified Git SHA. One paid frozen-credentials Store build only, EAS build `a4ac3fae-4154-431a-a10d-856638e575b5`, finished from that exact merge as Customer `0.1.0 (10)` with fingerprint `8eca174338a4fdc073a0b36ad9e3536c7a2410d5`. The downloaded signed IPA SHA-256 is `e7e423d66aa7f39cbec5ce604ec61dc29d4a2a4bb991f71bc23f07a1c4c949c0`; archive, app/display/bundle/version/build identity, arm64 iPhone-only family, iOS 16.4 minimum, light mode, production APNs, `get-task-allow=false`, exact Customer associated domain, Apple Team `U9Y2574Y7S`, frozen active profile `de9f56aa-c6b4-4d6f-b9ec-7e01c549d79d` and compiled in-app-map policy markers all passed direct inspection. The current Mac's deprecated colon-form entitlement decoder and local Apple certificate trust lookup produced the same warnings on already Apple-validated Build 9; the current decoder read Build 10's signed entitlements correctly and OpenSSL verified the embedded profile CMS.
- Exactly one EAS submission, `32675b3f-7d2c-489a-8ea6-5ac1e1c27030`, completed against existing App Store Connect app `6802691447`. Apple resolved exact Build 10 ID `d3dee321-a78f-45df-a33c-4fb3ce5b7d6f` as `VALID`, unexpired and ready for internal testing. The already-requested EAS group handoff did not attach the processed build, so no duplicate binary submission was made; signed-in App Store Connect instead added only exact Build 10 to existing internal `Owner Testing` group `c66d522b-c799-4134-be22-225a7192bf4a`. Visible readback proved the same group now has one tester and ten builds, Build 10 is `Testing`, and independent provider readback reports `IN_BETA_TESTING`. Build 10 remains only `READY_FOR_BETA_SUBMISSION` externally; no external-group assignment, tester/group creation or mutation, automatic future-build access, App Review or public release occurred. No real notification, booking/status, GPS, message, Customer wording, Driver/Admin app, Calendar, invoice, billing, payment, payout, PayNow, schema, environment or QA cleanup write was part of this release.
- Provider completion does not prove the physical map repair. The owner must install exact Customer Build 10 and use reusable QA booking `10906` to confirm `Track driver` keeps the map inside Prestige SG, fresh Driver movement updates the open panel, leaving and returning to Customer retains that panel under the opaque privacy cover, and top-level external navigation remains blocked. QA cleanup remains protected until that acceptance passes.

### Customer 30-Minute Tracking Gate And Driver Build 17 Repairs (source checkpoint 2026-08-27)

- Physical QA booking `10906` correctly showed the Customer tracking panel as waiting before its pickup window, but exact active runtime inspection proved the server did not enforce the intended eligibility time: it verified Customer ownership, service, OTW/OTS and fresh GPS but never selected `pickup_at` or compared it with the current time. The established runtime now reads the exact booking pickup after its existing authorization policy and returns no coordinate, status or latest-position read before the window opens. The exact boundary is eligible at `pickup_at - 30 minutes`; missing/invalid booking time or server time fails closed.
- The existing Customer tracking panel now maps that outside-window result to the exact light-mode message `Live driver tracking becomes available after your driver is on the way and within 30 minutes of pickup.` The existing pre-OTW, trip-closed, stale/fresh GPS, customer ownership, PA/Boss membership and privacy rules remain unchanged. The already-deployed Customer native wrapper keeps the same mounted WebView under its privacy cover, and the existing active `Track driver` state continues one eight-second refetch loop after foreground return; it does not start a second Driver tracking session. That source path requires no Customer Build 10, but the leave/return live-map behavior remains physical-acceptance pending.
- Driver Build 16 physically accepted OTW and three immediate GPS updates for the QA booking, then the position lease became stale while the phone was stationary/backgrounded. Exact native source and Expo location semantics proved both `distanceInterval: 20` and `deferredUpdatesDistance: 20` required movement before iOS delivered another background update, while the server lease expires after five minutes. The existing native location task, permissions, explicit OTW/Stop controls, API, booking token, stale threshold and error handling remain unchanged; only both movement thresholds are zero so stationary time-based system callbacks can refresh the existing lease.
- The Driver native privacy shell previously prompted Face ID on every ordinary return and unmounted its WebView while locked. The established app now uses one attempt-scoped lifecycle with the process monotonic clock: content is immediately covered when an enabled unlocked app leaves the foreground; a valid return before `180,000ms` reveals the same mounted WebView; `180,000ms` or later, invalid/backwards timing, an already-locked leave, cold launch, cancellation or failure requires exactly one Face ID attempt. Prompt-generated inactive/active transitions, concurrent/stale completions and repeated active events cannot bypass or duplicate the prompt.
- Customer alert/badge delivery, Driver alert/badge delivery, Driver link/ACK/status/messages, personal and Operations Calendar, Admin, invoice, billing, payment, payout, PayNow, Customer/Driver identity and all Production QA booking/link/event data remain unchanged. No new route, database write, timer, provider, notification, retry, session, button or background task is added.
- Fail-first protection is the strengthened `scripts/test-customer-live-location-map-runtime-guard.mjs`, `scripts/test-customer-live-location-app-side-runtime-path.mjs` and `scripts/test-customer-portal-driver-tracking-adapter.mjs`, plus new Driver timing guards. The Customer runtime test executes the actual mocked server path at just outside, exactly at and just inside the 30-minute boundary with no Production read. The Driver biometric test executes the `179999/180000/180001ms`, invalid/backwards clock, prompt-order, repeated-transition and concurrent-attempt matrix without sleeps.
- `scripts/test-driver-companion-background-location-runtime-guard.mjs` and `scripts/test-driver-biometric-single-flight-guard.mjs` are registered in `scripts/test-preactivation-verification-suite.mjs`.
- The pre-Build 17 release gate exposed two stale test-harness assumptions, not runtime defects. The established Driver account/device-lock guard still required the retired single-line Face ID enable state and foreground-resume refs even though the approved lifecycle repair now updates both the immediate render state and lifecycle ref and delegates foreground decisions to the executable lifecycle helper; that guard alone is narrowed to the new integration boundary and explicitly rejects both retired refs. The established Driver status persistence mock also omitted the already-imported native badge-count helper from its temporary fixture; that one source file is now copied into the same isolated harness. The native app, account/device binding, Face ID behavior, status persistence, Customer tracking, GPS, alerts, booking, Driver, Calendar, invoice and billing workflows are unchanged.
- Expo Doctor then proved the frozen Build 16 Driver dependency set (`expo 57.0.6` / React Native `0.86.0`) used the affected Hermes V1 patch and no longer matched ten supported SDK 57 patch versions. Only the isolated Driver package and lockfile advance to Expo `~57.0.17`, React Native `0.86.3`, and the exact compatible Expo module patches selected by `expo install --fix`; SDK 57, application source, identifiers and workflows remain unchanged. Post-alignment Expo Doctor passes all 21 checks. The production dependency audit has zero high or critical findings; ten inherited moderate Expo/Xcode build-tool advisories remain because npm's proposed automatic resolution would incorrectly downgrade Expo to 46, so no destructive audit fix is applied.
- A later owner-approved read-only Driver native-button audit correctly stopped when the older background-location foundation guard still required five retired pre-Build-17 package versions even though the executable stationary-background runtime guard passed and exact signed Build 17 already carried the intentional dependency alignment above. The guard alone now requires exact Build 17 `expo ~57.0.17`, `expo-location ~57.0.14`, `expo-secure-store ~57.0.2`, `expo-system-ui ~57.0.3` and `expo-task-manager ~57.0.14`. No package, lockfile, native/runtime source, location behavior, Face ID lifecycle, Driver workflow or Production state changes in this guard alignment.
- App Store Connect requires the next immutable Driver binary number, so only Driver `ios.buildNumber` advances from processed Build 16 to `17`. The app remains `Prestige SG Driver` / installed `Prestige Driver`, public iOS version `1.0.0`, light mode, bundle `sg.prestigelimo.drivercompanion`, EAS project `2a797181-d09d-4384-8d01-583456e83c3e`, App Store Connect app `6800706103`, and existing internal `Owner Testing` group `c0b20e3f-bb6b-45cc-8e31-c27519d8aa61`.
- Exact merged source `afa7ce6ea113146221d9fc0d6a4e316066049634` produced one paid Store build only: EAS build `2e04a122-c005-47ef-9aba-e11eb5c03dbf`, fingerprint `97e4dcc3893678949466cc12da09653032ae0684`, signed IPA SHA-256 `24e47f73455c5493376ae47e5017e4ae908477ed866c99911ac40c5b5a3ec34d`. The finished IPA passed archive, exact `1.0.0 (17)` bundle/display identity, arm64/iPhone/iPad, iOS 16.4 minimum, light-mode, background fetch/location, production APNs, `get-task-allow=false`, Apple Team and reused provisioning-profile inspection. Expo submission `e9efaf28-615c-4631-bd0d-92f4a3e4eb9d` completed once with no retry. App Store Connect readback then proved processing `VALID`, internal state `IN_BETA_TESTING`, exact Build 17 present in the existing one-tester `Owner Testing` group, and no external-testing group, App Review or public release. No Customer build, real notification, booking/status, Calendar, GPS, message, invoice, payment, payout, PayNow, schema or environment write was part of the release.
- Automated source, release and binary checks do not prove the physical behavior. Build 17 acceptance remains pending for stationary locked/background GPS freshness beyond five minutes, Customer 30-minute tracking and app-switch return, Driver 179/180-second Face ID boundary, and opaque privacy cover. Reusable QA booking `10906` and its exact test artifacts remain protected until that acceptance passes; cleanup is a separate exact-data action.

### Admin Generic Special Request Exact-Record Load Repair (source checkpoint 2026-08-26)

- Customer `/book` already validates and persists the optional multiline `customer_special_request` on both outbound and linked-return booking records. The exact Admin notification `Open request` already reloaded the full saved record and displayed the existing `Customer special request` row. Generic Admin Bookings `Open / Edit`, Dashboard quick-search and other standalone saved-booking opens instead converted the narrow `BookingRecord`, which intentionally excludes this Customer-only field, so those paths silently lost the saved request after load or reload.
- Generic Admin open now reuses the one established guarded exact `GET /api/admin-bookings?booking_reference=...` read already owned by notification Open request. An explicit full-record override or an exact full record already in memory remains first; otherwise one exact reference-validated read supplies the existing form loader before its unchanged narrow fallback. A monotonically increasing request revision prevents a slower earlier click from applying after a newer booking selection. Notification Open request now calls the same helper rather than duplicating that read contract.
- The shared `BookingRecord`, its narrow conversion, saved booking payload, Customer submission, linked-return writer, Driver surfaces, invoice, billing, payment, payout, PayNow, Operations Calendar and personal Calendar remain unchanged. Generic Open/Edit adds no save, update, Calendar write, Driver Job Link issue or external send; it only performs the existing guarded read when the full record is not already available.
- `scripts/test-customer-booking-special-request-guard.mjs` failed first on the missing exact-record generic loader and protects linked-return normalization plus the Customer/Driver privacy exclusions. `scripts/test-admin-booking-persistence-canonical-ui-browser.mjs` failed first with the exact multiline row absent, then proves one exact guarded read, the existing multiline label/value, and no additional booking, Calendar or Driver-link write across generic Open/Edit. The unchanged broad app-smoke currently stops earlier on its existing retained-edit-identity assertion (`LOADED-OPS-001` rather than blank); this exact-main baseline is unrelated to Special Request and was not modified in this repair. Physical Admin WebView acceptance remains untested at this source checkpoint.
- The broader `scripts/test-admin-load-bookings-crm-fallback-compact-guard.mjs` also stops identically on unchanged exact main because its olde…162152 tokens truncated…- Previous staging head before promotion: `17d65ab Move driver in-app button to driver dispatch`.
- New staging head after promotion: `5ce71cd Add Google Maps admin map provider support`.
- Staging root proof after promotion and after final rollback deployment: HTTP 200, title `Prestige Limo Ops`, with the existing Map Route Assist surface present.
- Google Maps evidence was run once through the guarded app routes only, using `scripts/run-admin-google-maps-read-only-verification.mjs`.
- Evidence approval env name used by the runner: `PRESTIGE_ADMIN_GOOGLE_MAPS_READ_ONLY_VERIFICATION_APPROVED`; no approval value or secret was recorded.
- Staging map gates were opened only for the bounded evidence window: `PRESTIGE_ADMIN_MAP_LOCATION_SEARCH_ENABLED=true`, `PRESTIGE_ADMIN_MAP_LOCATION_SEARCH_PROVIDER=google_maps_geocoding`, `PRESTIGE_ADMIN_MAP_ROUTE_ESTIMATES_ENABLED=true`, and `PRESTIGE_ADMIN_MAP_ROUTE_ESTIMATES_PROVIDER=google_maps_routes`.
- `PRESTIGE_GOOGLE_MAPS_API_KEY` was used server-side by the deployed staging app only; the key value was never printed, logged, committed, or exposed to the browser.
- A temporary admin/dispatcher session token was used only for the staging evidence deployments; the token value was never printed, logged, committed, or recorded, and the local temporary token files were deleted.
- Location search evidence used safe public-landmark scope only: Raffles Hotel Singapore.
- Location search returned normalized provider `google_maps_geocoding`, `found: 1`, and `result_count: 1`.
- Route estimate evidence used public landmarks only: RAFFLES HOTEL SINGAPORE to CHANGI AIRPORT TERMINAL 2.
- Route estimate returned normalized provider `google_maps_routes`, route type `drive`, distance `18890` meters, and duration `1211` seconds.
- Runner result: `google_maps_read_only_verification_passed`.
- Evidence proof: `customer_data_used: false`, `db_write: false`, `provider_send: false`, `raw_provider_payload_exposed: false`, `secrets_exposed: false`, and `google_maps_called_through_guarded_routes: true`.
- No real customer coordinates, customer contact data, booking private data, driver GPS, live-location data, raw Google payload, headers, API keys, tokens, debug/internal fields, pricing, payout, PayNow, payment/PDF/billing, `customer_rates`, `driver_payout_rules`, parser/debug fields, Save Booking internals, or `/api/admin-saved-bookings` internals were used or exposed.
- Rollback/disable proof after evidence: `PRESTIGE_ADMIN_MAP_LOCATION_SEARCH_ENABLED=false` and `PRESTIGE_ADMIN_MAP_ROUTE_ESTIMATES_ENABLED=false`, followed by a closed-gate staging deployment.
- Authenticated closed-gate proof after rollback: `GET /api/admin-map-location-search` returned HTTP 503 with `Admin map location search is not enabled on this server.`
- Authenticated closed-gate proof after rollback: `POST /api/admin-map-route-estimates` returned HTTP 503 with `Admin map route estimate is not enabled on this server.`
- Final post-evidence staging deployment was run without a temporary session-token override so staging returned to the saved project environment with the map gates closed.
- No database read/write, OneMap retry, driver GPS capture, live-location implementation, provider sends, Email/Resend/Telegram/WhatsApp/SMS sends, FlightAware call, auth activation, notification row write, OTS/photo/storage activation, calendar activation, scheduler/timer/polling/retry implementation, parser change, Save Booking change, `/api/admin-saved-bookings` change, pricing/rates/customer_rates change, `driver_payout_rules` change, payout/payment/PDF/billing/invoice activation, UI sector/card/button change, shim change, production deploy, or production activation occurred.
- Customer in-app notification send button remains not implemented and blocked separately pending customer auth/portal read path plus table/RLS proof; this Google Maps evidence did not change customer in-app notification runtime.

### OneMap Active Runtime Retirement Lock

- OneMap active runtime/provider paths are retired after Google Maps staging evidence completion.
- OneMap was parked after the safe HTTP 502 provider failure record.
- OneMap is no longer the active or fallback admin map provider.
- Google Maps remains the selected admin map/search/route provider.
- Current admin map routes must not call OneMap under any gate or provider configuration.
- `onemap_search` and `onemap_routing` provider values must fail closed as missing configuration with no provider call.
- The obsolete OneMap read-only evidence runner is removed so OneMap evidence cannot be retried accidentally from the repo.
- Admin map route-assist UI data attributes are provider-neutral `data-admin-map-*`; no new UI sector, card, or button is approved by this retirement lane.
- No OneMap retry, OneMap call, OneMap token setup, OneMap endpoint setup, env change, deploy, DB read/write, provider send, auth activation, billing activation, production activation, or customer data use is approved by this retirement lane.
- No Google Maps call was made in this retirement lane.
- No customer data, real customer coordinates, DB write, provider send, Email/Resend/Telegram/WhatsApp/SMS send, auth/session/cookie work, billing/payment/PDF/payout activation, parser change, Save Booking change, `/api/admin-saved-bookings` change, customer in-app runtime activation, shim change, env change, manual deploy, or production activation occurred.
- Future OneMap reintroduction requires separate owner approval, provider/token/endpoint readiness, a fresh contract guard, bounded staging evidence, and no secret exposure.

### Live location
- Live location setup foundation.
- Live location window policy setup foundation.
- `/api/admin-live-location-setup`.
- `/api/admin-live-location-window-policy-preview-readiness-setup`.
- `/api/admin-live-location-access-capture-disabled-setup`.
- Live location no-live guard.
- Setup-only. No real GPS/map/tracking.

### OTS photo proof
- OTS photo proof setup foundation with disabled upload/storage/viewer/customer access flags.
- `/api/admin-ots-photo-proof-setup`.
- `/api/admin-ots-photo-proof-preview-readiness-setup`.
- `/api/admin-ots-photo-proof-access-upload-disabled-setup`.
- OTS photo proof access/upload audit payload setup foundation.
- OTS photo proof no-live guard.
- Setup-only. No camera/upload/storage.

### Flight ETA / MNG Arrival
- Flight API setup foundation.
- `/api/admin-flight-api-setup`.
- `/api/driver-job/[token]/flight-eta-setup`.
- Driver flight ETA notification setup foundation.
- `/api/admin-driver-flight-eta-notification-setup`.
- Driver ETA acknowledgement setup foundation.
- `/api/driver-job/[token]/flight-eta-acknowledgement-setup`.
- Resend/escalation rule recorded: after 2 no-ack attempts, escalate admin to get replacement driver.
- Admin ETA escalation setup foundation.
- `/api/admin-driver-flight-eta-escalation-setup`.
- ETA reminder timing setup foundation.
- `/api/admin-driver-flight-eta-reminder-timing-setup`.
- ETA notification payload setup helper.
- `/api/admin-driver-flight-eta-notification-payload-setup`.
- Flight provider selection setup foundation.
- `/api/admin-flight-provider-selection-setup`.
- Future flight provider recorded: FlightAware AeroAPI, setup-only/disabled.
- Driver flight ETA live readiness setup foundation.
- Flight ETA result normalization setup foundation.
- Flight ETA comparison/update setup foundation.
- FlightAware/AeroAPI gated live lookup contract route/helper added:
  `POST /api/admin-flightaware-aeroapi-live-lookup-action` and
  `lib/admin-flightaware-aeroapi-live-lookup-action.ts`.
- FlightAware/AeroAPI live lookup gate remains closed by default:
  `PRESTIGE_FLIGHTAWARE_AEROAPI_LIVE_LOOKUP_ENABLED`.
- FlightAware/AeroAPI contract guards are registered in the preactivation suite and do not approve or run live lookup evidence.

## Not Live / Not Implemented

- Real flight provider activation.
- External flight API call.
- Provider token/env.
- Live ETA lookup.
- Real driver ETA notification sending.
- Real resend automation.
- Real admin escalation alert.
- Real Telegram/WhatsApp/email/SMS/push sending.
- Real customer driver-details link token issuance/access.
- Real GPS/live map.
- Real OTS photo upload/storage.
- Supabase Storage bucket/policies, admin viewer, customer visibility, and auth/live access.
- Customer/driver auth activation.
- Invoice PDF generation.
- Payment links.
- Invoice sending.
- Payout automation.
- Production auto-billing activation.
- Production deployment activation.
- Real customer amendment/cancellation booking writes, CRM updates, calendar sync/update/cancel, customer auth, notification sends, job-card creation, or live booking updates.
- Remaining risky shim cleanup for companies/travelers/rate_settings/full drivers.
- New product features without explicit owner approval.

## Current Flight ETA Rule

- MNG / Arrival only.
- Admin + driver only.
- Customer disabled by default.
- Future purpose: notify driver latest ETA 1 hour before pickup so driver does not miss arrival flight.
- If driver does not acknowledge, resend later.
- After 2 no-ack attempts, alert admin to get replacement driver.
- Current state is setup-only; no live sending or external API.

## FlightAware/AeroAPI Gated Live Lookup Contract Lock

- Approved contract route: `POST /api/admin-flightaware-aeroapi-live-lookup-action`.
- Approved server-only helper: `lib/admin-flightaware-aeroapi-live-lookup-action.ts`.
- Approved gate: `PRESTIGE_FLIGHTAWARE_AEROAPI_LIVE_LOOKUP_ENABLED`.
- Required env names are names-only and values must not be printed: `PRESTIGE_FLIGHTAWARE_AEROAPI_LIVE_LOOKUP_ENABLED`, `FLIGHTAWARE_AEROAPI_API_KEY`, and `FLIGHTAWARE_AEROAPI_BASE_URL`.
- The gate is closed by default. Closed gate returns safe HTTP 503 with `flight_lookup_gate_closed`, zero provider requests, no provider token read, no DB client, no scheduler, no retry, and no persistence.
- Missing provider token/base URL returns safe HTTP 503 with no provider request and no env value exposure.
- Invalid input returns safe HTTP 400. The allowed service scope is MNG/Arrival only.
- Success is allowed only after separate gate-opening approval and returns normalized fields only: provider, flight number, status, scheduled arrival ISO, estimated arrival ISO, source updated ISO, and `customerVisible: false`.
- Provider failure returns safe HTTP 502 or 504 with sanitized error code only; raw provider body, headers, token, debug/internal fields, mock archive, customer price, billing, payout, notes, and secrets remain excluded.
- The contract is manual admin/dispatcher-only. Public customer/driver routes and pages cannot trigger the lookup.
- The contract allows max one provider request per invocation, uses a strict timeout, and has no scheduler, cron, queue, polling loop, server retry, provider send, notification send, DB persistence, UI sector/button/card, parser change, Save Booking change, `/api/admin-saved-bookings` change, pricing/rates/payout/payment/PDF/billing change, auth/location/photo/calendar/OTS activation, or shim change.
- Guards added: `scripts/test-admin-flightaware-aeroapi-live-lookup-action-api-contract.mjs` and `scripts/test-flightaware-aeroapi-live-lookup-no-scheduler-guard.mjs`.
- This contract remains not-live until a separate owner-approved staging evidence pass names the staging target, opens the gate for one bounded live lookup window, records rollback/closed-gate proof, and confirms no forbidden surfaces were activated.

### Driver Job Calendar Add And Amendment Update

- The existing token-scoped Driver Job page now reveals one compact `Add / Update Calendar` action only after the driver has acknowledged the assigned job; no duplicate acknowledgement panel, booking lane, calendar provider lane, or driver link is added.
- The calendar handoff reuses the verified Driver Job Link token boundary and returns one private, no-store inline `text/calendar` response. The event contains only the booking reference, service, safe route/flight/pickup details, actual Singapore pickup time, and one display alarm at one hour before pickup.
- Driver calendar events never use the admin-only midnight display adjustment. A `00:00`–`03:00` pickup remains at its actual pickup date/time for the driver, while the established admin Google Calendar previous-night display rule and its two-hour/30-minute reminders remain unchanged.
- The event UID is stable by booking reference and the sequence changes with pickup schedule/route changes. Calendar clients vary in how imported `.ics` updates are handled, so the Driver Job page remains the source of truth and instructs the driver to use the same `Add / Update Calendar` action after an amendment.
- Visible staging evidence found that a schedule hash could decrease between imports, causing Mac Calendar to treat the amended file as older and ignore it. Calendar `SEQUENCE` now derives monotonically from the exact saved booking's safe `updated_at` revision; missing/legacy revision evidence falls back to zero without exposing internal fields.
- The verified production Driver Job read now prefers the current saved booking's safe operational schedule for the exact link booking reference, with the link's existing safe snapshot retained as a fail-closed fallback when the current safe read is unavailable. Driver-entered acknowledgement and vehicle details plus driver status history remain on their established persistence lanes.
- Owner-approved driver workflow requires the original Driver Job Link for the first open, `Save & Acknowledge Job`, and `Add / Update Calendar`. The phone calendar import then includes one standard calendar URL plus one visible `Open Driver Job` fallback link to the exact same token-scoped Driver Job page. Calendar clients that support event URLs may open it from the event/title; other clients can use the visible fallback link. Both entry points reuse the same status page, existing token validation, and existing status writer.
- Embedding the existing revocable/expiring Driver Job token in the downloaded event is an explicit owner-approved security tradeoff. The page and calendar event label the shortcut private and tell the driver not to share the event. Revoked, expired, invalid, or mismatched links continue to fail closed when opened. The token is not placed in the event title or booking text, and no second token, route, writer, reporting lane, or direct calendar status action is added. Customer price, billing, invoice/payment, payout comparisons, PayNow, finance/internal/admin notes, parser/debug internals, secrets, raw provider data, and mock QA/dev archive content remain excluded.
- The owner clarified that `Add / Update Calendar` must behave like a hospital-style calendar link: the driver opens the phone calendar import screen and confirms Add or Save, rather than the page creating a JavaScript blob and forcing an `.ics` attachment download. The established action is repaired in place as one direct same-origin link to the existing token-scoped calendar route, and that route now declares the same calendar payload inline. No second button, route, provider, event builder, or reporting lane is added.
- A later customer amendment continues through the established saved-booking schedule read. The driver opens the same Driver Job Link and uses the same calendar action again; the stable UID identifies the same job while the monotonic sequence marks the newest pickup date, time, route, and location revision. Calendar-client update handling still varies, so the phone's Add/Save confirmation and the Driver Job page remain the source of truth; the application does not claim a silent device-calendar write.
- The focused route guard verifies the inline calendar disposition, stable UID, higher amendment sequence, exact Singapore schedule, one-hour alarm, exact same-job URL and visible fallback shortcut, private-event warning, and privacy exclusions. The established Driver Job browser guard verifies that the acknowledged action exposes one exact token-scoped calendar URL without a `download` attribute, fetch/blob/object-URL path, duplicate action, or external provider navigation.
- No Google Calendar provider write, customer calendar, external send, Email, WhatsApp, SMS, Telegram, push notification, scheduler, migration, environment change, payment, invoice, payout, GPS/live-location write, OTS/photo change, CRM write, parser change, or deployment is introduced.
- Focused lock: `scripts/test-driver-job-calendar-download-guard.mjs`, alongside the existing Driver Job page, status-persistence, public-route privacy, and admin calendar guards.

## FlightAware/AeroAPI Commercial Activation Constraint Lock

- Owner has not approved the FlightAware company contract.
- Owner prefers usage/GET-style cost only and does not approve monthly/business service activation at this time.
- FlightAware AeroAPI business use may require Standard terms/monthly minimum, so the app must remain not-live until owner separately approves the company contract and cost model.
- Keep the internal app route as `POST /api/admin-flightaware-aeroapi-live-lookup-action`, not GET, because it is an admin action with authorization and request payload.
- The route must remain closed behind `PRESTIGE_FLIGHTAWARE_AEROAPI_LIVE_LOOKUP_ENABLED`.
- Future live evidence requires separate owner approval after company contract and cost approval.
- No live FlightAware/AeroAPI request, env change, token setup, provider credential read while the gate is closed, external request while the gate is closed, scheduler, polling, retry loop, customer-visible auto-refresh, monthly/business activation, deploy, DB write, provider send, Email/WhatsApp/SMS/Telegram, parser, Save Booking, `/api/admin-saved-bookings`, pricing/rates/payout/payment/PDF/billing, auth/location/photo/calendar/OTS, UI, or shim change is approved by this lock.
- This lock is guarded by `scripts/test-flightaware-aeroapi-commercial-activation-constraint-guard.mjs` and registered in `scripts/test-preactivation-verification-suite.mjs`.

### Public Booking Stale Portal Recovery And Return Extra Stops

- Production inspection of the owner-reported `/book` failure proved the exact customer booking POST reached `/api/customer-booking-requests` and returned HTTP 403 after the test-data cleanup had removed every `customer_access_accounts` row while the browser retained an earlier portal cookie. Read-only database evidence confirmed zero bookings in the failure window and zero customer-request bookings for that day, so neither an outbound nor return leg was partially created.
- The established `/book` request route still resolves verified PA identity before any persistence. When a retained portal session can no longer resolve a complete active identity, that request remains failed and creates no booking. The route returns HTTP 409, expires only the configured/default customer portal cookie names with `Path=/`, `Max-Age=0`, an expired date, `HttpOnly`, `Secure`, `SameSite=Lax`, and `Priority=High`, and instructs the customer to review and submit again.
- There is no automatic retry and no silent anonymous fallback for the failed request. A later manual Submit is a new public request after the obsolete portal access has been removed. Unsafe configured cookie names still fail closed with HTTP 403 and no expiry header.
- The existing `/book` form now includes one optional `Return extra stops` field inside its established return-trip section. It passes through the same customer-safe adapter and allowlisted request parser. Outbound `extraStops` populate only outbound route points; `returnExtraStops` populate only return route points and the established `extra_stop` service item on the linked return booking.
- `/my-bookings` remains a link to the canonical `/book` form. The focused return-trip guard was corrected to reject the retired duplicate portal form and now executes the parser to prove independent outbound and return stop ordering and quantities.
- Customer price, billing, invoice/payment/PDF, driver payout, PayNow, internal notes, parser/debug internals, admin finance, messaging, Google Maps, Google Calendar, CRM identity derivation, booking persistence wiring, environment gates, Supabase schema/configuration, provider sends, and Automation remain unchanged.
- Focused locks: `scripts/test-customer-booking-request-adapter.mjs`, `scripts/test-customer-booking-request-api-contract.mjs`, `scripts/test-customer-return-trip-request-guard.mjs`, `scripts/test-customer-booking-request-pa-identity-guard.mjs`, `scripts/test-public-customer-form-surface-boundary-guard.mjs`, and `scripts/test-public-api-session-cookie-cache-boundary-guard.mjs`.

### Update + Cal Refreshed Payload And Account-Label Consistency Repair

- Read-only production inspection reproduced the owner-reported amber Calendar status after refresh on the exact saved booking. The established status request carried the correct booking reference, schedule, route, flight, passenger, and booker, but its refreshed saved-booking adapter sent blank service and vehicle values plus the raw `admin_review_required` status while the existing Google Calendar event contained `DEP`, `AVF`, and `Admin Review Required`.
- The same inspection showed that the existing `Update + Cal` save formatter could wrap an already passenger-scoped company label again, changing `company [passenger]` to `company [passenger] [passenger]`. This was a display-label consistency fault; no verified company, booker, or traveler identity was inferred from that text.
- The established `buildSavedBookingCalendarEventPayload` lane now falls back from `booking_type` to the saved `service_type` or `route_type`, falls back from `vehicle` to `vehicle_type_or_category` or `vehicle_type`, and maps the known saved admin workflow statuses to the same display values already used by the Calendar write adapter. Unknown status values remain unchanged.
- The established traveler billing-account formatter now canonicalizes repeated trailing passenger brackets before applying the current passenger. This keeps repeated saves idempotent and allows the same company to be retained when the passenger changes without deriving verified identity IDs from names or labels.
- No second Calendar route, status lane, save path, CRM panel, button, or provider write was added. Customer booking persistence, verified identity selection, notification delivery, reminders, pricing, billing, payout, parser, and public/driver surfaces remain unchanged.
- Focused locks: `scripts/test-admin-booking-google-calendar-sync-api-contract.mjs` and `scripts/test-save-crm-billing-identity-review-guard.mjs`.

### Update + Cal Refresh Consistency Production Evidence

- Production release PR `#13` promoted exact staging head `eb4c5b7a` to `main` as `2a3520c1 Merge pull request #13 from Prestigelimosg/staging`. The PR was cleanly mergeable and contained only the four reviewed repair files.
- GitHub's Vercel status for exact merge commit `2a3520c1` changed from pending to success with `Deployment has completed`. No Vercel environment assignment, Supabase configuration, database schema, runtime gate, provider setting, or Automation state changed.
- Signed-in Production Chrome displayed exact build `2a3520c1`. Opening the existing Bookings tab issued one existing read-only `mode=status` Calendar comparison request; the exact linked return booking carried `DEP`, `AVF`, and `Admin Review Required`, and the safe response was `cal_saved`.
- The exact return-booking card displayed green `Cal saved`. After one normal Chrome reload and reopening Bookings, the same card again displayed green `Cal saved` automatically, with no amber `Update Cal` state and no browser logs.
- `Update + Cal`, Save + CRM, booking edit, Calendar write, CRM write, provider send, payment, invoice, payout, map, messaging, customer, and driver actions were not clicked or performed during acceptance.
- The saved company display label on this historical booking still contains the passenger suffix twice from the earlier Production save. The repair prevents a later save from appending another suffix but does not silently rewrite existing booking data; any historical label cleanup remains a separate explicit owner-approved data change.

### Driver Companion iPhone/Android Background-Location Foundation

- Owner approved one cross-platform Driver Companion foundation for iPhone and Android so a driver can keep the existing admin live map updated after the phone screen locks or the driver uses another app.
- The companion is a bounded Expo 57 native project under `driver-companion/`. It reuses the existing token-scoped `GET`, `POST`, and `DELETE /api/driver-job/[token]/live-location` contract plus the existing safe Driver Job GET. It does not add a route, table, writer, admin map, timer, customer lane, provider send, or Supabase key to the phone.
- The Companion accepts only the established exact HTTPS private Driver Job URL on cold start or while already open. Both entry paths reuse the existing production-origin and token-shaped-path validator, load the exact booking reference, passenger, pickup date/time, route, and status for human review, and replace the retired manual URL-paste field. A received link loads the safe job summary but never starts location sharing or requests location permission automatically; the driver must still tap the existing `Start trip tracking` action, and only one active job is permitted at a time.
- The Expo configuration declares only `applinks:app.prestigelimo.sg` for iOS and one Android auto-verified HTTPS intent filter limited to host `app.prestigelimo.sg` and path prefix `/driver-job/`. No Apple Team ID, Android signing fingerprint, store URL, domain-association file, signing credential, OAuth client, or provider setting is invented or changed. Actual verified App/Universal Link routing still requires separately approved production domain-association files with the owner-supplied exact platform identities and remains untested at this local source checkpoint.
- Expo SDK 57-compatible `expo-system-ui` keeps the generated native app in the existing light appearance on Android as well as iOS. The approved install added only that direct package plus its locked nested color-normalization dependency. npm reported 11 moderate and one high advisory, but the separately attempted online metadata audit was not authorized; whether any advisory is pre-existing or newly introduced remains unassessed, and no audit fix or unrelated dependency change is included.
- Local verification on 4 August 2026 used generated native projects only under `/private/tmp`. The current local source produced one unsigned iOS Simulator `BUILD SUCCEEDED` with Xcode 26.6 / iOS SDK 26.5 and one Android `:app:assembleDebug` `BUILD SUCCESSFUL` in 3m 34s with the Android SDK scoped to the command, keg-only Homebrew OpenJDK 17, and Expo's disposable temporary debug keystore. No Apple signing identity, permanent/Play signing credential, device installation, shell-profile edit, JDK symlink, live GPS, store action, deployment, Production write, or external contact was used.
- This changed build remains untested on a physical iPhone. Android warm private-link launch, explicit foreground/background permission, screen-lock updates, the persistent OS notification, and exact Stop cleanup are now physically verified on the owner's Pixel 6 Pro as recorded below. Current-build cold standalone launch, completed-job automatic stop, stale/offline presentation, zero-customer-visibility, the existing browser reporting fallback after verified association, Apple/Android production domain-association validation, store signing, review, publishing, and every iPhone behavior remain unverified.
- Booking/CRM persistence, Driver Job acknowledgement and OTW/OTS/POB/JC actions, Driver Reports, customer/driver messages, OTS photos, personal and Operations Calendar/OAuth, the browser Driver Job and Driver Portal fallback, existing live-location APIs/data, Admin and Customer apps, Supabase schema/data, invoice/billing/payment, driver payout/PayNow, provider, and every other wired lane are untouched.
- The private job token is stored with Expo SecureStore using `AFTER_FIRST_UNLOCK` and without biometric prompting so the native background task can read it after the phone has been unlocked once. The app does not log the token or coordinates and has no public environment variable, database credential, or direct database client.
- iOS enables the required location background mode and requests `Always`; the visible consent contract requires an iOS background indicator and Android persistent foreground-service notification while tracking is active. Android also enables precise/background location plus the location foreground service.
- The background task is defined once at bundle top level. It uses OS location callbacks rather than a polling timer, validates that the same private job remains active before each share, sends only the latest safe position in a delivered batch, and stops locally when the job is completed or the token becomes unauthorized, revoked, or expired.
- The explicit `Stop trip tracking` action stops the OS task first, calls the existing token-scoped DELETE once, and clears the secure active-job record. If the server marker cannot be cleared, the phone still stops and the UI truthfully says the existing admin map will show the last update as stale until cleanup.
- Driver status reporting, calendar, messages, driver details, OTS proof, and all other job actions remain on the existing Driver Job page; `Open Driver Job reporting` opens that same private page. Customer price, billing, invoice/payment, payout, PayNow, finance/internal/admin notes, parser/debug internals, and mock QA/dev archive content remain excluded.
- Expo SDK 57 configuration inspection produced the expected iOS bundle ID and Android package plus Android coarse/fine/background/foreground-service location permissions. At the earlier foundation checkpoint `npm run typecheck` passed, `npx expo-doctor` passed all 20 checks, and Metro produced clean temporary iOS and Android release bundles; the focused foundation guard protects the reuse/privacy boundaries. The 4 August 2026 final recheck passed typecheck and resolved configuration but Expo Doctor reported 19/20 because its current metadata expects five newer patch versions of the already-present Expo, Expo Dev Client, Location, Task Manager, and React Native packages. Those five versions were unchanged from `main`; they were not upgraded after the physical Pixel proof because doing so would produce a different untested native build. The current Expo dependency chain also reports a moderate build-tool-only `uuid` advisory through `xcode`; npm's offered forced fix would incorrectly downgrade Expo to 46, so no destructive or breaking automatic audit rewrite was applied.
- Android screen-off evidence now exists for the current local source on one physical Pixel 6 Pro. Full cross-platform acceptance still requires a physical iPhone test, and Android cold standalone launch, completed-job automatic stop, stale/offline presentation, zero-customer-visibility, and production domain association remain open. Source/type/build results alone must never be described as device proof.
- At the foundation-only code checkpoint, no native build had been distributed, no App Store or Play Console setting was changed, no live GPS was captured, no driver/customer was contacted, no live record was created or modified, no environment or runtime gate changed, and no Production web deployment was required.
- Owner created the Expo Organization `prestige-limo-ops` and authenticated the company build setup through their own browser session. The existing local companion was created/linked exactly once as `@prestige-limo-ops/prestige-driver-companion` with EAS project ID `2a797181-d09d-4384-8d01-583456e83c3e`; the app config is locked to that Organization and project so later builds cannot silently land in the personal Expo workspace. This ownership checkpoint created no phone build, signing credential, store submission, payment, live GPS session, database write, environment change, provider send, or customer/driver contact.
- Owner then explicitly approved generating the first Android signing credential and physical-phone test build. EAS generated and remotely stored one new Android keystore without exposing or downloading its values. Internal development build `6cb117cf-a67f-4223-b6b2-5fe975d0c56b` for Android package `sg.prestigelimo.drivercompanion`, app version `0.1.0` / build `1`, finished successfully from exact commit `046921bb` at 16:33 SGT on 17 July 2026. The private build page is `https://expo.dev/accounts/prestige-limo-ops/projects/prestige-driver-companion/builds/6cb117cf-a67f-4223-b6b2-5fe975d0c56b`; its temporary APK expires on 31 July 2026.
- The CLI emulator-install prompt was declined because this Mac has no Android emulator or connected device. The APK was not installed or run, no location permission was granted, no job token was entered, no live GPS or booking record was touched, and no store submission or Play Console change occurred. The successful cloud build proves only that Android native compilation/signing completed; the required physical Android screen-lock/background-location evidence remains open. No iOS build, Apple credential, certificate, provisioning profile, device registration, or App Store change was started.
- The owner installed that first development APK on a physical Pixel 6 and connected it to the local Metro server. The companion loaded without a JavaScript bundle error, the exact private test job `CUST-20260716021743-4IIAQ1-RET` passed the read-only `Check job` step, and tracking remained OFF until the owner explicitly approved temporary live-GPS writes for that exact test record. The screenshot exposed the private link token; the token is intentionally not repeated in this ledger and was not revoked or reused by Codex.
- The first approved Start attempt reproduced two real defects and was halted: the top `PRESTIGE LIMO` label collided with the Pixel status bar, and the Android location consumer crashed with `Requested job cannot be persisted without holding android.permission.RECEIVE_BOOT_COMPLETED permission`. The local development server was stopped immediately; the owner was instructed to force-stop the app and temporarily revoke its Location permission. No completed screen-lock/background evidence may be claimed from that failed attempt, and any first server marker must be treated only as stale/offline test residue until the normal Stop lane clears it.
- The bounded repair replaces React Native's deprecated `SafeAreaView` with Expo-compatible `react-native-safe-area-context`, a root `SafeAreaProvider`, and all-edge insets. It also declares `android.permission.RECEIVE_BOOT_COMPLETED` alongside the existing precise/background/foreground-service permissions so Expo TaskManager can persist the Android location job. These are repairs to the existing companion only; no route, table, writer, polling timer, map, customer lane, provider send, or second task was added.
- This repair requires a new signed Android APK because the permission changes the native manifest. Source checks or a Metro reload cannot prove it fixed the crash; the replacement APK must be built, installed on the Pixel 6, and rerun through Start, visible notification, background/lock, admin-marker, and Stop cleanup evidence before Android background tracking is considered verified.
- EAS replacement internal-development build `bcbf3c15-e377-41a2-822c-f5f132f1c3c1` finished successfully from exact repair commit `5ed59159` at 17:24 SGT on 17 July 2026 using the existing remotely stored Android keystore; no new signing credential was created. The private replacement page is `https://expo.dev/accounts/prestige-limo-ops/projects/prestige-driver-companion/builds/bcbf3c15-e377-41a2-822c-f5f132f1c3c1`, and its temporary APK expires on 31 July 2026. At that historical checkpoint the replacement APK had not been installed or rerun on the Pixel 6, and the old crashed installation's persisted task/token state still needed clearing before a retest.
- On 4 August 2026 the owner removed the previous Pixel installation and approved a fresh USB test of the current local source. Codex generated the Android project only under `/private/tmp`, built and installed a disposable debug APK with Expo's temporary debug keystore, and used a temporary development-only custom scheme only inside that disposable native copy so Metro could load. The repository app configuration was not given that scheme, no permanent signing credential was created or changed, and no store, Expo cloud build, deployment, Supabase configuration, schema, environment, provider, customer, or driver contact action occurred.
- The Pixel 6 Pro warm-link test used assigned booking `10871`; its private token is intentionally not recorded. Android's production-domain verification was unavailable, so the first WhatsApp tap opened the browser. The owner-approved per-device app-link selection then opened the Companion for the same link, proving the current warm-link handler but not automatic verified association or cold standalone release routing. The safe booking summary loaded with tracking OFF, and no permission prompt or GPS write occurred until the owner explicitly pressed `Start trip tracking`.
- Android granted precise foreground and background location for the exact test. With the screen power-button locked, ADB reported `mWakefulness=Dozing`; Expo's `LocationTaskService` remained an Android location-type foreground service with its persistent private notification for more than five minutes. The authenticated Production Live Dispatch map showed the exact `10871` marker and advanced its current-location time from 19:37 to 19:39 SGT while the screen remained off.
- The explicit red `Stop trip tracking` action returned the Companion to `TRACKING OFF`, removed foreground-service status, and cleared the server marker. Authenticated Live Dispatch then showed `0 live`, no `10871` marker image, and no current-location row for that booking. Booking status, acknowledgement, OTW/OTS/POB/JC, Calendar, CRM, booking details, Driver Reports, customer/driver messages, OTS proof, invoice/billing/payment, payout/PayNow, provider, schema, environment, and every other wired lane were not changed. The test did not verify iPhone behavior, cold standalone release routing, production domain association, completed-job automatic stop, stale/offline display, customer visibility, signing, review, or store publication.
- Focused lock: `scripts/test-driver-companion-background-location-foundation-guard.mjs`, registered in `scripts/test-preactivation-verification-suite.mjs`.

### Driver Companion iOS Universal Link Association

- The 12 August 2026 final release audit reproduced one exact Production blocker before any Apple identifier, App Store record, build, or submission write: `https://app.prestigelimo.sg/.well-known/apple-app-site-association` returned `404`, so iOS could not verify the Companion's existing `applinks:app.prestigelimo.sg` entitlement.
- The signed-in Apple identifier registration form confirmed the exact App ID prefix as `U9Y2574Y7S (Team ID)`. Combined with the already-established iOS bundle ID, the one authoritative application identifier is `U9Y2574Y7S.sg.prestigelimo.drivercompanion`; no prefix, bundle ID, domain, or path was inferred from a display name.
- The existing Production web app now serves one public, read-only JSON response at `/.well-known/apple-app-site-association`. It associates only `U9Y2574Y7S.sg.prestigelimo.drivercompanion` with `/driver-job/*`, uses no redirect, cookie, session, token, database, environment value, or writer, and leaves all other website paths in the browser.
- This source repair does not itself register the Apple App ID, create an App Store Connect app record, generate signing credentials, upload a build, submit for review, publish an app, or prove device behavior. After the endpoint is deployed and verified through Apple, physical iPhone verification remains required on the exact signed build before submission.
- Driver Job acknowledgement/reporting, live-location API/data, Driver Calendar, Operations Calendar, booking/CRM, Admin and Customer apps, messaging, invoice/billing/payment, payout/PayNow, provider, schema, migration, environment, and every established layout remain unchanged.
- Focused lock: `scripts/test-driver-companion-ios-universal-link-association-guard.mjs`, registered in `scripts/test-preactivation-verification-suite.mjs` alongside the existing Companion foundation guard.

### Driver Companion iOS Release Configuration

- The same final release audit proved that the Expo project had no declared iOS App Store icon, explicit iOS build number, or export-compliance flag. Those missing release inputs were repaired only in the established `driver-companion/app.json` configuration; no Companion screen, tracking action, API, or web workflow changed.
- `driver-companion/assets/icon.png` is a deterministic 1024-by-1024 RGB crop of the existing approved black-and-gold Prestige app mark. It contains the established gold `P` mark on the existing solid black background, has no alpha channel or pre-rounded corners, and does not introduce a generated or redesigned brand.
- `ios.version` is explicitly `1.0.0` for the first App Store release and `ios.buildNumber` is explicitly `1` for its first build. The shared root version remains `0.1.0` so the established Android release identity is unchanged; bundle ID remains `sg.prestigelimo.drivercompanion`, iPhone/iPad posture remains `supportsTablet: true`, and light mode remains locked.
- The Companion uses only the operating system's standard HTTPS transport and contains no custom or non-standard cryptography. `usesNonExemptEncryption: false` records that exempt posture in the generated iOS Info.plist; it does not bypass any Apple question that later requires the owner to confirm legal/export facts.
- This configuration does not prove a physical iPhone build, signing credential, background-location behavior, Universal Link routing, TestFlight installation, App Store review, or publication. All still require exact signed-build and device evidence.
- Android icon, version, signed build, verified Pixel behavior, Driver Job acknowledgement/reporting, live-location API/data, Calendar, messaging, booking/CRM, Admin and Customer apps, invoice/billing/payment, payout/PayNow, schema, provider, environment, and every established layout remain unchanged.
- Focused lock: `scripts/test-driver-companion-ios-release-config-guard.mjs`, registered in `scripts/test-preactivation-verification-suite.mjs`.

### Driver Companion iPad Support

- The owner-approved App Store-facing Driver name is `Prestige SG Driver`, while the shorter installed iPhone/iPad Home-screen label is `Prestige Driver` so it remains readable under the icon. The existing in-app `Driver Companion` heading remains so the operational role is explicit. This is a brand-name alignment of the same Driver Companion bundle and must not be interpreted as adding Customer or Admin functions to the native app; Customer remains on the established web/PWA surface and Admin remains on its separate private web/PWA surface.
- Owner approved extending the existing iOS Driver Companion from iPhone-only to iPhone and iPad. The established Expo configuration now declares `ios.supportsTablet: true`; the same bundle ID, App ID, Universal Link, private Driver Job URL parser, tracking actions, background-location task, light mode, and iOS app lane remain in place. No macOS target, second native role app, or second platform lane was added.
- The existing single scroll view remains the only Companion screen. Its content now stays fluid at narrow widths and centers inside a bounded 720-point column on wider iPad or variable-width iOS windows, without adding a tablet-only component, route, state, writer, or job workflow. Device-neutral visible copy now covers both iPhone and iPad while preserving the same explicit Start/Stop permission contract.
- Source/config validation does not prove physical iPad behavior, Universal Link launch, foreground/background location permission, screen-lock tracking, orientation handling, split-view resizing, App Store screenshots, signing, TestFlight installation, review, or publication. Those require an exact signed iPad-capable build and bounded physical iPad acceptance before submission.
- Disposable native verification on 12 August 2026 generated a clean iOS project from the bounded source, resolved `TARGETED_DEVICE_FAMILY = 1,2`, produced an unsigned Release iOS Simulator build for bundle `sg.prestigelimo.drivercompanion`, and confirmed the built `Info.plist` display name `Prestige SG` plus `UIDeviceFamily` values `1` and `2`. The exact build installed and launched on an iPad Pro 11-inch (M5) simulator running iOS 26.5.
- Visible light-mode acceptance on the owner's Mac passed in both portrait and landscape. Safe-area spacing remained intact, the bounded content column stayed centered and readable, and no clipping, full-width card stretch, Customer/Admin data, or role crossover was visible. The test did not open a private Driver Job Link, request location permission, start tracking, create GPS/Production writes, contact a driver/customer, or prove physical-device/background behavior.
- Android configuration and the physically verified Pixel behavior remain unchanged. Driver Job acknowledgement/reporting, live-location API/data, Driver Calendar, Operations Calendar, booking/CRM, Admin and Customer apps, messaging, invoice/billing/payment, payout/PayNow, provider, schema, environment, and every other wired lane remain untouched.
- Focused lock: the existing `scripts/test-driver-companion-ios-release-config-guard.mjs`, registered in `scripts/test-preactivation-verification-suite.mjs`.

### Driver App Store Name Alignment

- Signed-in, read-only App Store Connect inspection on 12 August 2026 confirmed one existing iOS record: `Prestige SG Driver`, Apple ID `6800706103`, version `1.0`, status `Prepare for Submission`. The record is the same established Driver Companion bundle; no second Driver app record was created.
- The inspection reproduced one exact release-preparation mismatch: the existing App Store record was `Prestige SG Driver` while `driver-companion/app.json` still declared the installed name `Prestige SG`. The focused release guard was first tightened to the exact App Store name and failed on that mismatch before the application configuration changed.
- The established Driver Companion configuration now declares `Prestige SG Driver`. Bundle ID `sg.prestigelimo.drivercompanion`, Apple App ID, build number, public version, iPhone/iPad support, Universal Link, location permissions, tracking workflow, APIs, privacy exclusions, and every Driver Job consumer remain unchanged.
- The owner found the full Store name too long beneath the Home-screen icon and approved `Prestige Driver` as the installed iPhone/iPad icon label. The established iOS configuration sets only `CFBundleDisplayName` to that shorter label; App Store Connect remains `Prestige SG Driver`, and the in-app role heading remains `Driver Companion`.
- The first visible name-alignment simulator check exposed the pre-existing App Store icon as a different cropped mark from the owner's intended artwork, so the release pass stopped before commit. The owner then supplied the exact authoritative square black-and-gold Prestige artwork. It was converted mechanically from its 640-by-640 JPEG source to a 1024-by-1024 RGB PNG with no alpha, redraw, generated replacement, transparency, or pre-rounded corners.
- `driver-companion/assets/icon.png` now contains that exact owner-supplied artwork. The focused release guard locks its SHA-256 fingerprint `b55f726603fbae4bf0c101fee8dc23e782089f6fcb7f963c2542c9fc297cb670` as well as the existing PNG, 1024-pixel, RGB, and no-alpha requirements so a different icon cannot silently pass later.
- A fresh unsigned Release simulator build from the resolved Expo/iOS configuration emitted `CFBundleDisplayName` `Prestige Driver`, bundle ID `sg.prestigelimo.drivercompanion`, and device families `1` and `2`. Visible light-mode checks on an iPhone 17 and iPad Pro 11-inch simulator showed the same `Driver Companion` screen and the exact black-and-gold icon with the readable `Prestige Driver` Home-screen label. The iPad SpringBoard initially displayed a loading placeholder while its icons populated; the compiled iPad icon asset was inspected as the correct artwork, and the normal icon rendered on the next settled read without a rebuild or source change.
- The future native Customer app name `Prestige SG` and future native Admin app name `Prestige SG Admin` are owner-parked. No Customer/Admin native project, App ID, App Store record, bundle, build, route, authentication lane, or role crossover is created by this Driver-only alignment.
- This source alignment does not upload a build, create or change signing credentials, start TestFlight, submit App Review, publish the app, request unlisted distribution, or prove physical iPhone/iPad behavior. Those remain separate exact-action approval and acceptance steps.
- Driver Job acknowledgement/reporting, live-location API/data, personal and Operations Calendar, booking/CRM, Customer and Admin web/PWA apps, messaging, invoice/billing/payment, payout/PayNow, provider, schema, environment, and every established layout remain untouched.
- Focused lock: `scripts/test-driver-companion-ios-release-config-guard.mjs`, registered in `scripts/test-preactivation-verification-suite.mjs`.

### Driver iOS App Store Version Alignment

- Signed-in, read-only App Store Connect inspection on 12 August 2026 reconfirmed the single `Prestige SG Driver` record as `iOS 1.0` with status `Prepare for Submission`. The exact version-detail page remained on the already-known Chrome-specific loading state, so no Apple control was clicked and no metadata, build, credential, TestFlight, review, or publication action occurred.
- The established resolved Expo configuration reproduced one exact release mismatch before upload: it had no iOS-specific public version, so iOS inherited the shared `0.1.0` while the prepared App Store record was the first `1.0` release. The focused release guard was first strengthened to require the canonical three-part iOS version `1.0.0` and failed with actual `undefined` before the application configuration changed.
- The same Driver Companion configuration now adds only `ios.version: "1.0.0"`, which Expo maps to iOS `CFBundleShortVersionString`. The shared root `version: "0.1.0"` remains unchanged for Android, while iOS build number `1`, bundle ID `sg.prestigelimo.drivercompanion`, App Store name, installed label, icon, iPhone/iPad support, Universal Link, location permissions, background tracking, and every established Driver Job consumer remain unchanged.
- Resolved Expo configuration and a clean disposable native prebuild produced iOS `CFBundleShortVersionString` `1.0.0`, build `1`, bundle ID `sg.prestigelimo.drivercompanion`, display label `Prestige Driver`, and device families `1` and `2`. The first compile attempt used a temporary symlink to the repository `node_modules` and failed inside ExpoModulesJSI because that harness path did not expose a Hermes header; no source or dependency was changed in response. Repeating the unchanged source in the previously proven fully isolated dependency harness completed one unsigned Release iOS Simulator build successfully. That exact build installed and opened in light mode on the iPhone 17 and iPad Pro 11-inch simulators with tracking off, no private Driver Job Link, no permission request, no GPS, and no Production write.
- This alignment does not create or upload a signed build, create or change Apple signing credentials or provisioning profiles, start TestFlight, select a build for version `1.0`, complete App Store metadata, submit App Review, or publish the app. Those remain separate exact-action approvals after resolved/native metadata verification.
- Customer/Admin native apps, Driver Job acknowledgement/reporting, live-location API/data, personal and Operations Calendar, booking/CRM, Customer and Admin web/PWA apps, messaging, invoice/billing/payment, payout/PayNow, provider, schema, environment, Production application/data, and every established layout remain untouched.
- Focused lock: `scripts/test-driver-companion-ios-release-config-guard.mjs`, registered in `scripts/test-preactivation-verification-suite.mjs`.

### Driver Live Location Guard Scope Alignment (2026-08-12)

- The pre-signing Driver Companion API audit stopped before any Apple credential, signed build, or upload because two established live-location guards rejected every timer anywhere in the production Driver Job page.
- Read-only tracing proved the only matched timer is the already-established visible `Driver App Updates` notification refresh. It calls only the token-scoped notification read while the page is visible; it does not request GPS, call the live-location route, use `sendBeacon`, start browser location sharing, or change Driver Job status.
- The two existing live-location guards are narrowed in place to exclude only that exact identifiable App Updates interval from their GPS timer scan. They still fail if any second interval, any timeout, any `sendBeacon`, or any timer appears elsewhere on the production Driver Job page or inside the live-location route/runtime helper.
- No Driver Job page, Companion source, message refresh, API, route, runtime helper, GPS behavior, Production data, Apple credential, signed build, or TestFlight record changes in this guard-only alignment. Customer/driver contact, Calendar, booking/CRM, invoice/billing/payment, payout/PayNow, provider, schema, environment, and established layouts remain untouched.

### Driver iOS TestFlight Submit Target (2026-08-12)

- The first approved signed iOS store build completed on EAS as build ID `50e61417-4ebe-47f0-9ee3-5028680ed5d8` from merged source `8be41c43203b5b5a42ed9d9fb434d02954decbb8`. The direct signed IPA inspection proved native version `1.0.0`, build `1`, bundle `sg.prestigelimo.drivercompanion`, display name `Prestige Driver`, iPhone/iPad device families `1` and `2`, background `location` and `fetch` modes, Team `U9Y2574Y7S`, and associated domain `applinks:app.prestigelimo.sg` before any App Store upload.
- The first non-interactive submit attempt stopped before upload because the existing `driver-companion/eas.json` had no production submit profile and therefore no exact App Store Connect target. No TestFlight build, tester assignment, App Review submission, publication, or metadata change occurred from that stopped attempt.
- The established production EAS submit profile targets only App Store Connect app `6800706103`, the already-verified `Prestige SG Driver` record. This numeric target configuration does not change or rebuild the signed IPA, add a tester/group, distribute a build, fill App Store metadata, submit App Review, publish, or create Customer/Admin native apps.
- Driver Job acknowledgement/reporting, live-location API/data, personal and Operations Calendar, booking/CRM, Customer and Admin web/PWA apps, messaging, invoice/billing/payment, payout/PayNow, provider, schema, environment, Production application/data, and every established layout remain untouched.

### Driver Companion Safe Summary Display Repair (2026-08-15)

- The first physical iPhone TestFlight acceptance of signed build `1.0.0 (1)` proved the established Universal Link opened the installed Companion and loaded exact booking `10889` with tracking still off and no automatic location permission or GPS write. Acceptance stopped at the first unexpected display result: the native summary printed canonical pickup key `2026-08-15T11:00` and unknown admin workflow value `admin_review_required` verbatim.
- Complete read-path tracing proved the token-scoped Driver Job API and shared safe payload were already returning the required schedule and terminal status evidence; the native `loadDriverJobSummary` boundary passed both strings straight to its one existing summary card. The repair stays inside that Companion read/display boundary: exact canonical timezone-less booking keys render as `15 Aug 2026, 1100hrs SGT`, known operational statuses retain bounded driver labels, and every unknown workflow value fails closed to `Pending dispatch confirmation` instead of trusting a server fallback label. The raw status remains internal to the Companion only for its existing completed-job tracking stop decision.
- Focused fail-then-pass protection is `scripts/test-driver-companion-safe-summary-display-guard.mjs`, registered beside the established Companion guards in the preactivation suite. It executes the real summary loader against the reproduced `10889` response shape and requires both safe displays while retaining the raw terminal-decision value.
- The mandatory staged application-change guard initially returned the unexpected message `No staged application change requires ledger enforcement.` because its application-prefix list predated the native `driver-companion/` project. Its existing prefix list now includes only that established native directory, so future Companion code changes require the same staged ledger and focused-guard pair as the Next application. `scripts/test-agent-startup-workflow-guard.mjs` fails if that native scope is removed again.
- This repair changes no Driver Job API, payload schema, link, acknowledgement/reporting page, status writer, live-location readiness/share/stop route, permission request, background task, Admin map, Calendar, Operations Calendar, booking/CRM, messaging, customer surface, invoice/billing/payment, payout/PayNow, provider, schema, environment, Production record, TestFlight group, tester, App Store metadata, or publication state. No GPS test resumed after the display failure. A new signed build and physical iPhone re-acceptance remain separate owner-approved actions after merge.

### Driver iOS TestFlight Build 2 (2026-08-15)

- The owner approved one replacement iOS TestFlight build to carry the already-merged Driver Companion safe-summary display repair into physical iPhone acceptance. The established `ios.buildNumber` is explicitly `2`; public iOS version `1.0.0`, bundle `sg.prestigelimo.drivercompanion`, App Store record `6800706103`, installed label `Prestige Driver`, iPhone/iPad support, light mode, icon, Universal Link, location permissions, and background tracking contract remain unchanged.
- The established production build and submit profiles remain the only release path and must reuse the existing Apple Distribution certificate, App Store provisioning profile, and App Store Connect API key. A prompt to create or replace any credential is outside this approval and must stop before mutation.
- This source checkpoint does not by itself prove a signed build, IPA metadata, App Store upload, Apple processing, TestFlight group assignment, physical iPhone display, or GPS behavior. Release acceptance requires direct inspection of the exact signed IPA as `1.0.0 (2)`, upload only to App Store Connect app `6800706103`, Apple processing to a TestFlight-ready state, manual assignment only to the existing `Owner Testing` internal group, installation, and a new physical iPhone check with tracking still off before any separately approved GPS start.
- PR `#243` merged the bounded build-number increment to `main` as `d917b97b24afb7366c92620e7de8d3edbd28f054` after both Vercel checks passed. EAS production build `55266e92-77d1-4f47-95b0-57164ac01003` then completed from that exact merge commit while reusing the existing active Apple Distribution certificate and App Store provisioning profile; no credential was created or replaced.
- A clean direct download of the signed IPA verified native version `1.0.0`, build `2`, bundle `sg.prestigelimo.drivercompanion`, installed label `Prestige Driver`, iPhone/iPad families `1` and `2`, background modes `fetch` and `location`, Team `U9Y2574Y7S`, associated domain `applinks:app.prestigelimo.sg`, a valid on-disk signature, and SHA-256 `38d49501e1a7a1e051d454581ad9b4ab794145edca34ac2299a76602bd91ba1e` before upload. An earlier inspection of the still-downloading local copy was discarded and never uploaded.
- EAS submission `f1d6557c-af8c-4b2a-9fb3-6fe178301317` finished successfully against exact App Store Connect app `6800706103`. Apple then reported build `1.0.0 (2)` as `VALID` and `READY_FOR_BETA_TESTING`; the owner gave action-time approval and the exact build was added only to the existing internal `Owner Testing` group. A separate App Store status read confirmed `IN_BETA_TESTING`. No tester, group, automatic future-build setting, App Review submission, or publication state was added or changed.
- No public App Review submission, publication, automatic future-build distribution, new tester, new group, Customer/Admin native app, Driver Job API, payload schema, acknowledgement/reporting, status writer, live-location route or task, permission behavior, Calendar, Operations Calendar, booking/CRM, messaging, customer surface, invoice/billing/payment, payout/PayNow, schema, environment, or Production record change is included.
- Focused lock: `scripts/test-driver-companion-ios-release-config-guard.mjs`, registered in `scripts/test-preactivation-verification-suite.mjs`.

### Driver iOS TestFlight Build 3 (2026-08-15)

- The owner approved one iOS-only internal TestFlight build carrying the merged complete in-app Driver workflow from `cc9c3f6fbc0feb92449e4a83dccd94cfb15f9294`. The established `ios.buildNumber` is explicitly `3`; public iOS version `1.0.0`, bundle `sg.prestigelimo.drivercompanion`, EAS project `2a797181-d09d-4384-8d01-583456e83c3e`, App Store Connect app `6800706103`, installed label `Prestige Driver`, iPhone/iPad support, light mode, icon, Universal Link, permissions, and background tracking contract remain unchanged.
- This build must reuse the existing Apple Distribution certificate, App Store provisioning profile, and App Store Connect API key. Any prompt to create, replace, rotate, download, or expose credentials is outside approval and must stop before mutation. Only one iOS production build and one submission are approved; queued or processing work must be monitored rather than retried or re-uploaded.
- This source checkpoint does not prove signed IPA contents, upload, Apple processing, TestFlight availability, physical-device behavior, or internal-group assignment. Direct IPA inspection and Apple processing evidence remain required. No Android build, tester/group change, App Review, public release, Customer/Admin native app, provider configuration, data/schema/runtime mutation, or unrelated source change is included.
- PR `#250` merged the bounded build-number checkpoint to `main` as `87a98820294afea8f89c4da3d6cda9a49086bc75`. Exactly one frozen-credential iOS production build, `67f53da1-b444-47c2-a941-013f07db680c`, completed from that exact merge while reusing the existing Apple Distribution certificate, active App Store provisioning profile, Team `U9Y2574Y7S`, and EAS-managed App Store Connect API key; no credential was created, replaced, rotated, downloaded, or exposed.
- Direct inspection of the exact signed IPA proved native version `1.0.0`, build `3`, bundle `sg.prestigelimo.drivercompanion`, installed label `Prestige Driver`, iPhone/iPad families `1` and `2`, light mode, OTS camera/photo explanations, background `fetch` and `location`, associated domain `applinks:app.prestigelimo.sg`, `get-task-allow` false, valid on-disk signature, and SHA-256 `1377c3f22c53fc45545e7c3a516d65dc4864c48540b64126afed6293ddec41c1`. The embedded bundle contains the verified native-app marker, native Calendar authorization start, and terminal tracking bridge. Generic generated Face ID and motion usage strings were byte-for-byte equal to Build 2 metadata and do not activate the parked account/Face ID workflow.
- Exactly one EAS submission, `4c31670b-a095-4dbd-954a-0aa5f145d577`, finished against App Store Connect app `6800706103`. Apple first reported build `1.0.0 (3)` as `VALID` and `READY_FOR_BETA_TESTING`, with external state `READY_FOR_BETA_SUBMISSION` and no expiry. Under the owner's later exact approval, read-only App Store Connect resolution found one unambiguous existing internal `Owner Testing` group, ID `c0b20e3f-bb6b-45cc-8e31-c27519d8aa61`, containing Builds 1 and 2, and one exact valid unexpired Build 3, ID `c5ed6207-e784-4428-949c-0404e4bc0bfd`. Only that build-to-group relationship was added; the same group then contained Builds 1, 2, and 3, Apple reported Build 3 `IN_BETA_TESTING`, and a count-only membership read confirmed the unchanged group still has one installed tester. No retry, re-upload, Android build, group creation/rename/deletion, tester membership change, external testing, feedback/email action, App Review, or public release occurred.
- Focused lock: `scripts/test-driver-companion-ios-release-config-guard.mjs`, registered in `scripts/test-preactivation-verification-suite.mjs`.

### Driver Native iOS Job Notifications (source checkpoint 2026-08-15)

- Physical iPhone Build 3 acceptance on exact test booking `10889` proved `Save & Acknowledge Job` persisted William, contact `87576969`, plate `SLT6969T`, and vehicle `AVF` while tracking remained off. The owner also physically observed that the existing Admin Operations Calendar event displayed the acknowledged plate. That is accepted evidence that the established acknowledgement-to-Operations-Calendar handoff succeeded; its writer, event identity, title, description, and best-effort boundary are locked and unchanged here.
- The same physical acknowledgement exposed one honest native gap: the embedded page intentionally suppressed browser Web Push, while Build 3 had no native notification module or APNs registration. Its browser fallback therefore reported that device alerts could not be enabled even though Messages & Updates remained available inside the embedded job. Native alert functionality was missing; this was not merely wording drift.
- The existing `driver_device_push_subscriptions` service-role-only table safely supports the iOS channel without a schema, migration, RLS, policy, or public API expansion. After exact active-link acknowledgement and verified-driver resolution, the existing token-scoped Driver Job PATCH may register an Expo push token with `source_surface = driver_native_ios`; malformed, unacknowledged, expired, revoked, or unverified link requests fail closed. Revocation uses the same exact verified link and driver boundary. The raw private Driver Job token is never stored in this subscription row.
- Existing saved `driver_app` App Updates remain the sole message/notification source and the existing push helper remains the sole delivery trigger. Browser Web Push subscriptions, VAPID configuration, service worker payloads, new-link browser alerting, Messages & Updates records, quick replies, and visible-browser acknowledgement wording remain unchanged. Native subscriptions never receive the pre-acknowledgement new-link payload because the app has no verified local job mapping at that stage.
- The iOS provider-visible notification is fixed to generic title `Prestige Driver`, body `Job update available`, and one opaque 64-hex job key. It contains no passenger, route, flight, contact, booking detail/reference, price, billing, invoice, payment, payout, PayNow, admin/internal note, parser/debug field, or raw private link token. The exact private job URL stays only on the device in SecureStore after successful acknowledged-link registration; tapping a notification opens the internal job only when the opaque key resolves to that verified local mapping. Missing or invalid mappings do not navigate.
- Prestige Driver uses the official Expo Notifications config plugin and EAS project identity already bound to `sg.prestigelimo.drivercompanion`. The embedded acknowledgement now asks the native shell to request iOS permission and register. It reports `Job alerts are enabled in Prestige Driver` only after permission, Expo token acquisition, server registration, and local mapping all succeed; denial or failure truthfully directs the driver to the same in-app Messages & Updates section. No browser notification API runs in embedded mode.
- Source verification passes the strengthened `scripts/test-driver-job-device-push-alert-guard.mjs`, which executes acknowledged-link native registration/revocation, rejects malformed tokens, proves the generic opaque payload, and proves native pre-ack new-link delivery is suppressed. The established complete embedded workflow, Driver Job browser, status persistence, acknowledgement-to-Admin sync, Operations Calendar, personal Calendar, admin-to-driver message, customer/driver notification, and public privacy guards also pass, alongside native TypeScript and the Next production build.
- This source checkpoint does not prove APNs credential readiness, EAS build output, signed IPA entitlements, Expo ticket acceptance, APNs device delivery, lock-screen display, notification tap routing, or TestFlight availability. Those require the separately approved Build 4 release checkpoint and physical iPhone acceptance. Expo push-ticket acceptance is not represented as device delivery. No test notification or Production row mutation is authorized before the post-install action-time checkpoint for booking `10889`.
- No Supabase data/configuration, schema, RLS, provider credential, Production notification, external contact, Calendar event/code, booking, status, acknowledgement, GPS, OTS photo, Report Issue, customer message, invoice, billing, payment, payout, PayNow, Admin app, Customer app, TestFlight group, tester, App Review, or public release changes in this source pass.

### Driver iOS TestFlight Build 4 (2026-08-15)

- The owner approved one iOS-only internal TestFlight Build 4 carrying the merged native iOS Driver job-alert source from `72a0c208b124ba99be07450e900ca7f708c17e78`. The established `ios.buildNumber` is explicitly `4`; public iOS version `1.0.0`, bundle `sg.prestigelimo.drivercompanion`, EAS project `2a797181-d09d-4384-8d01-583456e83c3e`, App Store Connect app `6800706103`, installed label `Prestige Driver`, iPhone/iPad support, light mode, icon, Universal Link, camera/photo explanations, and background-location contract remain unchanged.
- Read-only EAS credential inspection resolved the exact existing project, bundle, Team `U9Y2574Y7S`, active distribution certificate/profile, and existing App Store Connect key. The Push Notifications screen showed no APNs push key currently associated with this project and offered setup/add/use-existing actions. Inspection exited before selection and changed no credential. One exact APNs key setup/association is therefore required for real Build 4 notification delivery; any project, bundle, team, certificate, profile, or App Store target mismatch must stop before mutation.
- After this guarded build-number checkpoint merges, exactly one frozen-credential iOS production build is approved once the exact APNs association is ready, followed by direct signed-IPA inspection, exactly one submission to App Store Connect app `6800706103`, Apple processing, and assignment only to the existing internal `Owner Testing` group. No retry or re-upload is permitted while the same build/submission is queued or processing.
- Build 4 acceptance must distinguish EAS completion, signed IPA entitlements, Expo push-ticket acceptance, Apple/TestFlight processing, APNs device delivery, generic lock-screen presentation, and exact internal job tap routing. No Production test alert, acknowledgement replay, message write, or other booking `10889` mutation is authorized until Build 4 is installed and the owner gives the separate immediate action-time confirmation requested for that physical test.
- This checkpoint includes no Android build, external testing, group/tester membership change, App Review, public release, Customer/Admin native app, schema/migration, Supabase data/configuration, Vercel environment, Production notification, external contact, Calendar code/event, booking/status/ACK, GPS, OTS, Report Issue, messaging, invoice, billing, payment, payout, or PayNow action.
- Focused lock: `scripts/test-driver-companion-ios-release-config-guard.mjs`, registered in `scripts/test-preactivation-verification-suite.mjs`.

### Driver Build 4 Existing-ACK Native Alert Enrollment Repair (2026-08-15)

- After installing TestFlight Build 4, the owner physically reopened already-acknowledged booking `10889` without changing iPhone Settings or using status, message, Calendar, or tracking actions. The job opened inside Prestige Driver and still showed `Saved & Acknowledged`, but iOS displayed no notification permission prompt.
- Complete lifecycle tracing proved the native shell requests permission only after the embedded page posts the existing parameterless `native_notifications_register` bridge message. Before this repair, that post existed only in a successful fresh `Save & Acknowledge Job` callback. Booking `10889` had been acknowledged on Build 3, which had no native notification module; an acknowledged reload in Build 4 hydrated saved details and Calendar, disabled the unchanged acknowledgement button, and never invoked the bridge. Therefore no iOS permission request, Expo token registration, or SecureStore job mapping could begin. This was a page lifecycle defect, not evidence of an APNs entitlement, provisioning-profile, or provider failure.
- The established embedded Driver Job page now requests that same native registration exactly once per successfully loaded, acknowledged token. Fresh acknowledgement reuses the same helper, while an already-acknowledged reload receives the missing upgrade enrollment path. Exact loaded-token matching prevents a route transition from registering the wrong job. Ordinary Safari/Chrome continues using only the established browser Web Push lane; embedded mode invokes no browser Notification API or service-worker registration.
- The physical-equivalent browser regression first failed with zero bridge messages on the acknowledged embedded reload, then passed with exactly one parameterless registration message, zero browser permission requests, zero browser service-worker registrations, and zero repeated Driver Job PATCH. The strengthened source guard locks exact loaded-token, acknowledged-ready, and once-per-token boundaries.
- This is a live-web page repair consumed by the existing Build 4 WebView after Production deployment; it changes no native binary or build number and does not require Build 5. It does not prove that the owner has accepted the iOS permission prompt, that an Expo/APNs notification was delivered, or that notification tap routing works on the physical iPhone. Those remain explicit physical acceptance steps, and no Production alert/message/send action is authorized here.
- No acknowledgement, status, Calendar, Operations Calendar, message, GPS, OTS photo, Report Issue, booking, customer, Admin, invoice, billing, payment, payout, PayNow, schema/migration, Supabase data/configuration, provider credential/configuration, EAS build, TestFlight submission/group/tester, App Review, or public release lane changed. Focused protection is `scripts/test-driver-job-page-browser.mjs` and `scripts/test-driver-job-device-push-alert-guard.mjs`.

### Driver Build 4 SecureStore Native Alert Registration Repair (2026-08-15)

- The owner accepted iOS notification permission on the physical iPhone 15 running Prestige Driver Build 4, then sent one Admin-to-Driver message for acknowledged booking `10889` from the separate iPhone 16 Admin web/PWA while the receiver was locked and off-screen. No notification arrived. Read-only Production correlation found one exact matching Admin message and newest acknowledged Driver Job Link, but zero active `driver_native_ios` subscriptions at send time. The iPhone subscription had been created after permission and explicitly revoked only `791` milliseconds later; the same Admin send reached only two historical browser Web Push subscriptions. Expo/APNs was therefore never called for the iPhone in that test.
- Complete native lifecycle tracing proved the server registration succeeded before local finalization. The existing device-local job mapping used `prestige-driver-native-notification-job-v1:` as an Expo SecureStore key prefix, while the installed `expo-secure-store` contract permits only alphanumeric characters, `.`, `-`, and `_`. The colon made every acknowledged-job mapping write fail. The established native cleanup then correctly called the token-scoped unregister action, which exactly explains the immediate Production revocation. This was not an Admin messaging, outbox, APNs credential, provider-delivery, acknowledgement, or Calendar failure.
- The in-place repair changes only that invalid separator to `.`, retaining the same opaque 64-hex job key, exact private job URL value, SecureStore-only device boundary, token-scoped registration/unregistration API, and cleanup-on-genuine-local-failure behavior. No previously usable mapping requires migration because the former key was rejected before SecureStore could persist it.
- The strengthened `scripts/test-driver-job-device-push-alert-guard.mjs` executes the actual native mapping helper against an Expo-equivalent key validator. It first reproduced the invalid-key exception, then proves the derived mapping key is valid, the opaque key remembers and resolves the exact private Driver Job, invalid keys fail closed, notification-token remember/read/forget behavior remains intact, and a genuine local finalization failure still unregisters the server subscription before surfacing failure.
- Source guards cannot prove physical delivery. This native-binary repair requires the separately owner-approved Build 5 checkpoint, followed by owner install/reopen of acknowledged booking `10889`, read-only proof of one exact active native subscription, and a separately action-timed single Admin message test while the iPhone 15 is locked. No notification resend or Production data action occurs in this source repair.
- Admin messaging/outbox/push sender, browser Web Push, Messages & Updates, acknowledgement/status order, Operations and personal Calendar, GPS/background tracking, OTS photo, Report Issue, booking/CRM, customer/Admin apps, schema/RLS/data, provider configuration, invoice/billing/payment, payout/PayNow, and every role privacy boundary remain unchanged.

### Driver iOS TestFlight Build 5 (2026-08-15)

- The owner approved one iOS-only internal TestFlight Build 5 carrying the merged SecureStore native alert registration repair from Production source `6f78fbe165bff9ba5e22c6080da2da8b42d702d2`. The established `ios.buildNumber` is explicitly `5`; public iOS version `1.0.0`, bundle `sg.prestigelimo.drivercompanion`, EAS project `2a797181-d09d-4384-8d01-583456e83c3e`, App Store Connect app `6800706103`, installed label `Prestige Driver`, iPhone/iPad support, light mode, icon, Universal Link, notification permission, camera/photo explanations, and background-location contract remain unchanged.
- PR `#257` merged the guarded build-number checkpoint to `main` as exact source `291a1f758cee9c9536bc28f7ec2572bbc1da2e02`. Exactly one frozen-credential EAS iOS production build, `f8e9a45b-d861-497c-9926-b80e76554273`, completed from that exact merge while reusing Team `U9Y2574Y7S`, the existing Apple Distribution certificate ending `E4`, active App Store provisioning profile ending `N65`, associated Push Key ending `X4S`, EAS project, bundle, and App Store Connect key. No credential, profile, project, bundle, or target was created, replaced, rotated, downloaded, or changed.
- Direct inspection of that exact signed IPA proved SHA-256 `b2ce8a56c5b384d82324d15138d738d48423264cc5608d631922c4c454535650`, valid on-disk signature, native version `1.0.0`, build `5`, bundle `sg.prestigelimo.drivercompanion`, Team `U9Y2574Y7S`, production `aps-environment`, notification-capable embedded profile, associated domain `applinks:app.prestigelimo.sg`, background `fetch` and `location`, iPhone/iPad families `1` and `2`, and the bounded OTS camera/photo usage strings. The repaired dotted SecureStore mapping marker was present once, while the invalid colon marker was absent.
- Exactly one EAS submission, `7e721989-8462-4dd1-85fc-2e4c30481030`, finished against existing App Store Connect app `6800706103`. Apple resolved one exact unexpired Build 5, ID `c8b836d9-1783-41ef-a73d-6dca28e6fd2e`, as `VALID` and `READY_FOR_BETA_TESTING`. Read-only resolution found the one existing internal `Owner Testing` group, ID `c0b20e3f-bb6b-45cc-8e31-c27519d8aa61`, with automatic all-build access off, four prior builds, and one installed tester. Only Build 5's relationship was added; the group then contained five builds, the same tester and group settings remained unchanged, and Apple reported Build 5 `IN_BETA_TESTING` with external state still `READY_FOR_BETA_SUBMISSION`.
- Physical acceptance remains separate: after the owner installs Build 5 and reopens acknowledged booking `10889`, read-only evidence must first prove one exact active matching `driver_native_ios` subscription. No notification send or Production message/data action is authorized before the owner's later action-time confirmation. This checkpoint includes no Android build, external testing, App Review, public release, customer/Admin native app, provider credential/configuration change, schema/migration, Supabase data/configuration, booking/status/ACK, Calendar, GPS, OTS photo, Report Issue, messaging writer, invoice/billing/payment, payout, or PayNow change.
- Focused lock: `scripts/test-driver-companion-ios-release-config-guard.mjs`, registered in `scripts/test-preactivation-verification-suite.mjs`.

### Driver iOS TestFlight Build 6 (2026-08-15)

- The owner approved one iOS-only internal TestFlight Build 6 carrying the merged Driver embedded Calendar HTTPS callback association repair from Production source `afba5e3a357cd219a4a06ee2d9439d47af6567bb`. The established `ios.buildNumber` is explicitly `6`; public iOS version `1.0.0`, bundle `sg.prestigelimo.drivercompanion`, EAS project `2a797181-d09d-4384-8d01-583456e83c3e`, App Store Connect app `6800706103`, Team, certificate, APNs key, App Store Connect key, installed label, iPhone/iPad support, light mode, notification capability, camera/photo explanations, background location, and all Driver workflow writers remain unchanged.
- Build 6 must retain both exact same-host associated-domain values: `applinks:app.prestigelimo.sg` for the established Driver Job Universal Link and `webcredentials:app.prestigelimo.sg` for the verified Expo/iOS HTTPS Calendar auth-session callback. Any signing identity mismatch or requirement to regenerate or replace the active provisioning profile is a stop condition requiring fresh owner approval.
- This source checkpoint performs no EAS build, App Store upload, TestFlight group assignment, Calendar retry, notification send, Google/provider action, Production workflow/data write, Android action, external testing, App Review, or public release. Exactly one later EAS iOS production build and one submission are authorized only after this guarded source change merges and the existing signing identities remain valid.
- Focused lock: `scripts/test-driver-companion-ios-release-config-guard.mjs`, alongside the existing iOS association, Calendar, embedded workflow, privacy, acknowledgement/status, and GPS guards.

### Driver iOS TestFlight Build 7 (2026-08-16)

- The owner approved one iOS-only internal TestFlight Build 7 carrying the already-merged acknowledged-link Driver account enrollment, installed-app server sign-in, one-phone binding, and optional Face ID source. The established `ios.buildNumber` is explicitly `7`; public iOS version `1.0.0`, bundle `sg.prestigelimo.drivercompanion`, EAS project `2a797181-d09d-4384-8d01-583456e83c3e`, App Store Connect app `6800706103`, Team `U9Y2574Y7S`, installed label, iPhone/iPad support, light mode, notification capability, camera/photo explanations, background location, and existing Driver workflow writers remain unchanged.
- The Production Driver authentication gate remains absent/off during build and TestFlight submission. The browser Driver Job Link continues to require neither an account nor app installation, and this checkpoint creates no Driver enrollment, access account, authentication user, device binding, or Face ID enrollment. Build 7 retains both exact associated-domain values, `applinks:app.prestigelimo.sg` and `webcredentials:app.prestigelimo.sg`, plus the existing Calendar, notification, GPS, acknowledgement, reporting, and privacy boundaries.
- The preflight found the existing active signing lane unchanged: Distribution Certificate serial ending `E4` and provisioning profile ending `N65`, both expiring 12 August 2027; APNs key ending `X4S`; and App Store Connect API key `9A87PH98PH`. Any signing identity mismatch or request to regenerate or replace credentials is a stop condition requiring fresh owner approval.
- After this guarded source change merges, authorization is limited to exactly one frozen-credentials EAS iOS production build. The signed IPA must be inspected before exactly one submission to existing App Store Connect app `6800706103`; only after Apple processing may that exact build be added to the existing internal `Owner Testing` group. No Android or Google Play action, external testing, App Review, public release, Production authentication activation, account/data creation, provider configuration, Calendar write, notification send, booking/status/ACK, GPS, messaging, customer/Admin native app, invoice/billing/payment, payout, or PayNow action is included.
- Physical install and acceptance remain separate and untested: first approved sign-in, one-phone binding, Face ID enable/unlock, second-phone rejection, and immediate suspension/revocation enforcement each require later owner-controlled device testing while Production authentication is deliberately enabled for that bounded test.
- Focused lock: `scripts/test-driver-companion-ios-release-config-guard.mjs`, alongside `scripts/test-driver-account-device-lock-guard.mjs` and the existing iOS association, Calendar, embedded workflow, privacy, acknowledgement/status, notification, and GPS guards.
- PR #269 exact head `76f456220000a05b4727482418dc897eb554a329` passed both Vercel checks and merged as Production commit `fc53fd2f3bd3c9e8803494b8ae7e16d1fff6dff9`. Its exact Vercel Production deployment `dpl_5JizCiH4wEaZgAQPh3qFxCmPYpyE` reached `READY` and retained the `app.prestigelimo.sg` alias. A fully valid-shaped fake installed-app sign-in returned HTTP `503` with `not_configured`, proving the Production authentication gate remained closed before the native build.
- Exactly one frozen-credentials EAS iOS production build, `95f9fbaa-41c7-4862-9ae7-839f84ba637f`, finished from exact merge `fc53fd2f3bd3c9e8803494b8ae7e16d1fff6dff9` as Store-distribution Build 7. The unchanged active certificate serial ends `E4`, provisioning profile ID ends `N65`, both expire 12 August 2027, and Team remains `U9Y2574Y7S`. Direct inspection of that exact signed IPA proved SHA-256 `2c38c6a0e0bbc784b20d1d24b41d25a0a784989e59d30f0cc873e06b449d5765`, valid on-disk signature, native version `1.0.0`, build `7`, bundle `sg.prestigelimo.drivercompanion`, light mode, iPhone/iPad families `1` and `2`, background `fetch` and `location`, production push, both associated domains, bounded camera/photo/location/Face ID explanations, compiled installation and biometric markers, and no server-key-name marker.
- Exactly one EAS submission, `262d3754-338b-46c2-9ba4-6dabcdb5f9e0`, uploaded Build 7 only to existing App Store Connect app `6800706103`. Apple resolved one exact valid unexpired Build 7, ID `7ce05f76-a6e3-4282-861e-fee3c1f02e99`, as `READY_FOR_BETA_TESTING`. Read-only resolution proved the existing internal `Owner Testing` group remained ID `c0b20e3f-bb6b-45cc-8e31-c27519d8aa61`, with automatic all-build access off, Builds 1 through 6, and one installed tester. Only Build 7's relationship was added; the group then contained Builds 1 through 7, the same tester and settings remained unchanged, and Apple reported Build 7 `IN_BETA_TESTING` while external state remained `READY_FOR_BETA_SUBMISSION`.
- Closing Production proof again returned HTTP `503 not_configured` for a fully valid-shaped fake sign-in and found zero `driver_account_enrollments`, zero `driver_access_accounts`, and zero Supabase Auth users. No Driver account, device binding, Face ID enrollment, authentication activation, browser Job Link change, Android/Google Play action, external testing, App Review, public release, group/tester creation, contact, Calendar, notification, booking/status/ACK, GPS, messaging, customer/Admin app, invoice/billing/payment, payout, or PayNow action occurred. Physical Build 7 installation and the first bounded account/one-phone/Face ID/revoke acceptance remain untested and require the next separate owner-controlled step.

### Driver iOS TestFlight Build 8 (2026-08-16)

- The owner approved one iOS-only internal TestFlight Build 8 carrying only the repaired Apple-compatible Driver account Email/Password form after the Mac Chrome acceptance passes. The established `ios.buildNumber` is explicitly `8`; public iOS version `1.0.0`, bundle `sg.prestigelimo.drivercompanion`, EAS project `2a797181-d09d-4384-8d01-583456e83c3e`, App Store Connect app `6800706103`, Team `U9Y2574Y7S`, installed label, iPhone/iPad support, light mode, notification capability, camera/photo/location/Face ID explanations, background location, and every existing Driver workflow writer remain unchanged.
- The Production Driver authentication gate remains absent/off during source merge, build, submission, and Apple processing. Build 8 creates no Driver account, Auth user, device binding, Face ID marker, acknowledgement, report, Calendar event, message, notification, GPS record, customer/Admin app, invoice/payment/payout/PayNow action, Android/Google Play action, external test, App Review submission, or public release.
- Authorization is limited to the established frozen signing lane: one EAS iOS production build after the exact source merges, inspection of that exact signed IPA, one submission to existing App Store Connect app `6800706103`, and assignment of only that processed Build 8 to the existing internal `Owner Testing` group. Any certificate, provisioning profile, Team, bundle, project, App Store target, APNs key, or App Store Connect key mismatch or replacement request is a stop condition requiring fresh owner approval.
- Exactly one frozen-credentials EAS iOS production build, `2f58c2df-a12e-4b82-a207-4bb65b6d2a8d`, finished from exact merged source `a30022aa191c7b893962a5615dafab0c8eb2c40c` as Store-distribution Build 8. The established certificate serial ending `E4`, provisioning profile ID ending `N65`, Team `U9Y2574Y7S`, and App Store Connect key `9A87PH98PH` were reused without replacement. Direct inspection of that exact signed IPA proved SHA-256 `09f203c97cc2f4de0568be24e62074cd06e69518f08e58192fcc401612920ed0`, valid on-disk signature, native version `1.0.0`, build `8`, bundle `sg.prestigelimo.drivercompanion`, installed label `Prestige Driver`, light mode, iPhone/iPad families `1` and `2`, background `fetch` and `location`, production push, Team identity, `get-task-allow` false, and only the two established associated domains `applinks:app.prestigelimo.sg` and `webcredentials:app.prestigelimo.sg` before upload.
- Exactly one EAS submission, `a7713643-9226-431a-a333-f0b29d804143`, uploaded only that Build 8 to existing App Store Connect app `6800706103`. Apple resolved one exact valid unexpired Build 8, ID `dde2584b-c8f0-4ed9-9dc7-f8fac6ff4bf7`, as `READY_FOR_BETA_TESTING`. Read-only resolution found the one existing internal `Owner Testing` group, ID `c0b20e3f-bb6b-45cc-8e31-c27519d8aa61`, with automatic all-build access off and Builds 1 through 7. Only Build 8's relationship was added; a fresh read proved the same group then contained Builds 1 through 8 and Apple reported Build 8 `IN_BETA_TESTING`, while external state remained `READY_FOR_BETA_SUBMISSION`. No binary retry/re-upload, group creation/rename/deletion, tester setting or membership change, external testing, App Review, or public release occurred.
- Source, guard, Mac Chrome, signed-binary, and Apple-processing success cannot prove the physical iPhone defect is fixed. Build 8 must still be installed from TestFlight and the `@` key must insert directly into the same acknowledged Job Link Email field without invoking the unintended Password AutoFill Face ID flow before physical acceptance is claimed.
- Focused lock: `scripts/test-driver-companion-ios-release-config-guard.mjs`, `scripts/test-driver-account-device-lock-guard.mjs`, and the existing iOS association, complete embedded workflow, Calendar, notification, GPS, status, account privacy, and server-auth guards.

### Driver iOS TestFlight Build 9 (2026-08-16)

- The owner approved one iOS-only internal TestFlight Build 9 carrying only the narrow native Face ID foreground-resume loop repair after the visible Mac Chrome and focused native acceptance checks pass. The established `ios.buildNumber` is explicitly `9`; public iOS version `1.0.0`, bundle `sg.prestigelimo.drivercompanion`, EAS project `2a797181-d09d-4384-8d01-583456e83c3e`, App Store Connect app `6800706103`, Team `U9Y2574Y7S`, installed label, iPhone/iPad support, light mode, notification capability, camera/photo/location/Face ID explanations, background location, and every existing Driver workflow writer remain unchanged.
- Authorization is limited to the existing frozen-signing lane: one EAS iOS production build after the exact source merges, inspection of that exact signed IPA, one submission to existing App Store Connect app `6800706103`, and assignment of only that processed Build 9 to the existing internal `Owner Testing` group. Any certificate, provisioning profile, Team, bundle, project, App Store target, APNs key, or App Store Connect key mismatch or replacement request is a stop condition requiring fresh owner approval.
- PR #297 carried exactly the native resume-loop repair, its two focused guard updates, the Build 9 number, and this ledger checkpoint. Its Vercel check passed and it merged to `main` as `fb1caf99f0858aa8bc1a0ac39d9a740b944ea13a`. The exact merged worktree then passed the focused Driver account/device-lock guard, iOS release guard, native TypeScript check, complete embedded workflow boundaries, and a visible light-mode Mac Chrome Production Driver Portal load with zero console errors; that browser check intentionally performed no account, device, Face ID, Driver, Job Link, Calendar, message, notification, GPS, customer, invoice, payment, payout, or PayNow write.
- Exactly one frozen-credentials EAS iOS production build, `2371a253-1af1-4b9e-94ad-f427eb3f3a53`, finished from exact merge `fb1caf99f0858aa8bc1a0ac39d9a740b944ea13a` as Store-distribution Build 9. The established certificate serial ending `E4`, provisioning profile ID ending `N65`, Team `U9Y2574Y7S`, and App Store Connect key `9A87PH98PH` were reused without replacement. Direct inspection of the signed IPA proved SHA-256 `7a103b805e56f4b3ec70924b0e179e1c1ba6f304612744218328059e72894201`, valid on-disk signature, version `1.0.0`, build `9`, bundle `sg.prestigelimo.drivercompanion`, installed label `Prestige Driver`, light mode, iPhone/iPad families `1` and `2`, background `fetch` and `location`, Production push, Team identity, `get-task-allow` false, and only the established associated domains `applinks:app.prestigelimo.sg` and `webcredentials:app.prestigelimo.sg`.
- Exactly one EAS submission, `28907e65-4f9e-4577-a5b4-c36eba021848`, uploaded only that Build 9 to existing App Store Connect app `6800706103`. Apple resolved one exact valid unexpired Build 9, ID `7f4eb462-08da-42ec-b2c6-53c902856fb1`, as `READY_FOR_BETA_TESTING`. Read-only resolution found the one existing internal `Owner Testing` group, ID `c0b20e3f-bb6b-45cc-8e31-c27519d8aa61`, with automatic all-build access off, Builds 1 through 8, and one tester. Only Build 9's relationship was added; a fresh read proved the same group then contained Builds 1 through 9 and Apple reported Build 9 `IN_BETA_TESTING`, while external state remained `READY_FOR_BETA_SUBMISSION`. No binary retry/re-upload, group creation/rename/deletion, tester setting or membership change, external testing, App Review, or public release occurred.
- Source, guard, Mac Chrome, signed-binary, and Apple-processing success cannot prove the physical iPhone loop is fixed. Build 9 must still be installed from TestFlight, Face ID must open the Driver Portal without immediately starting another biometric cycle, and a genuine later app foreground must still require Face ID before physical acceptance is claimed. No account recreation is required.

### Driver iOS TestFlight Build 10 (2026-08-17)

- The owner approved one iOS-only internal TestFlight Build 10 carrying only the already-merged installed Driver Portal private-job reopen bridge from Production source `5fb04455693c35a427b65197229899b6ed5cafac`. The established `ios.buildNumber` is explicitly `10`, advancing only from `9`; public iOS version `1.0.0`, bundle `sg.prestigelimo.drivercompanion`, EAS project `2a797181-d09d-4384-8d01-583456e83c3e`, App Store Connect app `6800706103`, Team `U9Y2574Y7S`, installed label, iPhone/iPad support, light mode, notification capability, camera/photo/location/Face ID explanations, background location, and every existing Driver workflow writer remain unchanged.
- Authorization is limited to the existing frozen-signing lane: one EAS iOS Production build after this exact release checkpoint merges, inspection of that exact signed IPA, one submission to existing App Store Connect app `6800706103`, and assignment of only that processed Build 10 to the existing internal `Owner Testing` group. Any certificate, provisioning profile, Team, bundle, project, App Store target, APNs key, or App Store Connect key mismatch or replacement request is a stop condition requiring fresh owner approval.
- The binary must preserve the strict installed-app bridge already merged and guarded: only the exact installed `/driver-portal` may request one opaque 64-hex job key; the native wrapper may resolve only its encrypted local SecureStore mapping and reuse the established private-job navigation. No raw private token enters the portal jobs API or browser storage. Missing mappings and invalid, expired, revoked, completed, or unrelated links remain fail closed. Browser Job Links, acknowledgement/reporting, Calendar, messages, alerts, GPS, OTS photo, Report Issue, Driver Database/Delete + Revoke, customer/Admin surfaces, invoice/payment/payout/PayNow, schema/data, environment, Android/Google Play, external testing, App Review, and public release remain unchanged.
- Source, focused guards, Mac Chrome, signed-binary, and Apple-processing success cannot prove physical iPhone reopening. After Build 10 is installed, the owner must open the latest valid private dispatch link once in Prestige Driver on that same phone, return to Driver Portal, and confirm the existing `Open Driver Job` action reopens the exact private reporting page inside the app. No account recreation is required.
- PR #300 carried only the Build 10 number, its focused release-config guard update, and this release checkpoint. Its Vercel check passed and it merged to `main` as exact source `e05a7a33113b3249be94292775f1e92215402a7b`. The exact release source passed the Driver release, native private-job-open, internal acknowledgement/status, account/device-lock, and iOS universal-link guards; Driver Companion TypeScript; the Next Production build; the staged-app-change guard; and a visible light-mode owner Mac Chrome Production Driver Portal check with zero console errors and no account, job, Driver, Calendar, message, notification, GPS, customer, invoice, payment, payout, or PayNow mutation.
- Exactly one frozen-credentials EAS iOS Production build, `6c03ae91-b235-4488-adad-0a431972fa30`, finished from exact merge `e05a7a33113b3249be94292775f1e92215402a7b` as Store-distribution Build 10. The established certificate serial ending `E4`, provisioning profile portal ID ending `N65`, Team `U9Y2574Y7S`, and App Store Connect key `9A87PH98PH` were reused without replacement. Direct inspection of the exact signed IPA proved SHA-256 `e1a8e0f8a6408153e7c380eff077ba63b8c0e93f5a58f7371c46f81ce64910b8`, archive integrity, version `1.0.0`, build `10`, bundle `sg.prestigelimo.drivercompanion`, installed label `Prestige Driver`, light mode, iPhone/iPad families `1` and `2`, `arm64`, background `fetch` and `location`, Production push, Team identity, `get-task-allow` false, and only the two established associated domains `applinks:app.prestigelimo.sg` and `webcredentials:app.prestigelimo.sg`.
- Exactly one EAS submission, `7fa24f97-7ce2-44d7-b419-e8ed381274ef`, uploaded only that inspected Build 10 to existing App Store Connect app `6800706103`. Apple resolved one exact valid unexpired Build 10, ID `b8e395fa-ae8c-4b0c-a937-bf0c9b6a844b`, as `READY_FOR_BETA_TESTING`. Read-only resolution found the one existing internal `Owner Testing` group, ID `c0b20e3f-bb6b-45cc-8e31-c27519d8aa61`, with automatic all-build access off, Builds 1 through 9, and one tester. Only Build 10's relationship was added; a fresh read proved the same group then contained Builds 1 through 10 and Apple reported Build 10 `IN_BETA_TESTING`, while external state remained `READY_FOR_BETA_SUBMISSION`. No binary retry/re-upload, group or tester creation/rename/deletion, tester setting or membership change, external testing, App Review, or public release occurred.

### Driver iOS TestFlight Build 11 (2026-08-17)

- The owner approved one iOS-only internal TestFlight Build 11 carrying only the merged persisted Driver Face ID setup-state repair from Production source `ecf9ef7809112daf1b58319756b07e71d4b2cfb6`. The established `ios.buildNumber` is explicitly `11`, advancing only from `10`; public iOS version `1.0.0`, bundle `sg.prestigelimo.drivercompanion`, EAS project `2a797181-d09d-4384-8d01-583456e83c3e`, App Store Connect app `6800706103`, Team `U9Y2574Y7S`, installed label, iPhone/iPad support, light mode, notification capability, camera/photo/location/Face ID explanations, background location, and every existing Driver workflow writer remain unchanged.
- Authorization is limited to the existing frozen-signing lane: one EAS iOS Production build after this exact release checkpoint merges, inspection of that exact signed IPA, one submission to existing App Store Connect app `6800706103`, and assignment of only that processed Build 11 to the existing internal `Owner Testing` group. Any certificate, provisioning profile, Team, bundle, project, App Store target, APNs key, or App Store Connect key mismatch or replacement request is a stop condition requiring fresh owner approval.
- Build 11 preserves the device-only SecureStore Face ID marker and sends only its immutable enabled/disabled boolean through the existing native bootstrap. The existing green Face ID setup panel disappears immediately after successful enable and remains absent after portal reload or later app relaunch; Face ID authentication itself, one-phone binding, account/session checks, private Job Link reopening, Job Alerts, notifications, acknowledgement/reporting, messages, GPS, OTS photo, Report Issue, Driver Database/Delete + Revoke, Calendar, customer/Admin surfaces, invoice/payment/payout/PayNow, schema/data, environment, Android/Google Play, external testing, App Review, and public release remain unchanged.
- Source, focused guards, visible Mac Chrome, signed-binary, and Apple-processing success cannot prove physical iPhone persistence. After Build 11 is installed, the owner must enable Face ID once, confirm the green setup panel disappears immediately, close and reopen Prestige Driver, pass Face ID, and confirm that the setup panel remains absent while the Driver Portal and jobs still load. No account recreation is required.
- PR #305 carried only the Build 11 number, its focused release-config guard update, and this release checkpoint. Its Vercel check passed and it merged to `main` as exact source `2fa3bf983b0e25093c855e678218a0b243f926f8`. The exact merged worktree passed the Driver release guard and native TypeScript check without changing installed-app behavior outside the already-merged Face ID setup-state repair.
- Exactly one frozen-credentials EAS iOS Production build, `ad86ccd7-5b8f-4435-85aa-4ea15d5fbe31`, finished from exact merge `2fa3bf983b0e25093c855e678218a0b243f926f8` as Store-distribution Build 11. The established Team `U9Y2574Y7S`, certificate serial ending `E4`, provisioning profile portal ID ending `N65`, and App Store Connect key `9A87PH98PH` were reused without replacement. Direct inspection of that exact IPA proved SHA-256 `b97e106196b76ca5ba30e1811fa3e3baa7053bd8f7403ae6b2eb650cb9ee8fe6`, archive integrity, version `1.0.0`, build `11`, bundle `sg.prestigelimo.drivercompanion`, installed label `Prestige Driver`, light mode, iPhone/iPad families `1` and `2`, `arm64`, background `fetch` and `location`, Production push, Team identity, `get-task-allow` false, only the two established associated domains, and the compiled immutable Face ID enabled-state marker. The embedded provisioning profile was byte-identical to accepted Build 10 and remained valid until 12 August 2027; the local Mac trust-chain command produced the same non-trusted-root warning as accepted Build 10, so no broader signature claim is made from that command.
- Exactly one EAS submission, `d2ccdb5d-298f-44e6-b244-2fca96e83ab9`, uploaded only that inspected Build 11 to existing App Store Connect app `6800706103`. Apple successfully processed `1.0.0 - 11`, and the same submission distributed it to the one explicitly selected existing internal `Owner Testing` group with automatic TestFlight setup disabled. No binary retry or duplicate upload, group/tester creation or change, external testing, App Review, or public release occurred.
- Existing owner-confirmed physical iPhone TestFlight evidence already proves that live GPS continued while the phone was locked and that Pickup Risk worked on that iPhone. Build 11 did not change Expo Location, background tracking, or Pickup Risk. That physical evidence must not be incorrectly reported as Android-only or untested iPhone behavior; the new physical acceptance required for Build 11 is limited to the persisted Face ID setup-panel state described above.

### Driver iOS TestFlight Build 12 (2026-08-17)

- The owner approved one iOS-only internal TestFlight Build 12 carrying only the bounded Installed Driver Portal native Job Alerts repair described above. The established `ios.buildNumber` is explicitly `12`, advancing only from `11`; public iOS version `1.0.0`, bundle `sg.prestigelimo.drivercompanion`, EAS project `2a797181-d09d-4384-8d01-583456e83c3e`, App Store Connect app `6800706103`, Team `U9Y2574Y7S`, installed label, iPhone/iPad support, light mode, existing notification capability, camera/photo/location/Face ID explanations, background location, and every existing Driver workflow writer remain unchanged.
- Authorization is limited to the established frozen-signing lane after this exact application and release checkpoint merges: one EAS iOS Production build, inspection of that exact signed IPA, one submission to existing App Store Connect app `6800706103`, and assignment of only processed Build 12 to the existing internal `Owner Testing` group. Any certificate, provisioning profile, Team, bundle, project, App Store target, APNs key, App Store Connect key, build source, or signed-IPA mismatch or replacement request is a stop condition requiring fresh owner approval.
- PR #308 carried only the existing Driver Portal Job Alerts action's native Expo Notifications bridge repair, executable browser/native regression guards, Build 12 number, and this implementation checkpoint. It merged to `main` as exact source `0867529f039db59eca9dd52de6dcea7f141f7146`; Vercel Production deployment `dpl_B9wvk7U7qyXn4BbnVrdhPGmY6p2T` reached `READY` for `app.prestigelimo.sg`. The exact merged source passed the strengthened native Job Alerts guard, Driver account/device-lock guard, internal acknowledgement/status guard, Driver Portal jobs guard, iOS universal-link guard, live-location eligibility guard, both Driver privacy guards, release-config guard, root and native TypeScript, targeted lint, the Next Production build, staged-app-change guard, and two visible light-mode Mac Chrome Driver Job/Portal runs. The browser guard now presses the real Portal action, proves the exact native request and success response, reloads with persisted native enabled state, rejects invalid job keys and browser-storage duplication, and separately preserves the browser/PWA push lane; it no longer passes merely because registration code exists.
- Exactly one frozen-credentials EAS iOS Production build, `fff06041-79cc-4eda-be28-f1c9012ff97b`, finished from exact merge `0867529f039db59eca9dd52de6dcea7f141f7146` as Store-distribution Build 12. The established Team `U9Y2574Y7S`, certificate serial ending `E4`, provisioning profile portal ID ending `N65`, and App Store Connect key `9A87PH98PH` were reused without replacement. Direct inspection of that exact IPA proved SHA-256 `0bc2ba16da8d468aca5e166dd1ff53a482918d0197864d4d6958e607c9567f95`, archive integrity, version `1.0.0`, build `12`, bundle `sg.prestigelimo.drivercompanion`, installed label `Prestige Driver`, light mode, iPhone/iPad families `1` and `2`, `arm64`, background `fetch` and `location`, Production push, Team identity, `get-task-allow` false, only the two established associated domains, and the compiled immutable native-notification enabled-state and registration markers. The embedded provisioning profile was byte-identical to accepted Builds 10 and 11 and remains valid until 12 August 2027. The local Mac trust-chain command produced the same non-trusted-root warning as accepted Build 11; raw signed entitlements and the embedded profile decoded successfully, so no broader local trust-chain claim is made.
- Exactly one EAS submission, `44bb2e63-f485-4d88-a57a-b881f152b415`, uploaded only that inspected Build 12 to existing App Store Connect app `6800706103`. Apple successfully processed `1.0.0 - 12`, and the same submission distributed it to the one explicitly selected existing internal `Owner Testing` group with automatic TestFlight setup disabled. No binary retry or duplicate upload, group/tester creation or change, external testing, App Review, or public release occurred.
- Source, focused guards, visible light-mode Mac Chrome, signed-binary inspection, and Apple processing cannot prove physical push delivery. After Build 12 is installed, the signed-in Driver must press the existing `Enable Job Alerts` action once and allow iOS notifications if prompted. The action must change to `Job Alerts Enabled` and remain enabled after closing and reopening Prestige Driver. Admin must then issue one fresh test Driver Job to that exact Driver; the owner must confirm its alert appears while the app is backgrounded or the phone is locked and that tapping it opens the intended private job. If the app reports that its encrypted job mapping is absent, the Driver must open the latest acknowledged private dispatch link once inside Prestige Driver and retry. Apple APNs, Expo, network connectivity, Focus settings, and iOS background policy mean no honest implementation can guarantee an alert at every instant.
- Build 12 does not change or retest the already owner-confirmed physical iPhone live-location/Pickup Risk evidence. Face ID, one-phone binding, account/session, private-job reopening, acknowledgement and pre-ack safe-detail editing, post-ack lock, reporting/status/messages, OTS proof, Report Issue, Driver Database/Delete + Revoke, personal and Operations Calendar, Pending ACK Queue, customer/Admin surfaces, invoice/payment/payout/PayNow, schema/data, environment, Android/Google Play, external testing, App Review, and public release remain outside this bounded release and unchanged.

### Driver iOS TestFlight Build 13 (2026-08-17)

- The owner approved one iOS-only internal TestFlight Build 13 carrying only the merged native Admin-message notification destination repair from exact application merge `94e64ea215a39acfd2a04e3281bd899919494392`. The established `ios.buildNumber` is explicitly `13`, advancing only from `12`; public iOS version `1.0.0`, bundle `sg.prestigelimo.drivercompanion`, EAS project `2a797181-d09d-4384-8d01-583456e83c3e`, App Store Connect app `6800706103`, Team `U9Y2574Y7S`, installed label, iPhone/iPad support, light mode, notification capability, camera/photo/location/Face ID explanations, background location, and every existing Driver workflow writer remain unchanged.
- Authorization is limited to the established frozen-signing lane after this exact release checkpoint merges: one EAS iOS Production build, inspection of that exact signed IPA, one submission to existing App Store Connect app `6800706103`, and assignment of only processed Build 13 to the existing internal `Owner Testing` group. Any certificate, provisioning profile, Team, bundle, project, App Store target, APNs key, App Store Connect key, build source, signed-IPA, or Owner Testing target mismatch or replacement request is a stop condition requiring fresh owner approval.
- Only an Admin-to-Driver message notification may carry the fixed safe native destination `messages`. A tap resolves the existing opaque 64-hex job key through the encrypted device mapping, opens the exact private job, and moves to the existing `Messages & Updates` section. No private token, booking reference, message text, passenger, route, contact, customer, price, billing, invoice, payment, payout, PayNow, Admin note, parser/debug content, GPS, photo, or mock/archive data enters the push payload or native bootstrap.
- Generic Driver app updates, newly issued jobs, and the automatic one-hour-before-pickup reminder keep opening the normal exact job and never target Messages. The reminder retains the established fixed copy, verified-driver assignment gate, exact 60-to-61-minute scheduler window, existing Expo/APNs delivery lane, and privacy exclusions. Apple APNs, Expo, device connectivity, Focus settings, and iOS background policy can delay or suppress delivery; this build does not claim delivery at every instant.
- PR #311 carried only the explicit Build 13 release checkpoint and its focused release-config protection. It merged to `main` as exact source `13d2598347057b719c516a93453d9f2b8b4edc57`; that exact merge retained application repair merge `94e64ea215a39acfd2a04e3281bd899919494392` and passed the bounded source, native TypeScript, focused notification-routing, privacy, release-config, and staged-application guards before signing.
- Exactly one frozen-credentials EAS iOS Production build, `857ea362-40ca-4891-acae-f74cac5eec28`, finished from exact merge `13d2598347057b719c516a93453d9f2b8b4edc57` as Store-distribution Build 13. The established certificate serial ending `E4`, provisioning profile portal ID ending `N65`, Team `U9Y2574Y7S`, and App Store Connect key `9A87PH98PH` were reused without replacement. Direct inspection of that exact signed IPA proved SHA-256 `ad80844dd958208ba99ce3f76eca889c63a3b2cae5b08bb50c60c16c7ebeaa0f`, archive integrity, version `1.0.0`, build `13`, bundle `sg.prestigelimo.drivercompanion`, installed label `Prestige Driver`, light mode, iPhone/iPad families `1` and `2`, `arm64`, background `fetch` and `location`, Production push, Team identity, `get-task-allow` false, and only the two established associated domains. The compiled bundle retained `native_job_open` and the bounded `messages` destination marker without a private token or server-secret marker. The embedded provisioning profile was byte-identical to accepted Build 12 and remains valid until 12 August 2027. The local Mac trust-chain command produced the same non-trusted-root warning as accepted Build 12; raw signed entitlements and the embedded profile decoded successfully, so no broader local trust-chain claim is made.
- Exactly one EAS submission, `0ab0561e-c0b7-4c75-847b-e30ac6b36788`, uploaded only that inspected Build 13 to existing App Store Connect app `6800706103`. Apple processed exact Build 13 ID `195386d2-621a-460c-9ab8-02bf895c364a` as `VALID`. After separate action-time owner confirmation, only that Build 13 was assigned to the existing internal `Owner Testing` group; a visible signed-in App Store Connect read showed `Owner Testing` on the Build 13 row, and the independent EAS/App Store status read reported `IN_BETA_TESTING`. No binary retry or duplicate upload, group/tester creation or change, automatic future-build access, external testing, App Review, or public release occurred.
- Before the full Driver app can be owner-locked, physical Build 13 acceptance must prove three separate taps while the app is backgrounded or the phone is locked: one Admin-message alert lands at `Messages & Updates`; one one-hour pickup reminder opens the normal exact job; and one generic/new-job alert opens the correct normal job. Source, guards, Mac Chrome, signed-IPA inspection, Apple processing, and Owner Testing assignment cannot replace those physical checks.
- Build 13 changes no account, one-phone binding, Face ID, private-link association, acknowledgement, pre-ack safe-detail editing, post-ack lock, status/reporting, OTS proof, Report Issue, Job Alerts enablement, GPS/live location/Pickup Risk, personal or Operations Calendar, Driver Database/Delete + Revoke, Pending ACK Queue, customer/Admin surface, invoice/payment/payout/PayNow, schema/data, environment, Android/Google Play, external testing, App Review, or public release behavior.

### Driver iOS TestFlight Build 14 Logo Replacement (2026-08-18)

- The owner narrowly approved local source preparation of a replacement Driver icon from the supplied 1254-by-1254 RGB PNG. The artwork is normalized without cropping to the required 1024-by-1024 RGB PNG at `driver-companion/assets/icon.png`, with SHA-256 `bf866745d1f7a59791eea43204df17083a857a0943d7365ac3adf0161e008552`; the Driver-specific `DRIVER` word remains visible.
- Because App Store Connect requires a unique build number for any replacement binary, `ios.buildNumber` is explicitly `14`, advancing only from processed Build 13. Public iOS version `1.0.0`, bundle `sg.prestigelimo.drivercompanion`, EAS project `2a797181-d09d-4384-8d01-583456e83c3e`, App Store Connect app `6800706103`, installed label `Prestige Driver`, light mode, iPhone/iPad support, signing identity, capabilities, permissions, Universal Links, background location, notifications, and every existing Driver workflow remain unchanged.
- Final preflight found the root Next.js boundary already correctly excludes both isolated native projects, while the older Driver background-location guard still matched the retired exact two-item JSON line from before `customer-companion` existed. Only that guard assertion now parses `tsconfig.json` and requires `driver-companion` membership, matching the established Customer guard pattern; no TypeScript boundary, Driver source, native configuration, tracking, permission, background task, or runtime behavior changed.
- This source checkpoint proves only the bounded local icon/configuration change and focused guards. No commit, push, pull request, merge, EAS build, Apple upload, TestFlight assignment, external testing, App Review submission, or public release is included in this local checkpoint. Any later provider action requires the owner's separate exact action-time approval and must stop on any project, bundle, Team, credential, App Store target, source, signed-binary, or internal-group mismatch.
- PR #326 carried only the final Driver preflight guard correction and its ledger checkpoint after its mistaken `staging` base was detected before merge and retargeted to `main`. The corrected PR contained one commit and two files, passed its Ready preview, and merged to `main` as exact source `36ea8d782839401a3c515f2e706f894cc01325f3`.
- Exactly one frozen-credentials EAS iOS Production build, `c576aae8-de23-4d87-97d7-098e97302e91`, finished from exact merge `36ea8d782839401a3c515f2e706f894cc01325f3` as Store-distribution Build 14. EAS confirmed project `@prestige-limo-ops/prestige-driver-companion`, project ID `2a797181-d09d-4384-8d01-583456e83c3e`, bundle `sg.prestigelimo.drivercompanion`, Team `U9Y2574Y7S`, certificate serial ending `E4`, active provisioning-profile portal ID ending `N65`, and App Store Connect key `9A87PH98PH`, with no credential replacement. Direct inspection of that exact IPA proved SHA-256 `ea4762274801c34bf6af243d5adf50b04424dc1af035499229b7e0ac445fe302`, version `1.0.0`, build `14`, installed label `Prestige Driver`, opaque compiled iPhone/iPad icons containing the approved `PRESTIGE DRIVER` artwork, Production push, `get-task-allow` false, beta reporting active, and only the two established associated domains. Its signed entitlements were byte-for-byte identical to accepted Build 13; both binaries produced the same local Mac non-trusted-root warning, so no broader local trust-chain claim is made.
- Exactly one EAS submission, `4b83f56c-1566-40c5-8c20-63e4c37ccb6e`, uploaded only that inspected Build 14 to existing App Store Connect app `6800706103`. Apple processed exact Build 14 ID `b308a1fe-018a-4425-9d00-d3a4f6ac1831` as `Complete` on 19 August 2026. Although the EAS submission requested internal group `Owner Testing`, the processed build initially showed `Groups (0)`; no duplicate submission was made. The established Build 14 `Add Group` action then assigned only existing internal group `Owner Testing`, ID `c0b20e3f-bb6b-45cc-8e31-c27519d8aa61`. Final signed-in App Store Connect readback showed Build 14 with `Group (1)`, `Owner Testing · Internal · 1`, and the group page showed `1 Tester · 14 Builds`.
- Build 14 changes only the Driver icon and explicit build number already described above. No external tester or group, automatic future-build access, App Review submission, public release, Android build, credential replacement, Production application/data/environment change, notification send, booking/customer/driver write, Calendar action, invoice/payment/payout/PayNow action, GPS action, or external message occurred. On 19 August 2026, the owner explicitly confirmed that Build 14 was installed on the iPhone and that the new `PRESTIGE DRIVER` logo is good. This confirms physical installation and icon acceptance only; all other Build 14 workflows and the Admin-message, one-hour reminder, and generic/new-job background or locked notification-tap checks remain unverified.

## Customer Booking Public Reference Lane

- Status: source implementation, bounded local verification, Production schema activation, Production application deployment, and deployed owner-Chrome runtime proof complete.
- The established `bookings.booking_reference` remains the immutable internal workflow/auth/calendar/GPS/message/invoice join key. It is not shortened, replaced, or accepted as customer-visible authorization.
- `bookings.public_booking_reference` is an additive immutable public locator assigned for every booking row by the single established database insert path. Existing rows are backfilled once in deterministic order.
- Customers without a locked booking prefix receive one globally unique five-digit reference, with the approved global sequence beginning at `10826`. A customer with an admin-locked prefix receives a separate lifetime booking sequence in `PREFIX-00001` format. Each outbound/return row consumes its own number.
- Prefixes are unique, uppercase alphanumeric, 2-12 characters, locked after save, and managed only from the existing internal customer folder boundary. Existing trip references never change when a prefix is later added.
- Five-digit allocation fails closed after `99999`; it never wraps or reuses a reference. Digits alone remain a locator/search value, never an authorization credential.
- Admin search/display (including the existing live-map label), authenticated Customer My Bookings, customer/driver dispatch copy, customer request and driver-details email copy, customer request response, and the token-scoped Driver Job safe schedule prefer the persisted public reference while all writes and scoped reads continue using the internal reference.
- This lane does not read or write `customer_invoice_sequences`, `monthly_invoice_issue_records`, invoice prefixes, invoice running numbers, billing identity, pricing, payment, payout, PayNow, calendar UID, live-location authorization, message scope, or provider configuration.
- The two new sequence tables have RLS enabled, `anon` and `authenticated` fully revoked, and only the minimum server-side `service_role` read/write grants required by the established booking trigger and customer-folder prefix settings lane. No public Data API policy is added.
- Production migration `customer_booking_public_reference_foundation` was applied with owner approval on 18 July 2026. All 13 existing booking rows received unique valid public references `10826` through `10838`; the verified next global number is `10839`. A current Supabase default-grant preflight then found excess server-role privileges; follow-up migration `customer_booking_public_reference_service_role_least_privilege` was applied and verified to remove DELETE/global INSERT while retaining only the exact server reads and sequence advances required by this lane. `anon` and `authenticated` have no DML privilege on either sequence table.
- Draft PR #38 exact preview build `34a0fbdc` deployed successfully and was opened in the owner Chrome on 18 July 2026. Static admin/customer surfaces rendered, but the preview Vercel project reports its established saved-account/database read is not enabled or configured; therefore preview live-reference reads were not claimed. No environment setting was changed, and Production application runtime proof remains pending the owner-approved merge.
- After owner approval, PR #38 merged into Production `main` as `0ae8a096` and its exact Vercel deployment succeeded. Read-only owner-Chrome acceptance confirmed exact build `0ae8a096`, loaded the existing 13 saved bookings, and proved that searching completed jobs for `10826` selected exactly the Deep July booking. Expanding that card then exposed one display-only defect before completion was claimed: its existing Details section hid `Ref: 10826` whenever the operational compatibility card omitted its internal `booking_reference`, even though the saved booking retained and searched its persisted public reference.
- The bounded repair removes only that unrelated display condition and always renders the same established `operationalCard.public_booking_reference || bookingPublicReference(savedBooking)` value inside the existing Booking details section. Search, persistence, authorization, Calendar, GPS, messages, invoices, billing, customer/driver reads, sequence allocation, and live records remain unchanged. Focused protection now rejects reintroducing the unrelated operational-field gate. No booking, prefix, invoice, message, email, GPS, Calendar event, customer, driver, environment value, or Supabase record/configuration was changed during reproduction or source repair.
- Review also exposed one stale pre-existing assertion in `scripts/test-load-bookings-operational-record-mapper.mjs`: it still required the retired single-page `searchParams` fetch text even though Production commit `f384cd0a` intentionally moved the same established GET to paged `listSearchParams` for complete active-job monitoring. Only that guard assertion is aligned to the already-deployed GET; no runtime monitoring code, API, timer, pagination, booking read, mapper, or writer changed.
- Repair verification passed the strengthened public-reference guard, admin saved-booking read guard, aligned operational mapper guard, customer portal adapter, customer receipt email guard, all three existing PA/booker invoice identity guards, TypeScript, lint with zero errors and 161 existing warnings, and the Next.js 16.2.6 Production build. The first Driver Job browser command truthfully failed at `about:blank` because no local server was running; after starting the exact built app, the Driver Job browser guard passed and the full booking UI browser suite passed in 26 seconds with zero browser errors, zero console errors, zero blocked Supabase requests, and zero blocked Supabase mutation requests. Stopping that temporary server printed the established fail-closed `supabaseUrl is required` diagnostic from an unconfigured backend path; no Supabase request or mutation occurred in the guarded run.
- PR #39 merged as Production commit `4fc3747c` after its exact Preview build and both Vercel checks passed. Read-only owner-Chrome acceptance confirmed exact Production build `4fc3747c`, 13 loaded bookings, and four current Completed/History rows. Searching `10826` again selected exactly the Deep July booking, but expanding it still omitted the visible reference because Completed/History uses a second established operational details renderer that had never contained a Ref row; the first repair correctly covered only the active Bookings renderer. This remaining failure was reported before completion and no Completed action or live-record mutation was performed.
- The follow-up repair adds the same safe `operationalCard.public_booking_reference || bookingPublicReference(savedBooking)` label to that existing Completed/History Booking section. The existing focused guard now scopes the completed details block explicitly and fails unless it also renders the persisted public reference. No new renderer, card, route, API, table, read, writer, search, status action, invoice behavior, or customer/driver lane is added.
- Follow-up verification passed the strengthened public-reference guard, the same saved-booking/mapper/customer/email/invoice guard set, TypeScript, lint with zero errors and 161 existing warnings, and the Production build. Against the freshly built local app, the Driver Job browser guard passed and the full booking UI browser suite passed in 26.1 seconds with zero browser errors, zero console errors, zero blocked Supabase requests, and zero blocked Supabase mutation requests. The temporary server again printed only the established fail-closed unconfigured-Supabase diagnostic when stopped.
- PR #40 exact head `153e750e` passed both Vercel checks, rendered its exact protected Preview build in owner Chrome, and merged as Production commit `5778b71a`. Vercel reported the exact merge deployment successful. Read-only owner-Chrome acceptance confirmed build `5778b71a`, loaded the existing 13 saved bookings, opened Completed/History with its current four rows, searched `10826`, selected exactly the Deep July booking, and visibly rendered `Ref: 10826` in its expanded Booking section. The first combined browser step timed out before the Completed search box appeared because the tab selection had not settled; a second explicit tab click loaded the same established view, after which the exact proof passed. No Load, Undo completed, Billing ready, Delete, booking, prefix, invoice, message, email, GPS, Calendar, customer, driver, environment, or Supabase action was performed. The exact tested Production page was left visible in the owner Chrome.
- A fresh verified returning-booker submission then reproduced an uncovered active-request defect for public reference `10839`: the customer page and delivered receipt displayed `10839`, while Dashboard Booking Requests, Dispatch previews/status, and the Driver Job Link header still rendered the internal `CUST-...` key, and Bookings quick search returned no match for `10839`. Source diagnosis found that the operational-record-to-booking and booking-to-operational-record compatibility mappers dropped `public_booking_reference`, the notification row rendered its internal action key directly, and customer/driver copy text read the internal workflow reference instead of the existing public display selector.
- The bounded repair retains `public_booking_reference` through both existing compatibility mappers, includes it in operational request search, and routes the established Dashboard notification, Dispatch load/status summaries, Customer Copy, Driver Job Link copy/header, active-job label, driver report/OTS status text, and customer app-link copy feedback through the existing public-reference selector. Internal booking keys remain unchanged for writes, authorization, CRM identity, calendar UID, messaging, GPS, status, and billing joins. No route, booking writer, notification writer, panel, search lane, sequence, schema, migration, invoice behavior, or live record was added or changed.
- Focused protection: `scripts/test-customer-booking-public-reference-guard.mjs`.
- Local verification: the focused public-reference/customer/driver/admin-read/email guards, all three existing invoice identity guards, `npx tsc --noEmit`, lint (0 errors; existing warnings only), production build, Driver Job browser guard, and the full booking UI browser guard passed. The browser fixture retained internal key `ui-cleanup-load-fixture` for every guarded API request while visibly rendering `10839` in Customer Copy and rejecting that internal key from the copy; the final 27.4-second booking UI run reported 0 browser errors, 0 console errors, 0 blocked Supabase requests, and 0 blocked Supabase mutation requests. Receipt-provider mocks proved the verified same-booker link is included and first-time/mismatched recipients remain link-free. No live receipt or portal link was sent during local verification, and no local Postgres/Supabase runtime is configured.

### Admin Booking Created Time SGT Display Repair

- Read-only Production inspection reproduced booking `10841` displaying raw `Created 2026-07-18T14:00:18.861093` in the existing Active Bookings details card. The exact deployed build was `ca2d28bf`, and the browser reported no console warnings or errors.
- Read-only Production schema evidence confirmed the live `bookings.created_at` column is the legacy `timestamp without time zone` type and the exact booking stores `2026-07-18 14:00:18.861093`, even though repository migrations describe the intended foundation as `timestamptz`. The stored clock value is the UTC creation instant; no schema conversion or record rewrite is included in this repair.
- The existing client `formatCreatedAt` lane now treats timezone-less ISO/Postgres values as legacy UTC, converts both legacy and timezone-aware values explicitly through `Asia/Singapore`, and renders the compact admin format `18 Jul 2026, 2200hrs SGT`.
- Both established Active Bookings and Completed/History details renderers now pass the typed operational `created_at` value through that same formatter. No new formatter lane, card, route, API, table, read, writer, timer, status action, or UI control is added.
- The existing `scripts/test-load-bookings-operational-record-mapper.mjs` guard now executes the actual page formatter against the exact legacy bare-UTC value plus equivalent `+00:00` and `+08:00` values, and requires both existing renderers to format typed operational timestamps.
- Pre-fix focused testing also exposed a stale assertion in `scripts/test-load-bookings-typed-operational-display-merge-guard.mjs` that still named the retired single-page `searchParams` request. That assertion is aligned only to the already-deployed paged `listSearchParams` GET; runtime pagination, API wiring, booking reads, and actions are unchanged.
- Supabase schema/configuration, booking data, customer/driver surfaces, Calendar, GPS, messaging, email, provider sends, CRM identity, pricing, payment, payout, PayNow, billing, PDF, and every invoice workflow remain unchanged. No live record was modified during diagnosis or repair.
- Focused protection: `scripts/test-load-bookings-operational-record-mapper.mjs` and `scripts/test-load-bookings-typed-operational-display-merge-guard.mjs`.
- Local verification passed both focused Load Bookings guards, the customer public-reference guard, `npx tsc --noEmit`, lint with 0 errors and the existing 161 warnings, the Next.js 16.2.6 Production build, and the full booking UI browser guard. The 28.2-second browser run reported 0 browser errors, 0 console errors, 0 blocked Supabase requests, and 0 blocked Supabase mutation requests. Stopping the temporary built server printed only the established fail-closed unconfigured-Supabase diagnostic; no database request or mutation occurred in that run.

### Production Driver Demo Privacy Boundary

- Read-only Production inspection reproduced that `/driver-job-demo` remained publicly reachable and visibly exposed mock/development workflow controls plus a PayNow-number input. The page was not connected to live bookings, but a public driver-facing Production surface must not expose mock QA/dev workflow or PayNow details.
- The public Production `/driver-job-demo` route now terminates at the existing Next.js not-found boundary. The boundary is server-only, route-local, and keyed only to Vercel's exact `VERCEL_ENV=production` runtime identity.
- Local development and Vercel Preview retain the existing mock page for controlled training and browser regression tests. No duplicate demo, driver page, route, helper, API, table, button, persistence path, notification lane, or provider integration is added.
- The real token-scoped `/driver-job/[token]` operational workflow is unchanged. Driver acknowledgement, status reports, Driver Calendar, Live Dispatch, messages, booking, invoice, payment, payout, PayNow, GPS, OTS photo, customer, and admin workflows are not modified.
- Local runtime verification returned HTTP 404 with no demo heading, mock summary, or PayNow field under exact `VERCEL_ENV=production`, HTTP 200 with the unchanged mock page when that Vercel identity was absent, and HTTP 200 for the admin root in both checks. The focused Driver Job browser test, Next.js Production build, lint with zero errors and the existing 161 warnings, locked Pending ACK/Admin completion/Driver Calendar/invoice guards, and diff check passed. A redundant full app-smoke run was stopped after the relevant demo and real Driver Job checks had loaded successfully because its unrelated long admin workflow sweep continued detached; the previously completed full app-smoke audit remains green and no result from the stopped run is claimed.
- Focused protection: `scripts/test-driver-job-demo-production-privacy-guard.mjs`.

### Admin Booking Browser Fixture And Public OTP Smoke Alignment

- Failure inspection separated four unrelated conditions before repair: the visible Codex reconnection was an external ChatGPT/Codex HTTP 503; a later localhost refusal meant the local development server was stopped; local Supabase-backed admin APIs correctly failed closed because server-only local Supabase credentials were not configured; and the repository defects were stale in-memory browser fixtures and assertions.
- The full browser smoke now intercepts the established `/api/admin-saved-bookings` read beside its existing admin booking fixture and keeps the current customer app confirmation check/send in memory. Its accepted-request assertion matches the established confirmed review state and customer app confirmation feedback without changing the application route, persistence helper, notification lane, or UI.
- The canonical Admin booking browser test installs its fixture before navigation, uses the current auto-loaded saved-job search surface and `Open / Edit` action, keeps its sample booking safely in the future, selects by stable booking reference, and verifies only the customer-safe public reference is visible.
- The `/book` smoke fixture now exists before the page performs its initial customer access read and supplies the complete safe customer-memory profile shape. Direct public API probes recognize the exact existing phone-verification boundary; a mock portal navigation without a real customer session must remain on the public OTP gate.
- Test instrumentation ignores only Next.js development-owned `/_next/webpack-hmr` WebSocket traffic and `__next_debug_channel:` session entries. Prestige/customer storage and every non-framework WebSocket remain subject to the existing privacy assertions.
- No application route, component, API, helper, persistence path, Supabase configuration or data, environment variable, provider send, OTP implementation, booking workflow, customer/driver workflow, Calendar, GPS, payment, payout, billing, PDF, or invoice workflow was modified.
- Focused protection: `scripts/test-admin-booking-browser-fixture-boundary-guard.mjs`, registered in `scripts/test-preactivation-verification-suite.mjs`.
- Local verification passed the focused fixture boundary guard, canonical Admin booking browser test, public phone OTP guard, Admin route flow lock, and the full app browser smoke. The final 211.7-second smoke reported zero runtime errors and zero console errors across Admin, `/book`, `/my-bookings`, customer, Driver Job, responsive, and public route-leak checks.

### Returning Customer New Passenger Entry Repair

- The owner reproduced on the deployed `/book` page that a verified returning customer with at least one registered traveller saw only the registered-traveller selector. The selector was required, so the customer could not type a different passenger name and the form reported `Passenger name` as missing.
- The established booking request API already supports the correct identity boundary: a registered traveller is accepted only by its exact verified `travelerId`, while a typed passenger with an empty `travelerId` remains an unverified booking passenger name. No API, database, CRM writer, customer-access session, or identity derivation change was required.
- The existing Passenger name control now keeps the registered-traveller selector as an optional choice and displays a required blank passenger-name input whenever no registered traveller is selected. Choosing a registered traveller retains its verified ID and established saved trip defaults; choosing `Enter a new passenger name` clears the traveller ID and reveals the blank input.
- Typing a new passenger does not overwrite, rename, or falsely reuse the registered traveller record and does not silently create another CRM traveller. Admin review and the existing verified CRM maintenance lane remain the only place to establish that identity.
- OTP, booking persistence, customer/company/booker scope, invoice and billing workflows, payments, payouts, Driver Job, Calendar, GPS, messaging, notifications, provider configuration, environment values, Supabase schema/data, and all other wired lanes remain unchanged.
- Focused protection: `scripts/test-customer-rebooking-permanent-link-receipt-guard.mjs`, plus the returning-customer branch in `scripts/test-app-smoke-browser.mjs` and its fixture-boundary guard.

### PA Booking Submit Guard Alignment

- Validation of the returning-customer Passenger name repair exposed one unrelated stale source assertion in `scripts/test-customer-booking-request-pa-identity-guard.mjs`. The assertion still required the July 11 single-line submit-button implementation even though the established private-invitation and public OTP work later expanded the same button with booking-access resolution and verified-access locks.
- The application behavior was not broken: a successful request still renders `Submitted` and remains locked until a safe field edit clears `confirmationStatus`; the current button additionally stays locked while booking access is unresolved or unavailable and shows the established checking/phone-verification labels.
- Only the focused guard is aligned to the current complete button contract and the existing field-edit unlock. No application component, OTP behavior, booking route, identity boundary, API, persistence helper, database, CRM, customer/driver workflow, invoice/billing workflow, provider configuration, or environment value changed.

### Single-Field Passenger Name And Registered-Traveller Chooser

- Production Chrome visual inspection confirmed that the first returning-customer repair worked but stacked a registered-traveller selector above the new Passenger name input, making the Passenger name column twice as tall as the adjacent Flight number column.
- The established Passenger name lane now renders one full-width required input aligned with Flight number. Its contained dropdown arrow opens the same registered-traveller choices as an overlay; it does not add another field, column, route, panel, or persistence path.
- A registered traveller ID is set only by an explicit click on that exact saved option. The same input then displays the saved traveller name and retains the existing verified ID/default-trip behavior. Typing or editing any passenger name continues clearing `travelerId`, so free text cannot be inferred as a verified CRM identity.
- The server-side traveller validation, Customer Portal scope, booking-memory suggestions, booking request payload, CRM/customer creation or reuse, Admin alert, OTP/invitation gates, and booking persistence remain unchanged. A manually typed passenger still does not create, rename, overwrite, or impersonate a CRM traveller.
- Invoice/billing, payments, payouts, PayNow, Driver Job, acknowledgement, Calendar, GPS, messages, notifications, provider configuration, environment values, Supabase schema/data, and every other established workflow remain unchanged.
- Focused protection remains `scripts/test-customer-rebooking-permanent-link-receipt-guard.mjs`, with browser runtime coverage in `scripts/test-app-smoke-browser.mjs`.

### Single-Field Passenger Dropdown Collision Repair

- Production Chrome visual inspection reproduced two dropdown systems on the returning-customer Passenger name field: the native booking-memory datalist and the registered-traveller menu opened together, producing two arrows and overlapping suggestion boxes.
- The same Passenger name input now attaches the native booking-memory datalist only when no registered CRM travellers are available. When registered travellers exist, the established contained registered-traveller menu is the only dropdown attached to the field.
- The booking-memory datalist remains unchanged for customers without registered travellers. Exact registered-traveller selection still sets the verified `travelerId`, and manual typing still clears it.
- No API, CRM identity rule, persistence helper, OTP/invitation gate, booking request payload, invoice/billing, pricing, payment, payout, Driver Job, notification, provider, environment value, Supabase schema/data, or other wired lane changed.
- Focused protection remains `scripts/test-customer-rebooking-permanent-link-receipt-guard.mjs`, with the returning-customer browser assertion in `scripts/test-app-smoke-browser.mjs`.
- Production Mac Chrome acceptance on 2026-07-26 opened the existing `/book` Passenger name control with one registered traveller available. The live field showed exactly one contained arrow and one `Registered travellers` listbox containing `Enter a new passenger name` and the saved traveller; the input had no native datalist attached, no second suggestion box appeared, the document had no horizontal overflow, and Chrome reported zero warnings or errors. The form remained unsubmitted and no OTP, booking, CRM record, provider send, or Production data write occurred.

### Driver One-Hour Pickup App Push Reminder (2026-07-26)

- The owner clarified that Driver Google Calendar is an optional bonus and must not be the primary one-hour pickup alert. The established Driver App Update and device-push lane now schedules one reminder for every verified-driver booking whose saved `pickup_at` enters the exact 60-to-61-minute window, independent of whether that driver added the job to Google Calendar.
- The first Preview deployment reproduced that the linked Vercel project is on Hobby and rejects any Cron expression running more than once per day; its immediate failure linked directly to Vercel's Cron usage/pricing boundary. The incompatible Vercel schedule was removed before merge. The owner then specifically approved one Supabase Cron and server-only secret repair: `pg_cron` invokes the existing protected application route once per minute through `pg_net`, with the fixed Production endpoint and dedicated bearer value stored only in Supabase Vault. The migration contains neither secret value, and the cron command fails closed unless both exact named secrets exist and the endpoint equals the fixed Prestige Production route.
- Production activation created the dedicated sensitive Vercel value without reading or changing any existing environment value, stored the matching bearer and fixed endpoint as two named encrypted Vault entries, enabled `pg_cron`, and enabled `pg_net` in the protected `extensions` namespace. The initial default `pg_net` install placed only its extension namespace in `public`; the Supabase security advisor identified it immediately. Read-only inspection found an empty request queue and only two disposable pre-deployment HTTP response records, so that new extension was recreated once in `extensions`, removing only those two temporary response records. The follow-up advisor check must remain free of that warning.
- The bounded application check reads only non-terminal saved booking schedule/assignment fields, the newest exact active Driver Job Link for that booking and driver, and existing Driver Reports status evidence. A POB or Job Completed/JC job is skipped. Cancelled, completed, archived, history, or otherwise terminal bookings are skipped.
- Each eligible reminder is persisted through the existing `customer_driver_app_notification_outbox` as one high-priority `driver_app` App Update with fixed safe text. Its event key uses the exact booking reference plus exact saved pickup timestamp, so concurrent or repeated cron runs cannot duplicate the same reminder; a later real pickup-time amendment becomes a distinct correctly timed reminder.
- The existing Driver device-push sender and existing Driver service worker deliver the fixed lock-screen copy `Pickup is in 1 hour. Open Driver Portal to review.` to active subscriptions for only the exact verified assigned driver. A tap resolves the existing opaque job key to the already-known private Driver Job page on that device, otherwise it falls back to the established Driver Portal. No raw private token, booking reference, passenger, route, contact, customer price, billing, invoice, payment, driver payout, PayNow payout, internal/admin note, parser/debug content, GPS, photo, or mock/archive data appears in the push payload.
- Driver push still requires the driver's one-time browser/OS permission and an active subscription created by the established `Save & Acknowledge Job` or installed Driver Portal `Enable Job Alerts` action. Calendar enrollment is not required. Missing permission/subscription or provider failure does not roll back the saved App Update and does not create Email, SMS, WhatsApp, Telegram, retry, fallback, or batch delivery.
- Driver Calendar/OAuth/events, Driver Job acknowledgement, Pending Driver ACK Queue, Driver Reports and explicit `Admin confirm completed`, customer/driver quick replies, Live Dispatch, GPS, OTS photo, booking persistence, customer portal, pricing, payment, payout, PayNow, and the entire owner-locked invoice workflow remain unchanged. Before the separately approved activation, no Production booking, status, notification, subscription, push, Calendar event, customer/driver message, invoice, payment, or external contact was changed.
- Focused protection is `scripts/test-driver-one-hour-pickup-push-reminder-guard.mjs` plus the extended existing `scripts/test-driver-job-device-push-alert-guard.mjs`, registered in the preactivation suite.

### Driver Portal Cross-Platform Alert State Repair (2026-07-26)

- Production Android acceptance for booking `10846` reproduced that the existing installed Driver Portal always returned visually to `Enable Job Alerts` after reopening and displayed iPhone-only instructions, even though the same Android FCM endpoint had already been registered. After the owner had the driver use the existing action once inside the installed Android portal, the same endpoint was refreshed in place and the exact follow-up background push was received while the portal was backgrounded.
- The existing Job alerts card now uses platform-neutral device guidance. On each successful existing Driver Portal jobs read, it performs only a browser-local permission, exact `/driver-job/` service-worker registration, and Push subscription lookup. A granted permission plus an existing subscription restores the disabled `Job Alerts Enabled` state without another permission prompt, subscription POST, provider send, database write, or duplicate endpoint. Missing permission or subscription retains the established one `Enable Job Alerts` action.
- The existing service worker, scope, VAPID identity, subscription table, Driver Portal jobs route, registration write, background sender, one-hour scheduler, Driver Job acknowledgement, Calendar, Driver Reports, Pending ACK Queue, booking, pricing, billing, invoice, payment, payout, PayNow, GPS, customer messaging, and every other wired lane remain unchanged.
- Focused protection is the updated `scripts/test-driver-job-device-push-alert-guard.mjs` and `scripts/test-driver-job-page-browser.mjs`, alongside the unchanged Driver Portal session/jobs and one-hour pickup reminder guards.

### ChatGPT Booking Preview Foundation — Stage 1 (2026-08-02)

- One server-only reusable preview normaliser accepts already-parsed booking fields, validates and normalises their safe values, builds the established `AdminBookingPersistenceInput` shape, and then reuses `parseAdminBookingPersistencePayload` as the authoritative canonical DTO validator. It does not parse source messages or alter the protected booking parser.
- The new read-only-computation `POST /api/admin-booking-preview` route is protected by the existing same-origin admin/dispatcher boundary with the dedicated `x-prestige-admin-purpose: admin-booking-preview` purpose. It returns only `preview`, `validation_issues`, and `missing_required_fields`.
- The preview requires service, vehicle, pickup date/time/location, drop-off, passenger, and contact phone. Service is limited to `MNG`, `DEP`, `TRF`, or `DSP`; vehicle is limited to `E`, `S`, `AVF`, `VVV`, or `Combi`. An unrecognised vehicle may use only an explicitly supplied `E` or `AVF` fallback and records a visible warning.
- Customer price and general notes remain visible only as explicitly labelled preview-only values and are excluded from the canonical booking DTO. A supplied raw source message is never returned or retained in the DTO; the preview reports only that a source was supplied.
- The route and helper do not import or invoke a Supabase writer, create a customer, create a booking, assign a driver, write Calendar, send a message or provider request, prepare billing/invoice/payment/payout, or access live location. `POST /api/admin-bookings`, Dispatch `Save + CRM`, `app/page.tsx`, parser behavior, schema, environment, and all Production data remain unchanged.
- This is Stage 1 only. MCP, `create_booking`, machine authentication, confirmation tokens, idempotency, database writes, deployment, and Production configuration are not implemented.
- Focused protection is `scripts/test-chatgpt-booking-preview-api-contract.mjs`, registered in the preactivation suite. The guard executes the real canonical validator behind a writer that throws if called, verifies the exact response allowlist, authentication boundary, required-field failures, enum/date/time/count/email validation, explicit vehicle fallback, preview-only price/notes, raw-source exclusion, and absence of persistence/Supabase imports.

### Confirmed ChatGPT Booking Create Foundation — Stage 2 (2026-08-02)

- Stage 2 adds no booking endpoint. A future confirmed caller uses the existing `POST /api/admin-bookings` with the existing `x-prestige-admin-purpose: admin-booking-persistence` boundary plus the exact `x-prestige-booking-request-source: chatgpt-confirmed-preview` marker. All ordinary Dispatch `Save + CRM` requests remain the established unwrapped canonical payload and continue through the unchanged legacy branch.
- Explicit confirmation is a separate second request to the existing read-only Preview Booking API with `x-prestige-booking-preview-confirmed: true`. Only one valid, fully normalised preview receives a ten-minute HMAC confirmation token and expiry in response headers. The token is signed with the server-only `PRESTIGE_ADMIN_BOOKING_CONFIRMATION_SECRET`, binds the exact canonical payload plus bounded preview-only values and a digest of any raw source message, and fails closed when missing, invalid, expired, re-signed for another audience, or presented after any booking detail changes. The secret and raw source never enter a response, database row, audit snapshot, or client bundle.
- The confirmed create envelope contains only `request_source`, `idempotency_key`, `confirmation_token`, and `booking_preview`. Its booking preview is normalised through `normalizeChatGptBookingPreview`, which reuses `parseAdminBookingPersistencePayload`; after the server assigns the reserved booking reference, the final payload is parsed again and passed to the existing `createAdminBooking` function. No second parser, validator, booking writer, customer writer, route, database, or Save + CRM lane is introduced.
- Durable idempotency uses the new server-only `admin_booking_idempotency_reservations` table. It stores only SHA-256 idempotency and payload digests, one generated booking reference, a hashed owner claim, state, and timestamps. The primary-key uniqueness claim occurs before the existing create call: the winning request creates once, a completed same-key/same-payload replay reloads the existing booking reference, a same-key/different-payload request returns a validation conflict, and concurrent duplicates wait for the winning completed reservation without calling the booking writer. Failed owned attempts are marked failed for a later exact retry.
- The table has RLS enabled, no policies, explicit `anon`/`authenticated` revocation, and only `service_role` select/insert/update grant. The migration file is committed preparation only and was not applied to local, Preview, Staging, or Production Supabase during this stage. No Production environment value is added or changed; without the migration and a valid confirmation secret, the confirmed create branch fails closed.
- The confirmed caller response always uses only `success`, `booking_reference`, `saved_booking`, and `validation_issues`. The saved summary is allowlisted to service, vehicle, pickup time/location, drop-off, flight, passenger name/count, and bag count. It never returns Supabase IDs, customer/company/booker/traveller IDs, contacts, audit data, parser internals, raw source, idempotency digests, confirmation material, credentials, provider responses, prices, billing, invoices, payment, payout, driver data, Calendar, messaging, or live location.
- This is server-side preparation only. MCP, OAuth, ChatGPT connectivity, machine authentication expansion, deployment, environment configuration, and migration application are not implemented. Driver assignment, messaging, personal or Operations Calendar, billing, invoice, payment, payout, live location, provider integrations, parser behavior, `app/page.tsx`, and the existing Save + CRM payload/logic remain untouched.
- Focused protection is `scripts/test-chatgpt-confirmed-booking-create-api-contract.mjs`, registered in the preactivation suite. It covers HMAC/expiry/payload binding, exact envelope validation, canonical-validator reuse, safe output allowlist, missing/invalid confirmation, completed replay, different-payload conflict, concurrent single-create behavior, hashed-only/RLS migration boundaries, and the unchanged existing Save + CRM source.

### Compact Airport Arrival Origin, Passenger And Address Parser Repair (2026-08-03)

- The exact compact arrival message `Tue 4 Aug / Arrive from Bangalore / SQ 509, ETA 19:00 Mr. Jwalant. 82 Grange Road` reproduced a deterministic-parser collision: `Arrive` was treated as the passenger/booker, `Bangalore` as the company, and the numbered Singapore destination was dropped.
- The established parser now treats the standalone `Arrive from ...` line as flight-origin context only when the same message also contains ETA, a flight number, an honorific passenger, and a numbered destination address. It reuses the existing narrated name/address normalisers and produces `MNG`, `04 Aug 2026 1900hrs`, `SQ509`, `Changi Airport > 82 Grange Road`, passenger `Mr Jwalant`, and no inferred company or booker.
- The flight-origin context is not guessed into the company, customer, pickup, or drop-off fields. The established booking form and persistence DTO have no separate origin-country field, so no schema, UI field, secondary parser output, or persistence lane was added.
- Dispatch UI, `Save + CRM`, booking APIs/persistence, CRM identities, driver assignment, messaging, Calendar, live location, pricing, billing, invoice/payment/payout, provider integration, environment, schema, and Production data remain unchanged.
- Focused protection is the exact regression in `scripts/test-booking-parser.mjs`, alongside all existing confidence examples and real-world parser fixtures.

### Customer Profile Verified Booker And Traveller Maintenance (2026-08-03)

- Read-only Production diagnosis of booking `10868` proved that its typed company, Booker, passenger, contact and email text had saved, but the exact booking still had verified `booker_id` and `traveler_id` set to null. The verified company existed, while that company had no Booker or Traveller rows. This was not a missing Dispatch text field: the app correctly refuses to infer authorization IDs from names, email, phone, passenger text or parser output.
- The existing exact-customer company profile now contains one compact `Booker / PA & Travellers` disclosure. It displays safe linked names and provides Booker / PA name, optional email/contact and Traveller name inputs. Internal numeric IDs remain hidden. When a company has no identities, the disclosure opens so Admin can establish the first exact pair.
- The deliberate `Save Booker + Traveller` action requires visible Admin confirmation, exact-reads before creating, reuses an existing exact same-company Booker or Traveller, refuses a Traveller already linked to a different Booker, creates only missing records through the existing typed Booker and company/traveller writers, links the exact Traveller through the existing allowlisted PATCH, and re-reads the established rate-setup identity projection before reporting success. A retry after a partial save reuses the existing record instead of deliberately creating another one.
- The existing typed Booker route accepts tokenless same-origin POST only when the verified admin/dispatcher session comes from the exact `/customers` profile surface. Its later same-surface PATCH repair is recorded in `Corporate Booker And Traveller Save-Stick Repair (2026-08-05)`. Cross-origin, missing-purpose, unsupported role, PUT and DELETE access remain blocked.
- Dispatch `Load CRM identities` continues reading the same rate-setup company/Booker/Traveller projection, so a newly verified pair becomes selectable in the existing Booking Details identity row. Existing bookings are not bulk-linked or inferred; booking `10868` still requires the established Section 4 `Proceed for this booking` confirmation to attach the reviewed exact IDs.
- Customer `/book` behavior and code remain unchanged. Its registered Traveller chooser uses the same verified company/Booker/Traveller records only after the established `Copy + App Link` flow creates verified customer access; free-text passenger entry remains unverified and creates no CRM identity.
- No parser, Dispatch `Save + CRM`, booking persistence, invoice layout/calculation/issue/email/payment, driver assignment, Driver Job, Calendar, messaging, GPS, provider, schema, migration, environment or Production data path changed. Focused protection is `scripts/test-customer-profile-verified-identities-guard.mjs` plus the existing admin Booker, Dispatch CRM identity, customer booking PA identity and customer invoice privacy guards.

### Customer iPhone Companion Foundation

- A separate `customer-companion/` Expo iOS project now provides an iPhone-only, light-mode native shell named `Prestige SG` with the unique proposed bundle identifier `sg.prestigelimo.customer`.
- At this foundation checkpoint, it retained the exact then-current Prestige logo byte-for-byte and presented only a compact native header, `Request a Ride` and `My Bookings` tabs, strict same-origin navigation, and an optional local Face ID privacy lock. It has no giant card, dashboard sector, duplicate form, or alternate Customer workflow.
- The native WebView reuses only the established Production `/book`, `/my-bookings`, signed `/api/customer-portal-access/[token]` redirect, and public `/privacy` and `/terms` pages. Same-origin cookies stay inside the existing portal session; external web pages, Admin routes, Driver routes, embedded credentials, unknown Prestige paths, mixed content, and third-party cookies fail closed.
- This foundation does not modify `/book` or `/my-bookings`, their APIs, CRM identification, booking persistence, customer/PA scope, invoice/PDF lane, messaging, live tracking, amendment/cancellation review, OTP/invitation behavior, or any database writer. It adds no Supabase client, server secret, Stripe/payment, pricing, payout, PayNow, provider-send, notification, Calendar, GPS, Driver, Admin, schema, environment, deployment, or Production data change.
- No Apple App ID, App Store Connect record, EAS project, signing credential, build, TestFlight assignment, submission, or public release is created here. Universal Link association remains unmodified until the exact Customer Apple identity exists and the owner separately approves that public read-only association change; existing signed Customer App links therefore retain their established browser behavior at this checkpoint.
- This local source/type and Simulator checkpoint does not prove App Store acceptance or physical iPhone behavior. Later acceptance must separately prove installed signed-link activation, session continuity, Face ID relaunch/foreground lock, both tabs, PDF behavior, exact customer/PA isolation, forbidden-field absence, and physical-device operation.
- The install-time audit reported inherited Expo/Metro/Xcode and React Native advisories with no critical finding; npm's proposed automatic resolution was an incompatible downgrade to Expo 53/React Native 0.72. No automatic audit fix or dependency widening was applied, and Expo remains pinned to the established Driver project's `57.0.6` pending a separate current-toolchain release audit.
- Visible Mac verification used Xcode 26.6 and an iPhone 17 Pro iOS 26.5 Simulator. The native Debug build installed and opened with zero build errors and one duplicate-library linker warning; `Request a Ride` displayed the unchanged live `/book` form, `My Bookings` displayed the unchanged live portal and correct unauthenticated `Sign in to view bookings` state, the native tab return worked, no form was submitted, and no customer, booking, invoice, or Production record was written.
- After the owner found the first native top sector too broad, only that existing header was made slimmer: its subtitle was removed, the logo reduced to 30 points, and the minimum header height reduced from 62 to 46 points. The two established native tabs and every embedded Customer page, route, workflow, API, persistence boundary, and privacy rule remain unchanged.
- PR `#316` first failed its Vercel preview because the root Next.js TypeScript include glob entered the isolated `customer-companion/App.tsx` while the root-only Vercel install correctly lacked that nested Expo project's dependencies. The focused guard reproduced the missing boundary before repair. Root `tsconfig.json` now excludes `customer-companion` beside the already excluded `driver-companion`, so the root web build does not type-check a separate native project; the Customer project retains its own strict `tsconfig.json` and passing type check. No root dependency or runtime lane changed.
- Face ID is source/type-protected but not Simulator-accepted: this Xcode `simctl` has no biometric subcommand, so no enrollment, prompt, relaunch unlock, or foreground unlock result is claimed. Physical iPhone acceptance remains required.
- The Customer TypeScript check, focused ESLint, Expo public-config resolution, iOS production JavaScript export, focused foundation guard, nine established Customer workflow/privacy guards, root diff check, and root lint all passed. Root lint retained 165 pre-existing warnings and no errors. Before the native-project exclusion existed, the local root build entered installed React Native globals and surfaced an unrelated-looking `global.FormData` mismatch inside the unchanged Driver OTS-photo route, while the clean root-only Vercel install stopped earlier on the absent Expo modules. After excluding the isolated Customer project, the unchanged root Next.js build completes through compilation, TypeScript, page-data collection and all 19 static pages with no Driver, root dependency, or runtime-lane change.
- Focused protection is `scripts/test-customer-companion-foundation-guard.mjs`, registered in `scripts/test-preactivation-verification-suite.mjs`, alongside the unchanged Customer form, portal, push, quick-reply, tracking, invoice-trust, and PA-scope guards.

### Customer iOS Store Icon And TestFlight Preparation (2026-08-18)

- The owner identified the supplied 640-by-640 JPEG as the Customer App Store artwork. It is normalized without cropping to the required 1024-by-1024 RGB PNG at `customer-companion/assets/app-icon.png`, with SHA-256 `722d9df4d237a42fedf03bffa3f48078b148d796714ff17d18467b2b320f4f0e`. It remains distinct from the Driver artwork and contains no Driver label.
- The Customer app's compact native header continues importing its established `customer-companion/assets/icon.png` artwork, SHA-256 `b55f726603fbae4bf0c101fee8dc23e782089f6fcb7f963c2542c9fc297cb670`. Separating the App Store icon avoids changing that visible in-app header or duplicating any Customer workflow.
- The established Customer app stays iPhone-only, light mode, named `Prestige SG`, with proposed bundle `sg.prestigelimo.customer`; `ios.buildNumber` remains explicitly `1` for its first future signed build. The native tabs, Face ID privacy lock, same-origin navigation, signed Customer access, booking, amendment/cancellation, messaging, tracking, invoice/PDF, privacy exclusions, and all server/data writers are unchanged.
- `customer-companion/` is now included in the existing staged application-change enforcement so a future Customer native change cannot be committed without the implementation ledger and a focused `scripts/test-*.mjs` guard. This adds no application route, provider, signing, data, or runtime lane.
- No Apple App ID, App Store Connect record, EAS project, signing credential, build, submission, TestFlight assignment, external testing, App Review submission, or public release is created by this local checkpoint. Those remain separate provider actions requiring exact action-time owner approval and must stop on any name, bundle, Team, credential, project, source, signed-binary, or TestFlight-target mismatch.

### Customer App Store Icon Clarity And SG Approval (2026-08-18)

- The owner rejected the first Customer App Store icon because the supplied 640-by-640 JPEG remained visibly soft after deterministic enlargement. The approved replacement is the reviewed clearer 1254-by-1254 RGB PNG with the exact centered gold text `SG` below the existing `PRESTIGE` wordmark, normalized without cropping to the required 1024-by-1024 RGB PNG at `customer-companion/assets/app-icon.png`.
- The exact approved normalized Customer App Store asset has SHA-256 `f04038c4b341131cc36d182943da0a1b661c6b30420f20fe4ff2db14a36f6c9e`. The separate compact native-header artwork remains byte-for-byte unchanged at SHA-256 `b55f726603fbae4bf0c101fee8dc23e782089f6fcb7f963c2542c9fc297cb670`; the Driver icon and Driver Build 14 configuration remain unchanged.
- Under the owner's exact TestFlight action-time approval, one Customer EAS project was created under the existing operational owner as `@prestige-limo-ops/prestige-customer`, project ID `ce71ff91-7f71-4297-bcef-edf420f94316`, and linked in the existing Customer app configuration. Its name `Prestige SG`, slug `prestige-customer`, iPhone-only light-mode scope, proposed bundle `sg.prestigelimo.customer`, and explicit first iOS build number `1` remain unchanged.
- This icon repair changes no Customer route, tab, Face ID lock, access/session, booking, amendment/cancellation, messaging, tracking, invoice/PDF, server/data writer, Driver/Admin workflow, Calendar, notification, GPS, payment, payout, PayNow, schema, environment, signing, TestFlight, App Review, or public-release behavior. Focused protection remains `scripts/test-customer-companion-foundation-guard.mjs`.

### Customer iOS TestFlight Build 1 Owner Access (2026-08-18)

- Under the owner's exact provider approval, one Store-distribution Customer EAS iOS build, `57c2dc33-4ffe-4225-aba0-5049fa9f857d`, finished from exact merged source `45d532088a76ca053ec4525c3199ef61016fedb0` as Build 1. Direct inspection of that exact signed IPA proved SHA-256 `09fd34e20589b4802759778ef672f0c95e853baf42036323dc588a29d01870fe`, the approved `Prestige SG` artwork, and the established Customer bundle/version/build identity without changing the separate Driver app.
- Exactly one EAS submission, `1c990381-24e4-4fe0-9e4c-3aef36e20f9a`, uploaded only that inspected binary to existing App Store Connect app `6802691447`, whose App Store record is `Prestige Limo Sg` while the installed app remains `Prestige SG`. Apple processed exact Build 1 ID `4c5dd638-5d63-42a5-8034-ff4eee0292b5` and the build was assigned only to internal group `Owner Testing`, ID `c66d522b-c799-4134-be22-225a7192bf4a`.
- The initial group page showed `0 Testers · 1 Build` even though the Account Holder/Admin already existed as one internal tester under All Testers with `No Builds Available`; the group-level Add Testers dialog disabled that already-existing tester. The established All Testers `Add to Group` action then assigned only that existing owner tester to `Owner Testing`. Final signed-in App Store Connect readback showed `Owner Testing · 1 Tester · 1 Build` and the owner row in that group. No new tester, external testing group, public link, automatic future-build access, App Review submission, or public release was created.

## Telegram Application Removal Lock (2026-08-25)

- The owner approved one bounded application-only Telegram removal from authoritative `origin/main` at `ab4660f4`. This section supersedes the earlier Telegram setup, preview, manual-copy, provider-send, and future live-location planning sections as current application state.
- Telegram application controls, routes, helpers, provider calls, and Telegram-specific planning documents are removed. The removed surface is exactly two manual-copy controls, three admin Telegram routes, four Telegram helpers, three obsolete Telegram documents, and ten Telegram-retention guards.
- `scripts/test-telegram-application-absence-guard.mjs` is the single replacement guard. It prevents Telegram-named application routes, helpers, documents, UI state/handlers/controls, and retired retention guards from returning.
- Telegram privacy denylist protections remain in the established public/API/persistence boundaries. The native admin device-push capability continues reporting `telegram_enabled: false`; this safety assertion is not an application Telegram lane.
- Customer Copy, `Copy + App Link`, Driver Job Link creation/copy/revoke/manual dispatch, Email, WhatsApp/SMS parked controls, Customer/Driver/Admin in-app messaging, native push, notification outboxes/audit, Calendar, GPS, bookings, invoices, payments, payout, PayNow, parser behavior, and all privacy exclusions remain unchanged.
- No Telegram database table, notification-outbox type, or persisted Telegram audit lane existed, so no schema, migration, database write, or Production data cleanup is part of this removal.
- Telegram environment-secret cleanup and bot/provider revocation remain separate external actions requiring exact action-time owner approval. This removal does not read, print, change, or delete any environment value, token, chat ID, provider configuration, or bot.
- Any Telegram reintroduction requires a new explicit owner-approved lane. No provider send, external message, native build, environment change, or Production data action is authorized by this lock.

## Company + Booker Customer Account Identity (2026-08-29)

- The durable Customer Account identity for the established corporate Save + CRM lane is the exact verified Company ID plus exact verified Booker ID. Names, email, phone and passenger text remain display/search evidence only and never authorize an account, merge or customer read.
- A Booker without an approved account always stops for Admin review. Admin must explicitly choose the existing exact candidate or approve creation of a new account; a matching typed name cannot select or create the relationship. One exact approved Company + Booker relationship resolves to one `customer_id`; a different Booker under the same Company remains a separate first-review relationship.
- After that exact relationship exists, selecting the Company + Booker account carries its durable `customer_id` into the existing booking persistence lane without asking who the passenger is again. The typed passenger remains booking-specific, any unverified `traveler_id` is cleared, and changing the passenger does not change billing ownership or reopen account review.
- The additive nullable `bookers.customer_id` foreign key is the relationship source of truth. It has one partial unique index for non-null accounts, no legacy backfill, no RLS/grant change and restrictive update/delete behavior. Existing legacy rows continue through the established reviewed-candidate path and are never inferred, merged or rewritten in bulk.
- The existing authenticated Admin rate-setup CRM read returns only the safe Booker projection `id`, `company_id`, `customer_id`, and `booker_name` alongside its unchanged Company and Traveller records. The unified chooser is built from verified Booker rows, so an approved account remains selectable even when that Booker has no durable Traveller row; Traveller records remain passenger-search evidence only. No rate value, precedence or pricing writer changes.
- Save + CRM continues using the existing booking writer, reload and successful-save reset. It persists only verified `customer_id`, `company_id`, `booker_id` and an explicitly selected still-valid `traveler_id`; a free-typed passenger keeps `traveler_id` null. Customer access/session and invoice consumers continue reading the existing verified IDs and fail closed on partial or ambiguous scope.
- The Customer driver-details row retains Email, SMS, Send In-App and the existing manual Copy fallback. Only the extra visible WhatsApp pseudo-button is removed; its established manual/external handler is not repurposed, and automatic Driver acknowledgement to Customer details/notification remains unchanged.
- Focused protection is `scripts/test-admin-booking-supabase-adapter-contract.mjs`, `scripts/test-admin-dispatch-crm-identity-selectors-guard.mjs`, `scripts/test-admin-dispatch-customer-account-browser.mjs`, `scripts/test-booking-ui-browser.mjs`, `scripts/test-customer-booking-request-pa-identity-guard.mjs`, and the existing Save + CRM, customer access, invoice, Calendar, DSP, push/badge and privacy suites. Mocked browser evidence proves no passenger identity prompt, typed-passenger retention, null unverified Traveller ID, repeat-account reuse, first-time review, ambiguity fail-closed behavior, successful-save reset, and no test booking write.
- Calendar, Operations Calendar acknowledgement sync, invoice layout/workflow/calculation, DSP hourly calculation/time correction, pricing, automatic Driver acknowledgement delivery, notifications/push/badges, native apps, payments, payout, PayNow, Manage Access and QA account 192 remain unchanged. No real booking, provider send, invoice, Calendar event, native build or App Store action is part of this change.

## Bridge Data Centres Customer177 Company + Booker Production Repair (2026-09-01)

- The owner approved one Customer only: Booker `20` / Company `45` / Customer `177` for Bridge Data Centres. Fresh Production readback proved Customer folder `Bridge Data Centres`, Company and Booker contact `Laurel Wong`, exact booking `10873` / row `195`, and no invoice, monthly-draft or customer-access dependency. This repair does not infer or amend any other Customer.
- Booker `20` was still unlinked and had an empty customer-rate map, while its exact verified Traveller `33` carried `MNG S180`, `DEP S170`, `TRF S150`, and `DSP S160`. The established pricing runtime suppresses legacy Traveller rates once an approved Company + Booker account exists, so linking without copying that map would change pricing behavior. The bounded repair therefore changes only Booker `20.customer_id` from null to `177` and copies Traveller `33.customer_rates` exactly to Booker `20.customer_rates` in the same serializable transaction.
- The migration `20260901230229_bridge_customer_177_company_booker_account.sql` locks and revalidates the exact Customer, Company, Booker, Travellers `33` and `34`, booking row `195`, customer contact, invoice, monthly-draft and access-account dependency hashes before writing. Any identity, contact, rate, booking, timestamp or dependency drift aborts the complete transaction. It requires one affected Booker row from the exact pre-repair state, accepts only the exact already-applied post-state when the Production migration record is created, and verifies that every protected hash remains unchanged afterward.
- Traveller `33` retains its complete legacy rate map; Traveller `34` remains untouched. Customer `177`, Company `45`, the raw customer-folder label, Laurel contact fields, booking `10873` / row `195` and its stored SGD1600 reviewed price, invoices, monthly billing, access, Calendar, messages, notifications, push, Driver, providers and every other Customer remain unchanged. The runtime pricing amount is unchanged; only the authoritative source moves from `legacy_traveler` to `account` for the same exact rate map.
- Focused fail-first and passing protection is `scripts/test-bridge-customer-177-company-booker-repair-guard.mjs`. It permits only one persistent `public.bookers` update, forbids Customer, Company, Traveller, booking, invoice, monthly and access writes, and executes the established pricing runtime to prove identical before/after rate and amount with the expected source transition.
- PR `#472` merged the four-file migration, guard, suite registration and ledger checkpoint to `main` as `0760a3313cd9c374454827fe99ee9ecf0d093ce9`. The exact approved serializable transaction committed once through the authenticated Production Supabase SQL lane when the intended rollback-only rehearsal did not replace its final `commit`; no mismatch occurred and no second data update was attempted. The official migration runner then recorded `bridge_customer_177_company_booker_account` as remote version `20260901151036` through the exact already-applied post-state path with zero affected rows.
- Immediate Production readback changed only Booker `20` full-row MD5 from `b893c5a4c08d13c030c429aa814c6ab1` to `0496727d664a2166ecebbc8046ac68e0` because `customer_id` became `177` and its rates became the exact Traveller `33` map. Customer `177` remained `7f4358e08d467eeb3c40d1a3b1769690`; Company `45` remained `dcc73fd7d74372c936b69baae191bcd1`; Traveller `33` remained `e5dfe1e551e29d3ce63e4cee9fbd5577`; Traveller `34` remained `a37e8bfbced4402cb19fc06f23ff9264`; booking `10873` / row `195` remained `501bcc169e8a1e3f87e548c0178bf214` with SGD1600 and its original timestamp. Contact, invoice, monthly-billing, monthly-invoice and access hashes also remained exact, with zero invoice/monthly/access dependencies.
- Signed-in Production Chrome on build `0760a331` navigated away to the Customer dashboard, loaded accounts, and reopened Customer `177`. The dashboard and exact-customer heading show `Bridge Data Centres (Laurel Wong)`; profile edit shows one verified Booker, `Booker / PA: Laurel Wong`, and booking-only `Traveller: Drew Chen`; the Rates tab shows `Bridge Data Centres · Laurel Wong` with `MNG S 180.00, DEP S 170.00, TRF S 150.00, DSP S 160.00`. No Save, invoice, Calendar, message, notification, push, Driver, provider or other Customer action was performed during acceptance.

## Monthly Billing Nine-Booking Company + Booker Production Repair (2026-09-02)

- The owner approved exactly nine August Monthly Billing bookings whose existing Customer profiles already contain one unambiguous Company + Booker pair: `10853`, `10857`, `10858`, `10862`, `10872`, `10877`, `10878`, `10890`, and `10896`. Traveller remains optional and null on all nine; no passenger, folder, contact, email, phone, or name text establishes identity.
- The bounded migration `20260902084500_monthly_billing_nine_company_booker_repair.sql` changes only `bookings.company_id` and `bookings.booker_id` on exact rows `175`, `179`, `180`, `184`, `194`, `199`, `200`, `212`, and `218`. Every Customer, Company, Booker, contact, price, booking status, route, timestamp, invoice, monthly draft, access account, Driver Job Link/status, DSP timing event, closeout, notification, Calendar/provider and unrelated booking field is protected by exact preconditions and before/after hashes.
- All seven authoritative Company rows and all seven authoritative Booker rows have empty customer-rate maps, every target booking has no Traveller, and the seven Customers have zero invoice, monthly-draft, and portal-access dependencies. Therefore the identity repair does not change current pricing precedence or amount and creates no access, invoice, draft, provider, Calendar, message, push or notification action.
- Customer `174` remains untouched because current Production binds its profile to Booker `33`, while an earlier owner instruction named Booker `15`, booking `10883` still uses Booker `15` with Traveller `28`, and the Customer has one portal access account. Customers `166`, `168`, and `186` also remain untouched because they do not yet have one verified Company + Booker profile.
- The serializable transaction accepts only the exact audited pre-state or the exact already-applied post-state, locks all in-scope rows, requires exactly nine affected bookings on first application, and aborts on any identity, timestamp, rate, dependency, or protected-hash drift. Focused protection is `scripts/test-monthly-billing-nine-company-booker-repair-guard.mjs`.
- A first rollback-only Production rehearsal aborted on an invalid final aggregate expression before commit, so PostgreSQL rolled back the attempted changes and no row changed. After the expression was narrowed to one scalar exact-count assertion, the complete guarded transaction passed in Production and ended with an explicit `ROLLBACK`; a fresh admin-gated read confirmed all nine rows still held the exact original tuples before the one authorized application.
- The one authorized Production transaction then committed successfully exactly once. Immediate independent admin/API and direct database rereads confirmed exact tuples `175→32/36`, `179→31/24`, `180→60/38`, `184→30/29`, `194→42/35`, `199→46/31`, `200→46/31`, `212→42/35`, and `218→51/34`, with Customer IDs and null Traveller unchanged. Protected hashes remained exact: bookings `e6ae44c22dd96fec9798b0fb65e6daa8`, `908b3de7d82b2f835bb922a795aebb57`, `efe162c451fdd4c65223df9e1ba66a80`, `a5effdeb93f9542362759f02b32a71a0`, `271692cd47499d84484b488946437fec`, `53c79a0bec6545f4015b0715f00f84c0`, `c5ce69307992a96e20bc6d881797c4aa`, `bf83a134c1d8547b5ef46e841cc3b25e`, and `83cf2d84e6bb1666ec764961b5a732ad`; contacts `3ba7dd84fcc24300b8a0e744fe1266cf`; Driver links `5990f4359b692bb77433150ed7550041`; Driver statuses `4e33f0aabf36726a673b6879bab5c02d`; DSP timing `b834634a9b551fc8cfd5193431a847d6`; customer/driver notifications `5fe1edebea5630f67e2866d93a67c036`; and the empty invoice, monthly-plan, monthly-invoice, access, closeout, and admin-notification sets `d751713988987e9331980363e24189ce`.
- Signed-in Production Chrome refreshed the existing Monthly Billing review UI without clicking Review, Resolve, or any save control. The grouping read returns the exact repaired Company + Booker IDs for all nine rows and none retains an identity reason. Existing booking/customer display labels were deliberately left unchanged; this data repair does not certify those legacy labels as Company + Booker titles. Eight rows show only `Completed job closeout is missing.` and `10857` shows only `DSP billing time needs review.`; their existing `Needs review` state is therefore preserved rather than blindly dismissed.
- The owner separately confirmed Customer `180` Booker identity as Alson, with Mona Hum remaining passenger only. Read-only Production verification confirms Booker `31` is `Alson`, bound to Company `46` and Customer `180`; bookings `10877` and `10878` now reference Booker `31`, while each booking's existing passenger remains `Mona Hum`. No passenger, contact, title, or other booking field was rewritten by this repair.
- Bounded closeout checks passed the focused repair guard, Monthly Billing grouping contract/workflow lock, Company + Booker root/backfill, invoice identity/scope, push, notification, Driver billing privacy, public privacy, lint, build, booking UI, Driver Job Page and mobile viewport suites. The broader safe suite then stopped on the unchanged current-main customer-company-profile browser fixture expecting `Transzend Groundbooker (David Kelly)` while its isolated runtime rendered the legacy raw folder and no identity editor; this branch has no `app`, `lib`, package, or lockfile diff from `origin/main`, so that unrelated existing failure was recorded and not repaired here. The unchanged current-main Calendar contract also retains its previously documented stale `if (assignmentOnly) { const retainedAssignmentBooking` source-selector failure; no Calendar guard or source was altered.
- PR `479` merged as `996d6f3965b46093c081def86a587cb472fce221`; the merged Vercel deployment passed and signed-in Production reported `Build 996d6f39` with no failed resources, Next.js error overlay, or visible application-error text. Because the owner's Mac login password does not unlock the Supabase CLI keychain, that path was aborted and not retried. The already-authenticated Supabase dashboard recorded only the already-applied migration in `supabase_migrations.schema_migrations`: version `20260902084500`, name `monthly_billing_nine_company_booker_repair`, creator `william@prestigelimo.sg`, one statement, exact file MD5 `ea3afa120ffe60969d46ecd29c77bc19`. A guarded rollback-only history rehearsal passed first; the history write did not rerun the booking update.

## Company + Booker Release-Gate Alignment (2026-08-29)

- Exact untouched-main reproduction proved four broad release failures were established test/lint drift rather than effects of the Company + Booker workflow. The Customer portal app-smoke now waits for the existing resolved Customer access state and its rendered `Driver / Admin alerts` control before asserting it; `/my-bookings`, Customer auth, alerts, push and fallback behavior are unchanged.
- The global preactivation guard now recognizes only the established verified-Admin Email-AI review `PATCH` beside its existing `GET`, requires the exact Admin/Dispatcher boundary and exact `PATCH` server-session allowlist, and continues forbidding every other write verb and direct live activation. The Email-AI route and review writer are unchanged.
- The Admin sign-in mount initializer now runs in one cancellable zero-delay browser task so React state is not synchronously updated inside an effect. The same checking, ordinary six-digit PIN sign-in, recovery-token parsing/clearing, invalid-recovery result, wording, redirect and native WebView route remain in place; this alignment does not claim the earlier transient physical spinner was caused or repaired without a fresh reproduction.
- The mobile archive browser test explicitly opens the established `Dispatch` tab before checking its Dispatch-only production anchor. Dashboard remains the owner-approved default, the internal QA archive remains absent, and neither tab navigation nor archive/runtime behavior changes.
- The Load Bookings typed-read query-shape and DB-read env/table-policy guards now classify the already-established bounded nullable `luggage_count` booking scalar and exact safe nested relation fields `booking_service_items.item_type`, `.quantity` and `.notes` as safe. Focused saved-booking and customer-price contracts plus the earlier ledger checkpoints already require that exact query shape; both guards retain their write ban, forbidden relationships, unsafe-source privacy denylist and quarantined finance/internal fields. The runtime query, mapper, DTO, Customer booking inputs/validation, notes/special requests, SMS/session access and customer-visible workflow remain unchanged.
- The historical Admin Build 5 all-native-alert guard now reads the authoritative accepted native baselines when it inspects current configuration: Admin Build 7, Customer Build 12 and Driver Build 17. Its historical Build 5 checkpoint wording remains unchanged, and no native configuration, notification event type, payload, recipient, badge, subscription, provider, TestFlight or governance behavior changes.
- The historical Admin Build 2/3 release guard likewise reads accepted current Admin Build 7 while preserving its original Build 2/3 ledger evidence and every app-name, version, bundle, iPhone-only light-mode, EAS project, App Store target, notification-plugin and associated-domain lock. No Admin native configuration or release state changes.
- The compact Dispatch feedback guard now requires the already-established responsive Customer live-location helper class introduced by the approved Admin mobile/Customer Copy layout repair: full-width rounded rectangle on phones and compact rounded pill from the small breakpoint. The helper element, wording, readiness state, live-location behavior, Dispatch controls and runtime layout remain unchanged.
- The same historical compact-feedback guard now requires the current Save + CRM success key to include the already-resolved billing-identity account override, matching the established one-shot Billing Identity Review continuation. The save writer, account decision, Calendar continuation, feedback and reset behavior remain unchanged and separately protected by the focused billing-review guard.
- The ledger preactivation-registration guard now reads only script references that form the registration claim before the suite target, so browser evidence named later in the same sentence is not falsely treated as a suite entry. It also excludes only scripts explicitly retired by the current Telegram application-absence guard while requiring that replacement guard itself to remain registered. Untouched current main reproduced the prior three-item failure; no browser runner, Telegram application lane, runtime, provider, data or Production behavior changes.
- The Driver payout-rules app-wiring guard no longer requires the retired new-Traveller insert and created-Traveller payout branches removed by the verified DSP customer-rates sync. It continues requiring payout runtime before the exact existing-Traveller update and now explicitly rejects both retired creation markers. Payout runtime, rate precedence, Customer pricing, PayNow and every write path remain unchanged.
- The historical Company/Traveller CRM approval-packet guard now preserves the current Company identity/rate insert split while requiring the retired new-Traveller CRM payload and legacy Traveller insert helpers to remain absent. Exact existing-Traveller selection/update remains authoritative; no CRM, rate, payout, booking, parser or database runtime changes.
- The Company/Traveller CRM runtime action contract keeps its generic server-side verified Traveller create/update capability tests, but its Admin rate-override wiring assertions now reject the retired Traveller identity-create payload and legacy insert fallback. The current rate screen remains existing-Traveller-only; the runtime helper, route, gate, safe fields and all unrelated consumers are unchanged.
- The CRM runtime env/table-policy guard retains the generic server helper's exact Traveller insert/update table policy while requiring the Admin rate-override flow's retired closed-gate Traveller insert fallback to remain absent. This is test alignment only and changes no table, policy, environment gate, route, helper or write behavior.
- The CRM activation-readiness guard likewise distinguishes the reusable server route's tightly bounded Traveller capability from the Admin rate screen's current existing-Traveller-only wiring. It rejects the two retired Traveller-create markers while keeping Company CRM split, Traveller rate update, safe-field, gate, parser, Save + CRM and saved-booking separation checks intact.
- The older CRM identity/rate payload-split guard now slices the current Company identity payload and legacy Company insert through their real boundaries, preserves the separate existing-Traveller rate payload/update, and explicitly rejects the removed Traveller identity payload and insert fallback. The source split, generic server route and established rates behavior are unchanged.
- The WhatsApp provider no-send packet now preserves the parked setup-only route and shared disabled-send helper while requiring the owner-removed visible WhatsApp pseudo-action to stay absent. Provider packages, environment reads, live APIs, sends, database writes and every remaining Email/SMS/In-App/Copy action remain protected and unchanged.
